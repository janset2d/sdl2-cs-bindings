# SDL2 Upstream Test Assets

These assets are copied from `libsdl-org/SDL` branch `SDL2`, `test/`, for the ClangSharp raw ABI test slice.

Source clone used during authoring: `spikes/binding-generators/references/SDL`.

Files:

- `icon.bmp` from `test/icon.bmp`
- `sample.bmp` from `test/sample.bmp`
- `sample.wav` from `test/sample.wav`
- `SDL-LICENSE.txt` from upstream `LICENSE.txt`

Do not copy the upstream `test/shapes/` directory into this slice.
