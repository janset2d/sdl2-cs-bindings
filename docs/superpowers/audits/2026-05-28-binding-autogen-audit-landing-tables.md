# Binding-Autogen Doc Consolidation — Audit Landing Tables

**Date:** 2026-05-28
**Status:** In progress
**Scope:** Phase 0 evidence for the [doc consolidation design spec](../specs/2026-05-28-binding-autogen-doc-consolidation-design.md) Rule 1 audit gate.

## Purpose

Per-file unique-info-landing tables for every retiree. No `git rm` until each retired file has a verdict + destination map for its unique kernels.

## Audit Format

Each entry follows:

```text
### File: <path>

**Sections audited:**
- <section heading>: <unique kernel description> → <destination doc + section>
- <section heading>: duplicate of <other doc + section> → no fold needed
- <section heading>: sunset-specific / no durable kernel → no fold needed

**Verdict:** ready to git rm | additional fold needed
```

## Audit Entries

(Entries added per Task 0.2–0.10 below.)

### File: docs/parking-lot/binding-autogen-cake-implementation/README.md

**Sections audited:**

- "Archived/Reason" header: explains the toolchain re-evaluation rationale and links ADR-004 — duplicate of identical sunset banner already in constitution (§"Toolchain re-evaluation (2026-05-23)") and roadmap → no fold needed.
- "What lived under build/_build/Targets/GenerateBindings/" inventory paragraph: enumerates the sunset Cake impl's ~30-file `ModelBuilding/` layout (parser/model/emitter separation, semantic type classification, source-first macro pipeline, multi-platform parse-view merge, dynapi name validation, fixture-backed tests, SDL2.Core ABI blockers) — sunset-specific path enumeration → no fold needed; the toolchain-neutral parser/model/emitter separation principle is covered by constitution §"Generator Engine And SDL Policy".
- "Archived plans" table: maps each milestone file to its "Architectural-intent reference for..." disposition (safety-harness pattern; parser/model/emitter separation; manifest-config-vs-code-owned-policy boundary + family profile shape) — duplicate of the dispositions captured in milestone-specific audit entries below → no fold needed at README level.
- "Why preserved rather than deleted" bullets: git-history continuity rationale plus a sentence asserting the toolchain-neutral intents survive — sunset-specific archival rationale, becomes obsolete once the parking-lot folder retires; constitution §"Generator Engine And SDL Policy" already encodes the surviving principles → no fold needed.
- "What is *not* archived here" bullets: callouts that constitution / roadmap / testing-strategy / playbooks remain canonical — sunset-specific cross-reference list that is replaced by the consolidation spec's new doc topology → no fold needed.
- "Unparking" paragraph: speculation about possibly unparking milestone plans back to `docs/binding-autogen/milestones/` if the spike selects CppAst — sunset-specific speculation invalidated by ClangSharp Layer 1 closure (2026-05-28) and by Q10's audit-and-retire decision → no fold needed.
- Note: family profile shape enumeration (`sdl2-core`, `sdl2-satellite`, `sdl2-gfx`, `sdl3-core`, `sdl3-satellite`) appears in the M3 row of the archived-plans table — already covered in roadmap §"Milestone 3" → "Profile direction" list → no fold needed.

**Verdict:** ready to git rm. Pure parking-lot index meta-document; every durable kernel either appears in the milestone-specific audit entries below or is already in the destination docs.

### File: docs/parking-lot/binding-autogen-cake-implementation/milestone-1-safety-harness-baseline.md

**Sections audited:**

- "Goal" / "Architecture" header pair: defines the safety-harness pattern (snapshot gates around generated file sets, fake task orchestration via `FakeCakeWorld`, embedded `.h` fixture integration coverage, opt-in real generated-preview snapshot gate, RED/GREEN discipline before refactor, behavior-preservation invariant) — toolchain-neutral principle. Partially covered by testing-strategy §"GenerateBindings Snapshot Conventions" (Verify snapshots, `.verified` baselines, opt-in `JANSET_VERIFY_GENERATED_PREVIEW=1` gate, Linux-only semantic fixture rule). Also covered by roadmap §"Operating Rules" (RED/GREEN bullet) and the implicit testing layer model. **Additional fold needed:** the explicit *snapshot-first, fixture-backed, embedded-`.h`-fixture integration before refactor* pattern — landing place per design spec §"New Documents — Content Outlines" → `binding-generator-implementation-notes.md` does not currently have a section for this; the closest destination is `canonical/testing-strategy.md` (renamed in Phase 1). Specifically: add a "Refactor Safety Harness Pattern" subsection to testing-strategy that codifies: snapshot generated file sets + fake-task orchestration output as RED before topology changes; embedded-`.h` fixtures as primary parser/model evidence; opt-in real-preview inventory hashes; commit-`.verified`/never-commit-`.received` discipline as a refactor gate, not just a snapshot housekeeping rule.
- "Non-Negotiables" bullets: behavior preservation, `git mv` rule, `dotnet add package` for NuGet, approval gates, native-free inner loop, Verify hygiene — partially duplicate of AGENTS.md operating rules; the *normal build-host test runs stay native-free* rule is durable. Already in testing-strategy §"Build-Host Unit Tests" ("do not require Docker/libclang/vcpkg for ordinary refactor unit tests") and §"Flakiness And Scope Rules" → no fold needed.
- "Implementation Notes" bullets: Verify 31.16.x ↔ TUnit 1.43.x compatibility constraint; `global using VerifyTests` collision with `Build.Tests.Fixtures.TempDirectory`; `UseProjectRelativeDirectory` (not stale `VerifyBase`) configuration; MTP exit code 8 for skip-only filtered runs requiring `--ignore-exit-code 8`; Linux-only semantic snapshot Docker invocation pattern (`mcr.microsoft.com/dotnet/sdk:10.0.203` mounted at `/workspace`) — toolchain-neutral testing infrastructure facts; MTP exit code 8 + Docker Linux container pattern already in testing-strategy §"GenerateBindings Snapshot Conventions". The Verify+TUnit version-pinning constraint is sunset-specific package combo → no fold needed. The `global using VerifyTests` collision note is sunset-specific.
- "File Structure" planned files list: Verify configuration, conventions test, shared semantic fixture parser, generated file-set snapshot, fake task orchestration snapshot, opt-in generated-preview attribute + test, two embedded `.h` fixtures (`platform-conditioned-functions.h`, `manual-policy-surface.h`), semantic model snapshot test — sunset-specific Cake-impl test-tree layout. The *types* of tests (file-set snapshot, fake orchestration, opt-in real-preview, fixture-driven semantic snapshots) are toolchain-neutral and already covered conceptually in testing-strategy §"High-value characterization fixtures" → no fold needed.
- "Task 1" baseline verification before new tests: pure execution scaffolding (git status, dotnet test, `tools.cs -- generate-bindings`, compile-check command) — sunset-specific tool invocations → no fold needed.
- "Task 2" Verify infrastructure: `dotnet add package Verify.TUnit`/`Verify.DiffPlex`, `.gitignore` `*.received.*` rule, `.gitattributes` `*.verified.*` line endings, `[ModuleInitializer]` configuration, `VerifyChecks.Run()` conventions test — toolchain-neutral Verify hygiene. Source-control conventions (`.gitignore`/`.gitattributes`/never-commit-`.received`) already in testing-strategy "Rules" bullet list → no fold needed.
- "Task 3" extract shared semantic fixture parser (`SemanticFixtureParser.cs`): `git mv`-then-replace pattern, `parseAsSdl2Header` toggle, `CppParserOptions` with `ParserKind = CppParserKind.C` / `TargetSystem = "linux"` / `ParseMacros` / `Defines` / `AdditionalArguments.Add($"-U{undefine}")` — sunset CppAst-specific test fixture infrastructure → no fold needed.
- "Task 4" snapshot deterministic generated file sets: `CsCommandEmitter.Emit(BindingModelData.ModelWithRichParseViewEvidence(), new BindingEmissionOptions("SDL2", "SDL"))` projection with ordinal sort + LF normalization + `GeneratedFileSnapshot(RelativePath, Content)` record — sunset Cake-impl type names. Toolchain-neutral kernel: snapshot deterministic file-set output as a refactor gate. Covered conceptually under the additional fold needed for testing-strategy noted above.
- "Task 5" fake task orchestration snapshot via `FakeCakeWorld` + `ICppAstParseRunner` substitute + emitted-files + log messages projection: sunset-specific Cake/Frosting/NSubstitute setup → no fold needed; fake-orchestration testing is build-host-test-host-specific.
- "Task 6" semantic model snapshot fixtures: `platform-conditioned-functions.h` (Neutral/Linux/Windows split via `JANSET_TEST_*_VIEW` defines); `manual-policy-surface.h` (manual macro include/exclude/override coverage including `SDL_HINT_*` string macros, `SDL_FIXTURE_OVERRIDE 1u`, `SDL_ASSERT_LEVEL 1`, function-like `SDL_BUTTON(X) (1u << ((X)-1))`, computed-flag `SDL_TEST_COMPUTED_FLAG (1u << 4)`) — sunset-specific test fixture content; toolchain-neutral kernel is "use small embedded `.h` fixtures to characterize policy seams" which is already covered by testing-strategy §"Build-Host Unit Tests" → "prefer small embedded `.h` fixtures for parser/model behavior" → no fold needed.
- "Task 7" opt-in real generated preview snapshot gate: `GeneratedPreviewSnapshotAttribute : SkipAttribute` + `JANSET_VERIFY_GENERATED_PREVIEW=1` env var + SHA256 hashing per file + `FindRepositoryRoot()` ascent looking for `tools.cs` — sunset-specific TUnit attribute mechanism; env-var-gated opt-in already in testing-strategy → no fold needed.
- "Task 8" milestone verification commands: ordered command list (build-host suite, `tools.cs -- generate-bindings`, compile-check, `git --no-pager diff --check`, `slopwatch analyze`) — sunset-specific tool invocations; testing-strategy already covers the layered gate philosophy → no fold needed.
- "Task 9" docs updates after implementation (testing-strategy update, roadmap M1 evidence) — sunset workflow → no fold needed.
- "Task 10" review and commit gate — sunset workflow → no fold needed.
- "Self-Review Checklist" — sunset workflow → no fold needed.

**Verdict:** additional fold needed. Add a "Refactor Safety Harness Pattern" subsection to `canonical/testing-strategy.md` (the renamed/purified Phase 1 destination) that promotes the snapshot-first / embedded-`.h`-fixture / fake-orchestration / opt-in-real-preview composition from M1 design-intent into an explicit refactor gate, so future toolchain changes have a documented pattern to reproduce (the spike's ClangSharp + Roslyn implementation will need its own equivalent harness before Layer 2 production flip). Everything else is sunset-specific Cake/CppAst plumbing safe to retire.

### File: docs/parking-lot/binding-autogen-cake-implementation/milestone-2-behavior-preserving-topology-refactor.md

**Sections audited:**

- "Goal" / "Architecture": parser/model/emitter separation principle, target-centric build-host layout, extract per-family workflow from Cake task, split current buckets into parse/platform/model-building/emission concepts, collapse one-line records into cohesive files, make SDL2-specific policy visible — toolchain-neutral separation principle already covered by constitution §"Generator Engine And SDL Policy" ("core pipeline is an ABI engine for parsed declarations, native type classification, platform parse-view merge, raw ABI projection, and deterministic file-set emission") → no fold needed.
- "Non-Negotiables": preserve generated output unless RED test, `git mv`, no `Shared`/`Common`/`Features`/`Pipeline`/`Runner`/`Operation`/`Processor` bucket names, no ceremonial interfaces, no DI conversion without workflow/policy/test/profile justification, target-local until second target consumes — toolchain-neutral collaborator extraction discipline; partially in `docs/knowledge-base/extraction-guidelines.md` (referenced by constitution). The *banned bucket-name list* (Shared/Common/Features/Pipeline/Runner/Operation/Processor) is durable architectural taste → already covered by extraction-guidelines or AGENTS.md → no fold needed.
- "North Star" diagnosis: `GenerateBindingsTask` hiding generation story behind private methods; `Translation/` mixing declaration filtering + type classification + function merge + macro taxonomy + model composition; `Emitting/` mixing file-set orchestration + raw ABI + public artifacts + report + TFM guards; SDL2 concerns spread across multiple policies — sunset-specific code-smell catalog → no fold needed.
- "Design Rules → Task-Centric Build Host Rules": ADR-002 mapping, GenerateBindingsTask retains target lifecycle orchestration, multi-step per-family generation becomes named collaborator, Cake-native IO stays at build boundaries, validators under `Build.Validation.BindingGeneration`, data repositories under `Build.Data.BindingGeneration` — sunset-specific Cake/Build.* namespace mapping; the ADR-002 task-centric pattern survives independent of the binding-autogen workstream → no fold needed.
- "Design Rules → Static Class Decision Rule": when to keep static (pure, stateless, no DI/profile, narrow formatter/factory/helper) vs convert to instance collaborator (workflow order, multi-collaborator composition, policy branches, injected dependencies, Cake IO/logging, growing private helpers); plus "do not add interface unless real second impl / external/process seam / validator/data-boundary convention / important task/test contract" — toolchain-neutral collaborator-extraction discipline; already covered by `docs/knowledge-base/extraction-guidelines.md` (which constitution and roadmap cross-reference) → no fold needed.
- "Design Rules → Manifest Vs Policy Boundary": preview of manifest-owned facts (family id, enabled, managed namespace, public class name, header set, owned prefixes, parse defines/undefines/clang args, explicit excluded/required/deferred declarations, macro overrides/exclusions, validator opt-ins) vs code-owned policy (scalar width mapping, SDL2/SDL3 bool shape, opaque handle detection, callback and function-pointer classification, macro taxonomy, struct/union layout rules, platform view merge rules, TFM/backend compatibility) — fully covered by constitution §"Manifest Configuration Vs Code-Owned Policy" → no fold needed.
- "Target File Structure" Cake-impl folder layout: `HeaderSet/`, `SyntheticHeaders/`, `Parse/`, `PlatformViews/`, `Model/`, `ModelBuilding/{Declarations,Types,Functions,Macros,SdlPolicy}/`, `Emit/{RawAbi,PublicApi,Reports,Tfm}/` — sunset-specific Cake target layout → no fold needed; the *concept partitioning* (parse vs platform vs model vs emit) is the parser/model/emitter principle already captured in constitution.
- "Planned Move Map" 30+-row table mapping current paths to M2 paths — sunset-specific Cake refactor migration → no fold needed.
- "Record Collapse Plan" table: `Model/BindingFunctions.cs` (function + parameter), `BindingStructs.cs`, `BindingEnums.cs`, `BindingDeclaredTypes.cs` (constant + handle + callback grouped), `NativeTypeMetadata.cs` (abi shape + kind + diagnostic + severity); keep `BindingModel.cs`, `NativeTypeRef.cs`, `BindingTypeRef.cs`, `MacroConstantReport.cs` separate — sunset-specific record consolidation choices → no fold needed; the *taste rule* ("collapse cohesive one-line records only when grouped file is a real concept") is generic C# file-organization advice.
- "Task 1" Baseline And Safety RED Tests: scenario tests `RunAsync_Should_Throw_When_Config_Enables_Unknown_Validator` (fail-closed unknown validator semantics) and `RunAsync_Should_Remove_Stale_Generated_Files_When_Regenerating_Family` (stale generated output cleanup before regeneration) — **durable behavior contracts**, not just sunset-specific tests. The principles are: (a) unknown enabled validators must fail closed with named id, not silently no-op; (b) regenerating a family must clean its previous output before writing new files. **Partially covered:** roadmap §"Continuous Maintenance" → "Add stale-generated-output validation before expensive native/package work" hints at (b); constitution does not explicitly state either. **Additional fold needed:** add to constitution §"Manifest Configuration Vs Code-Owned Policy" (or to a new "Generation Process Contracts" sibling subsection) two contract bullets: "Enabled validator ids unknown to the registered validator set MUST fail closed with the unknown id named, not silently no-op" and "Per-family regeneration MUST clean its prior output tree before writing new files; the cleanup MUST be bounded by a path-prefix guard to its family root under `artifacts/generated-bindings-preview/`". These are toolchain-neutral correctness rules any successor implementation (ClangSharp + Roslyn postprocess included) must obey.
- "Task 2" Implement Tiny Safety Fixes: `enabledIds`/`registeredIds`/`unknownIds` HashSet comparison + `CakeException` with named ids; `WriteAsync` with `EnsureSafeGeneratedOutputDirectory(environment, previewRoot, outputDirectory)` doing prefix-segment validation that refuses to clear paths outside `artifacts/generated-bindings-preview/` — sunset-specific Cake exception type + Cake path API. The toolchain-neutral kernel (validator-unknown-id contract + cleanup-prefix-guard) is captured in the previous bullet's fold target.
- "Task 3" Extract `BindingFamilyGeneration` collaborator with `IBindingGenerationConfigRepository` + `HeaderSetResolver` + `ICppAstParseRunner` + `IEnumerable<IBindingFamilyValidator>` + `ICakeLog` dependencies — sunset-specific Cake collaborator extraction; the *named-collaborator-not-Pipeline/Runner* rule already in extraction-guidelines → no fold needed.
- "Task 4" Split Parse And Platform View Concepts: `git mv` from `Parsing/` to `Parse/` + `PlatformViews/`; namespaces `Build.Targets.GenerateBindings.Parse;`, `...PlatformViews;` — sunset-specific Cake namespace shuffle → no fold needed.
- "Task 5" Reorganize Model Building Topology: `git mv` from `Translation/` to `ModelBuilding/{Macros,Types,SdlPolicy,Declarations,Functions}/`; rename `CppAstToBindingModel` → `BindingModelBuilder`; introduces explicit `SdlPolicy/` seam (`SdlWideStringPointerPolicy`, `SdlOpaqueStructPolicy`, `SdlNativeTypeSubstitutionPolicy`, `CoreOwnedTypeMap`) — sunset-specific Cake refactor. The *SDL-policy-as-named-seam* principle is in constitution §"Generator Engine And SDL Policy" ("SDL-specific decisions live behind named policy/profile concepts") → no fold needed.
- "Task 6" Collapse Cohesive Model Records: file-by-file `git mv` plan — sunset-specific → no fold needed.
- "Task 7" Reorganize Emission Topology: `git mv` from `Emitting/` to `Emit/{RawAbi,PublicApi,Reports,Tfm}/`; rename `CsCommandEmitter` → `BindingEmitter`; extract `RawAbiCommandEmitter` (with `IncludeLibName` toggle and `EmitCommandsFile`/`JoinParameters` private helpers); extract `BindingParseViewReportBuilder` for report DTO projection — sunset-specific Cake emitter restructure. The *split raw ABI emission from public artifact emission from report generation* principle is implicitly in constitution Layer Contract → no fold needed.
- "Task 8" Update Documentation: README/roadmap link refresh — sunset workflow → no fold needed.
- "Task 9" Full Verification Gate: ordered command list (full suite, Linux-only Docker snapshots, refresh generated preview, opt-in preview snapshot, compile-check, package-consumer smoke, `git diff --check`, slopwatch, ensure no transient Verify files) — sunset workflow → no fold needed.
- "Review Checkpoints" / "M3 Planning Carry-Forward": six review checkpoints + M2-to-M3 carry-forward bullets reiterate the M2-review findings already promoted into the roadmap M3 section (TypeMappingPolicy split, LegacyBindingTypeRefBridge retirement, foreign ABI vs SDL policy naming, SDL2.Core platform macro hygiene, function-only platform scanning insufficiency, Result-shaped record naming, behavior preservation). All of these are already in roadmap §"Milestone 3" → "M2 review findings to carry into the detailed M3 plan" bullets → no fold needed; the durable principles will be carried forward by Phase 1 roadmap restructure into the new Layer 2 / production-flip milestone copy via design spec Q4D.

**Verdict:** additional fold needed. Add to `canonical/binding-generator-constitution.md` (the Phase 1 destination after the constitution moves into the spike) a "Generation Process Contracts" subsection — either as a new sub-section under §"Generator Engine And SDL Policy" or a sibling of §"Manifest Configuration Vs Code-Owned Policy" — codifying: (1) unknown enabled validator ids MUST fail closed with named-id error, never silently no-op; (2) per-family regeneration MUST clean prior output before writing, with a path-prefix guard pinning cleanup to the family's generated-preview root. These are durable correctness contracts proven by RED tests in M2 Task 1, and any successor implementation must satisfy them. Everything else in M2 is sunset Cake refactor mechanics safe to retire.

### File: docs/parking-lot/binding-autogen-cake-implementation/milestone-3-profile-boundary.md

**Sections audited:**

- "Goal" / "Architecture": `GenerateBindingsTask` resolves enabled generation plans once and passes target-local plan to `BindingFamilyGeneration`; disabled satellites resolve through config/profile tests without running parse/emit/compile-check/package/smoke; manifest-backed data contracts under `Build.Data.BindingGeneration`, profile composition under `Build.Targets.GenerateBindings`, cross-cutting validation under `Build.Validation.BindingGeneration` — toolchain-neutral profile-boundary architecture. Already partially covered by constitution §"Manifest Configuration Vs Code-Owned Policy" and roadmap §"Milestone 3" engine-owned-concepts/manifest-owned-facts/code-owned-policy lists. The *disabled-satellites-resolve-without-side-effects* concept (audit plan vs generation plan) is a durable correctness rule. **Additional fold needed:** add a contract bullet to constitution §"Manifest Configuration Vs Code-Owned Policy" stating "Disabled binding-generation families MUST be resolvable through config/profile checks without invoking parse, model build, emit, validator run, output cleanup, compile-check, or smoke side effects. The audit-plan resolution path exists precisely to enable disabled-satellite manifest hygiene without enabling generation." This survives toolchain swap; the ClangSharp + Roslyn implementation will need the same separation.
- "Non-Negotiables": preserve SDL2.Core output; do not enable SDL2_image/mixer/ttf/gfx generation in M3; do not flip package source/compile-check scope/smoke/production paths for satellites; no SDL3 in M3; **no JSON ABI policy knobs for scalar widths, SDL bool shape, C long, wchar_t, pointer classification, callbacks, variadics, macro taxonomy, platform merge, struct/union layout** — durable manifest-vs-policy boundary already in constitution §"Manifest Configuration Vs Code-Owned Policy" → no fold needed. The *forbidden-JSON-knob enumeration* is also in roadmap M3 section "Non-goals" → no fold needed.
- "Design Reference" / "Target File Structure": planned production additions (`BindingGenerationConfigEnvelope`, `Profiles/{BindingGenerationProfile, BindingGenerationProfileRegistry, DeclarationVisibilityStrategy, Sdl2CoreProfile, Sdl2SatelliteProfile, Sdl2GfxProfile}`, `Planning/{BindingGenerationPlanResolver, BindingGenerationResolutionError, ResolvedBindingGenerationConfig, ResolvedBindingGenerationPlan, ResolvedBindingGenerationAuditPlan}`) — sunset-specific Cake-impl type-naming proposals. The *concept partitioning* (profile registry + plan resolver + resolved-config envelope + audit-plan vs generation-plan) is toolchain-neutral and already covered by roadmap §"Milestone 3" Engine/Manifest/Profile authority split → no fold needed; ClangSharp + Roslyn will land equivalent collaborators with different names.
- "Task 1" Baseline Verification + "Approval Gate Before Implementation": sunset workflow → no fold needed.
- "Task 2" Add Manifest Profile Fields And Data Round-Trip Tests: introduces `profile_id` and `export_macro_names` JSON fields; `LoadConfig` returns `BindingGenerationConfigEnvelope` carrying the full manifest root so target-local planning can derive cross-family facts (e.g., satellite's core managed namespace) without adding `binding_generation.core_*` fields; manifest entries for `sdl2-core` (`profile_id: "sdl2-core"`, `export_macro_names: ["DECLSPEC"]`), satellites (`profile_id: "sdl2-satellite"`), gfx (`profile_id: "sdl2-gfx"`, `export_macro_names: ["SDL2_GFXPRIMITIVES_SCOPE", "SDL2_IMAGEFILTER_SCOPE", "SDL2_ROTOZOOM_SCOPE", "SDL2_FRAMERATE_SCOPE"]`) — toolchain-neutral manifest schema decision. `profile_id` is already in constitution §"Manifest Configuration Vs Code-Owned Policy" manifest-owned-facts list (L162); `export_macro_names` is mentioned in constitution L178 ("M3 may add `export_macro_names` as declaration-visibility token inventory when tests prove it earns its place") and in roadmap M3 manifest-owned-facts bullet ("export macro token inventory if M3 tests prove `export_macro_names` earns its place"). **Additional fold needed:** the gfx four-scope token list (`SDL2_GFXPRIMITIVES_SCOPE`, `SDL2_IMAGEFILTER_SCOPE`, `SDL2_ROTOZOOM_SCOPE`, `SDL2_FRAMERATE_SCOPE`) is durable family-identity evidence; cross-check confirms it appears in `spikes/binding-generators/docs/satellites/sdl2-gfx-header-analysis.md` (4-row table + multiple repeats) so no fold-into-satellite needed, BUT constitution §"Function Surface" already mentions all four tokens by name → no fold needed. The *envelope-carries-manifest-root* pattern (avoid `binding_generation.core_*` fields by deriving from `package_families[].depends_on` + cross-family lookup) is toolchain-neutral derivation discipline already implicit in constitution L180 ("Raw ABI class name, native import name, and satellite core-reference identity are derived first from existing manifest facts ... Promote them into explicit manifest fields only when a RED test proves the convention is insufficient for a real family") → no fold needed.
- "Task 3" Add Profile Registry And Profile Value Types: `BindingGenerationProfile` value type with `SdlBoolWirePolicy`, `DeclarationVisibilityStrategy`, `RequiresCoreFamilyReference`, `UsesFullSdl2CorePlatformCatalog` fields; `DeclarationVisibilityStrategy` with `Kind = SdlDeclspec | Sdl2GfxScopeMacros` + `RequiredMacroNames`; concrete `Sdl2CoreProfile`/`Sdl2SatelliteProfile`/`Sdl2GfxProfile` static factories; `IBindingGenerationProfileRegistry.Resolve(profileId, exportMacroNames)` returning `Result<BindingGenerationProfile, BindingGenerationProfileError>` — sunset-specific Cake-impl type design. The *DeclarationVisibilityStrategy.Sdl2GfxScopeMacros vs SdlDeclspec* discriminator is the durable kernel: SDL2_gfx uses per-header `SDL2_*_SCOPE` export macros (4 different ones, per-header) while SDL2_image/mixer/ttf use SDL's `DECLSPEC` convention, so declaration visibility is family-specific. **Already covered:** constitution §"Function Surface" (L243) ("SDL2 satellite function names are validated by a family-specific evidence source... For Image/Mixer/Ttf this starts from public `extern DECLSPEC` declarations; for SDL2_gfx it starts from the `SDL2_GFXPRIMITIVES_SCOPE`, `SDL2_IMAGEFILTER_SCOPE`, `SDL2_ROTOZOOM_SCOPE`, and `SDL2_FRAMERATE_SCOPE` declaration macros") → no fold needed. Note "Do not add an empty macro-name overload in M3 tests" Step 5 guidance is sunset test-design detail → no fold needed.
- "Task 4" Add Plan Resolver And Resolution Contracts: `BindingGenerationResolutionError` with `Kind = ConfigLoadFailed | UnknownProfile | ProfileMismatch | Disabled | MissingHeaderSet | MissingCoreFamilyReference | UnknownValidator` and `Mode = "config" | "enabled-generation" | "audit"`; resolver derives `RawAbiClassName = PrimaryClassName + "Native"`, derives `NativeImportName` from primary-binary metadata (e.g., `SDL2_image.dll` → `SDL2_image`), derives core-reference from `package_families[].depends_on` (require exactly one entry for satellite profiles) — toolchain-neutral derivation rules. **Already covered:** constitution L180 derives-first-from-manifest-facts language. The *Mode discriminator (`config`/`enabled-generation`/`audit`) for the task-boundary error message* is a durable usability rule. The *fail-with-ProfileMismatch-rather-than-silently-falling-back-to-new-JSON-field* discipline for `NativeImportName` derivation when manifest metadata is too wildcarded is durable manifest-vs-policy boundary enforcement. **Additional fold needed:** add to constitution §"Manifest Configuration Vs Code-Owned Policy" a sub-bullet under the "Raw ABI class name, native import name, and satellite core-reference identity are derived..." paragraph stating: "Derivation failures (e.g., primary-binary metadata too wildcarded to prove the import name) MUST fail with a typed error citing the family + profile + derivation mode; silently introducing a new JSON field as workaround is forbidden." This codifies the discipline that prevents JSON-knob creep.
- "Task 5" Add Pre-Parse Validation For Validator IDs And Profile Coherence: pre-parse fail-closed validator-id check; profile coherence checks (gfx profile requires SDL2_*-prefixed export-macro names; satellite profile requires exactly one core dependency) — the validator-id-unknown contract is already noted as the M2 Task 1 fold target for constitution. The *gfx-profile-requires-SDL2_*-prefixed-export-macros coherence check* is toolchain-neutral profile-coherence guard, already implicit in constitution §"Function Surface" gfx scope-macro enumeration → no fold needed. The *satellite-requires-exactly-one-core-dependency* rule is durable manifest-shape contract — **already covered** by constitution L180 "satellite core-reference identity are derived first from existing manifest facts (`primary_class_name`, `library_manifests[].name`, and `package_families[].depends_on`)" → no fold needed.
- "Task 6" Integrate Plans Into GenerateBindings Task Flow: `GenerateBindingsTask` switches from `IBindingGenerationConfigRepository` to `IBindingGenerationPlanResolver`; family-iteration uses resolved plans; task-boundary `CreatePlanResolutionException` translates typed `BindingGenerationResolutionError` to `CakeException` with `$"... family '{error.FamilyId}', profile '{error.ProfileId ?? "<unresolved>"}', mode '{error.Mode}' ({error.Kind}): {error.Reason}"` shape — sunset-specific Cake task wiring. The *task-boundary-translates-typed-error-to-host-exception* pattern is toolchain-neutral correctness; already in extraction-guidelines or AGENTS.md → no fold needed.
- "Task 7" Move Emission Identity To The Resolved Plan: `BindingEmissionOptions` constructor takes `managedNamespace`, `primaryClassName`, `rawClassName`, `nativeImportName`; `RawAbiCommandEmitter` emits `private const string LibName = "{options.NativeImportName}";` instead of hardcoded `SDL2` — sunset-specific Cake emitter API. The toolchain-neutral kernel (raw ABI class name + native import library name must come from family identity, not be hardcoded) is in constitution §"Family Identity" table → no fold needed.
- "Task 8" Select SDL Bool Policy Through Profiles Without Changing SDL2 Output: `NativeTypeClassificationContext` gains `SdlBoolWirePolicy`; `SDL_bool` mapping moves from static `TypeMappingPolicy.ExplicitTypedefMap` to `NativeTypeClassifier.ClassifyTypedef` using the context bool policy; SDL2 retains int-backed `SDL_bool` (managed name `int`, size 4) — the policy itself (SDL2 `SDL_bool` int-backed; SDL3 separate policy not yet implemented) is already constitution §"SDL_bool" → no fold needed. The *SDL2-specific-SDL_bool-mapping-belongs-in-profile-not-generic-TypeMappingPolicy* M2-review principle is already roadmap M3 review-findings bullet 1 ("TypeMappingPolicy still mixes generic primitive-width mapping... M3 should split those responsibilities so SDL2/SDL3 bool shape is selected through an explicit SDL profile/policy seam") → no fold needed. Fixture `sdl2-satellite-callbacks-long-and-bool.h` proves satellite C `long`/`SDL_bool`/by-value-`SDL_Color`/callback shapes — sunset fixture; cross-check: TTF `TTF_OpenFontIndex` C `long` param and `Mix_EffectDone_t` callback are already in `sdl2-ttf-header-analysis.md` and `sdl2-mixer-header-analysis.md` → no fold needed.
- "Task 9" Retire `LegacyBindingTypeRefBridge`: replace bridge with `RequiredFunctionNativeTypeParser` — sunset-specific Cake bridge retirement, already in roadmap M3 review-findings bullet 2 ("LegacyBindingTypeRefBridge is a spike-era adapter...") and exit-evidence bullet 7 ("LegacyBindingTypeRefBridge is removed") → no fold needed. The parser's enumerated test cases (`int`, `uint`, `void`, `CLong`, `CULong`, `SdlBool`, `Utf8Pointer`, `WcharTPointer`, `ConstStructPointer`, multi-pointer depth, `byte**`, unsupported spellings) form a useful translation-test catalog; covered conceptually by testing-strategy §"High-value characterization fixtures" → no fold needed.
- "Task 10" Add Declaration Visibility Strategy Fixtures: two fixtures (`sdl2-gfx-scope-macros.h` with 4 scope macros + 4 declarations; `sdl2-satellite-declspec-and-core-types.h` with `DECLSPEC` + `IMG_Load`/`IMG_Load_RW`/`IMG_FreeAnimation`); tests prove (a) gfx profile recognizes scope-macro decls as bindable, (b) satellite profile recognizes DECLSPEC decls as bindable, (c) core-owned `SDL_Surface`/`SDL_RWops` not emitted as satellite-owned types, (d) real installed SDL2_gfx headers still contain configured scope macro tokens (opt-in real-header audit only); also notes "CppAst sees preprocessed declarations, so do not make the whole strategy depend on recovering original macro tokens from `CppFunction` nodes after preprocessing" — toolchain-neutral profile-boundary correctness. The *post-preprocessing-token-recovery-is-not-reliable* note is CppAst-specific framing of a real issue but the principle ("declaration-visibility policy must work from a configurable token inventory, not from grep-the-original-source") is durable. **Already covered:** constitution L243 lists the four gfx scope macros and the DECLSPEC distinction. The *core-owned-types-MUST-NOT-emit-as-satellite-owned* contract is the cross-family `auto_detect_well_known` ∪ `force_opaque_exceptions` data pull discipline in constitution §"Opaque Handles" "Cross-family handle name resolution (rewriter input set)" → no fold needed.
- "Task 11" Add Disabled Satellite Audit Path Without Emission: `ResolveAuditPlan` returns `ResolvedBindingGenerationAuditPlan` wrapping resolved config; does NOT call header resolution, parser, model builder, emitter, validators, output cleanup, compile-check, or packaging — already captured in the Task 2/3 fold target above for constitution "Disabled binding-generation families MUST be resolvable through config/profile checks without invoking parse, model build, emit, validator run, output cleanup, compile-check, or smoke side effects."
- "Task 12" Focused Linux/Header Readiness Command: `docker run --rm --entrypoint bash -v "${repo}:/workspace" -w /workspace -e REPO_ROOT=/workspace -e RID=linux-x64 janset-binding-generator:focal-latest -lc "dotnet test ... --filter 'FullyQualifiedName~RealHeaderReadiness'"` pattern — toolchain-neutral *focused-Docker-command-override-instead-of-running-full-generation-for-narrow-readiness-checks* pattern. **Additional fold needed:** the new `canonical/binding-generator-maintenance.md` playbook §"Linux-Canonical Generation" (planned per design spec content outline) should include this focused-command-override pattern. Specifically: when proving a narrow Linux/header readiness fact, prefer `docker run --rm --entrypoint bash ... -lc "<focused test filter>"` over `dotnet run --file tools.cs -- generate-bindings`. This is build-host plumbing wisdom that survives the ClangSharp swap (the docker image will be the spike's `docker/binding-generator.Dockerfile`).
- "Task 13" Documentation Updates: roadmap M3 detailed-plan link; constitution `Manifest Configuration Vs Code-Owned Policy` updates promoting `profile_id` to required field + noting `export_macro_names` token-inventory candidacy + noting raw-ABI/native-import/core-reference-derivation discipline; maintenance playbook profile-boundary maintenance rules — sunset workflow already encoded in current docs (constitution L162 has `profile_id`; constitution L178 covers `export_macro_names` candidacy; constitution L180 covers derivation-first; roadmap M3 has the authority split) → no fold needed.
- "Task 14" Milestone Verification: ordered command list (focused tests, full suite, slopwatch, canonical generation, compile-check, generated-output diff review, commit approval) — sunset workflow → no fold needed.
- Satellite reconnaissance evidence in fixture files (Tasks 8 + 10) — TTF C `long` (`TTF_OpenFontIndex` + `long index`, `TTF_FontFaces` return) confirmed in `sdl2-ttf-header-analysis.md` rows 34-40 + 55 + 110-127; Mixer `Mix_EffectDone_t` callback confirmed in `sdl2-mixer-header-analysis.md`; gfx 4-scope-macro list confirmed in `sdl2-gfx-header-analysis.md` rows 17-20; Image function-count + foreign types confirmed in `sdl2-image-cross-check.md` rows 11-12. **No reconnaissance gap found.**

**Verdict:** additional fold needed (two targets). (1) Add to constitution §"Manifest Configuration Vs Code-Owned Policy" a contract bullet codifying: "Disabled binding-generation families MUST be resolvable through config/profile checks without invoking parse, model build, emit, validator run, output cleanup, compile-check, or smoke side effects. The audit-plan resolution path exists precisely to enable disabled-satellite manifest hygiene without enabling generation." (2) Add to the same section a sub-bullet under "Raw ABI class name, native import name, and satellite core-reference identity are derived..." stating: "Derivation failures (e.g., primary-binary metadata too wildcarded to prove the native import name) MUST fail with a typed error citing the family + profile + derivation mode; silently introducing a new JSON field as workaround is forbidden." (3) Add to the new `canonical/binding-generator-maintenance.md` §"Linux-Canonical Generation" subsection a "focused-Docker-command-override" pattern for proving narrow Linux/header readiness facts without running full generation. Everything else in M3 is sunset Cake-impl plumbing safe to retire; the durable principles (profile shape `sdl2-core`/`sdl2-satellite`/`sdl2-gfx`/SDL3 future; manifest-vs-policy authority split; M2 review findings; satellite reconnaissance facts) are already in constitution + roadmap.

### File: docs/binding-autogen/README.md

**Sections audited:**

- "Status (2026-05-23)" banner paragraph: workstream index banner stating active under toolchain re-evaluation, names canonical *policy* docs as constitution/roadmap/testing-strategy, historical specs/transcripts/notes folded for unique current facts, git history is archive — PURE CUT (sunset-banner). Status sentence is moment-in-time; canonical-doc enumeration is replaced by the new design-spec topology (canonical/ subfolder).
- "Toolchain re-evaluation (2026-05-23)" callout: ADR-004 Reopened state, active selection in `spikes/binding-generators/` between ClangSharp + Roslyn postprocess and Alimer-style single-pass CppAst, sunset Cake implementation under `build/_build/Targets/GenerateBindings/`, "Policy in the constitution remains binding regardless; implementation-specific language in this README and the roadmap describes the sunset Cake implementation and will be rewritten when the spike concludes" — PURE CUT (sunset-banner). The toolchain decision posture is replaced by the post-spike "Direction is leaning ClangSharp + Roslyn postprocess based on Layer 1 closure evidence" framing in the new design spec; the new README's Current Decision Posture refresh captures the post-spike state.
- Sunset-implementation overview paragraph (`build/_build/Targets/GenerateBindings/` Cake-hosted CppAst Linux-canonical, `library_manifests[].binding_generation` config, SDL2.Core preview, active spike under `spikes/binding-generators/`) — PURE CUT (sunset-banner / sunset-impl description). Replaced by post-spike "SDL2 Layer 1 raw ABI: closed 2026-05-28 across Core/Image/GFX/TTF/Mixer on the ClangSharp + Roslyn postprocess spike" framing.
- "Folder Layout" table (current state with `binding-autogen/README.md` + constitution + roadmap + testing-strategy + parking-lot + 2 playbooks + ADR-004 + spike root + spike charter row): STAYS in purified version with updated paths to canonical/. Per spec §"Target Doc Topology" the new layout points to `canonical/README.md`, `canonical/binding-generator-constitution.md`, `canonical/binding-generator-roadmap.md`, `canonical/testing-strategy.md`, **NEW** `canonical/binding-generator-implementation-notes.md`, **NEW** `canonical/binding-generator-maintenance.md`, `canonical/binding-output-oracle-validation.md`, `canonical/satellites/`, `canonical/references/ppy-reference-analysis-2026-05-25.md`. Parking-lot row CUTS (folder retires per Q10). ADR-004 row updates to neutral banner per Q13. Spike charter row (`spikes/binding-generators/docs/generator-spike-goals.md`) CUTS (Q6 retire).
- "Reading Order" table (9-row reading sequence): STAYS in purified version with refreshed sequence pointing at new sibling docs (constitution → roadmap → testing-strategy → implementation-notes → maintenance → oracle-validation → satellites). Row 4 (parking-lot) CUTS (Q10 folder retires). Row 8 (ADR-004 "Reopened") updates to neutral banner per Q13. Row 9 (active spike re-evaluation) reframes post-spike-closure (spike now contains canonical/ subfolder, not toolchain comparison).
- "Historical research and task-by-task plans were intentionally removed..." trailing paragraph: PURE CUT (sunset-specific archival rationale; one-time consolidation note). The purification gate is now the design-spec Rule 1 unique-info audit, not a one-time README assertion.
- "Current Decision Posture" 14-bullet list:
  - Bullet 1 ("Toolchain selection is open. ADR-004 Reopened... active selection happens in `spikes/binding-generators/` between ClangSharp + Roslyn postprocess and Alimer-style single-pass CppAst..."): PURE CUT (sunset-banner content). Replaced with refreshed post-spike posture: SDL2 Layer 1 raw ABI closed 2026-05-28 on ClangSharp + Roslyn postprocess across Core/Image/GFX/TTF/Mixer; ADR-004 formal amendment deferred to post-Layer-2.
  - Bullet 2 ("Priority C closure design (2026-05-24)..."): STAYS in purified version (refreshed) — the four key decisions (uniform Pattern B typed handles + force-opaque allow-list, C `long` hybrid, shared `wchar_t*` opaque, per-header RSP organization migration) are durable Layer 1 facts. The "Toolchain-neutral policy decisions integrated back into the constitution" framing stays; the link target updates to the new canonical/ home of the spec.
  - Bullet 3 ("Public API shape is internal raw ABI externs + public typed low-level API + friendly overloads..."): STAYS (durable Layer Contract summary; matches constitution §"Layer Contract" rules and is consistent with Q3C constitution-purification scope).
  - Bullet 4 ("Output class identity is family-based, manifest-driven, and not parse-view-based..."): STAYS (durable family-identity policy; consistent with constitution §"Family Identity" table).
  - Bullet 5 ("String-like SDL macro constants such as `SDL_HINT_*` use canonical `ReadOnlySpan<byte>` UTF-8 literal properties..."): STAYS (durable macro-translation rule; consistent with constitution §"Constants And Macros" rules).
  - Bullet 6 ("Generator is build infrastructure..."): STAYS in purified version with sunset-impl-path enumeration cut. The "Whichever toolchain the spike selects..." framing CUTS (post-spike-closure: spike has selected ClangSharp + Roslyn postprocess for Layer 1 evidence); the build-infrastructure principles (no consumer-build codegen, committed `.g.cs`, per-family `.generated-stamp`, validators reachable from PreFlight/Pack, local invocation via `tools.cs`) STAY. The "sunset Cake-hosted implementation lived under `build/_build/Targets/GenerateBindings/`..." sentence CUTS to implementation-notes §11 (Hardcoding Rules) as historical reference; spec content outline doesn't require it.
  - Bullet 7 ("Generator architecture separates ABI engine from SDL policy..."): STAYS in purified version. The "ClangSharp orchestrator + Roslyn postprocess and CppAst single-pass emitter are both viable engine shapes; the spike is selecting" sentence CUTS (post-spike-closure: ClangSharp + Roslyn selected). The "reusable core normalizes parsed declarations, builds semantic models, projects raw ABI, and emits deterministic file sets; SDL-specific choices belong behind named policy/profile concepts" principle STAYS (durable engine-vs-SDL-policy boundary, consistent with constitution §"Generator Engine And SDL Policy"). The "Keep target-local until a second real generator target earns promotion" rule STAYS (collaborator-extraction discipline).
  - Bullet 8 ("Refactor safety is snapshot- and fixture-first..."): STAYS (durable refactor-discipline principle; will be expanded by the M1-fold "Refactor Safety Harness Pattern" subsection in testing-strategy per Phase 0.2 audit fold target).
  - Bullet 9 ("Linux-canonical generation for ABI correctness across the 7-RID surface. The sunset Cake implementation enforced this through a pinned `linux-builder` Docker container..."): STAYS in purified version with sunset-impl-mechanism description CUT. The Linux-canonical principle STAYS (durable, consistent with constitution §"Generator Home" Linux-canonical-parsing requirement). The pinned-`linux-builder`-Docker-container-with-`libclang.runtime.linux-x64`-pins enumeration CUTS to implementation-notes §2 (Generation Determinism Implementation) and §9 (Linux-Canonical Generation in maintenance playbook).
  - Bullet 10 ("Preprocessor-macro switching only for platform passes. No `--target` cross-compile flag, no mingw-w64, no Apple SDK headers..."): STAYS (durable platform-pass-architecture rule; consistent with constitution §"Platform Views" rules and §"Generation Determinism Contract" Pure-Inputs Discipline). The "Verified against ppy/SDL3-CS Dockerfile + `generate_bindings.py` (WebFetch 2026-05-15) and the local CppAst spike" evidence citation CUTS to implementation-notes §2 (Generation Determinism Implementation evidence section).
  - Bullet 11 ("`SDL_syswm.h` typed-union layout is Stage 2. Stage 1 emits `SDL_GetWindowWMInfo` as a function with opaque `SDL_SysWMinfo*` parameter..."): STAYS (durable SysWM staging; per-spec Q4D the new "SysWM Layout And Satellite Sweep Close" milestone holds this).
  - Bullet 12 ("SDL3 binding generation is gated on PD-7..."): STAYS (durable SDL3 gating; per-spec Q4D the new "SDL3 Extension" milestone holds this; matches AGENTS.md release-strategy PD-7 gate).
  - Bullet 13 ("Toolchain re-evaluation under `spikes/binding-generators/` covers both directions: CppAst version-trio coupling and owned-emitter cost vs. ClangSharp orchestrator + Roslyn postprocess footprint..."): PURE CUT (sunset-banner / toolchain-spike-active-state; post-spike-closure reality is the ClangSharp + Roslyn implementation is the path of record).
  - Bullet 14 ("ppy/SDL3-CS is the strongest SDL-specific reference... SkiaSharp is a strong CppAst discipline reference. Alimer.Bindings.SDL provides the comparison CppAst toolchain in the spike..."): STAYS in purified version. The "Alimer.Bindings.SDL provides the comparison CppAst toolchain in the spike" sentence CUTS (post-spike-closure: Alimer is reference, not active comparison). The ppy + SkiaSharp + Alimer-as-shape-reference framing STAYS (durable peer-binding evidence per constitution §"Authority Order"); the peer-binding row in folder layout already points to `canonical/references/ppy-reference-analysis-2026-05-25.md`.
  - Bullet 15 ("Structural emission is generic AST-driven work. Name allowlists such as `Stage1StructNames` and `SDL_GameControllerButtonBind`-specific flattening are not canonical design; anonymous unions are translated from AST shape. `SDL_GUID` is explicitly mapped to `System.Guid` through substitution policy and is not emitted as a generated struct."): STAYS (durable structural-emission rule; consistent with constitution §"Structs And Unions" + §"Opaque Handles" anti-allowlist discipline). The `SDL_GUID -> System.Guid` substitution evidence citation CUTS to implementation-notes §6 (SDL_GUID Substitution mechanism description).
- "Related Canonical Docs" 11-row bulleted list: STAYS in purified version with path updates. Constitutionally-canonical refs (Phase 4 brief, release-strategy, plan, ADR-002, ADR-003, knowledge-base testing/extraction guidelines, AGENTS.md) all STAY with refreshed paths if any. ADR-004 row updates to neutral banner per Q13. The "Cake-host fold inherits ADR-002" / "binding validators extend ADR-003" framings STAY but the inheritance is now phrased post-spike (ClangSharp + Roslyn implementation will need own ADR per spec).

**Verdict:** ready to git mv (purification scope: cut three status-banner paragraphs at the document head; rewrite Folder Layout table to canonical/ subfolder paths with new sibling docs added; rewrite Reading Order table to new 7-step sequence covering canonical/-home docs; cut historical-removal-rationale trailing paragraph; refresh Current Decision Posture from 14 toolchain-spike-active bullets to ~14 post-spike-closure bullets, with bullet 1 reframed, bullets 6/7/9/13/14 trimmed of sunset-impl path enumeration and toolchain-spike-active framing, evidence citations folded to implementation-notes; update Related Canonical Docs paths if any moved. Estimated reduction: from 69 lines to ~50 lines).

### File: docs/binding-autogen/binding-generator-constitution.md

**Sections audited:**

- "Status (2026-05-23)" header banner ("Canonical binding-generator constitution... Code and pinned SDL public headers remain the north star..."): PURE CUT (sunset-banner). The "code and pinned headers are north star" principle is durable but already restated by the §"Maintenance Rule" at the end of the file ("Code and pinned headers are the behavior authority").
- "Toolchain re-evaluation (2026-05-23)" callout immediately under status banner: PURE CUT (sunset-banner; post-spike-closure reality is ClangSharp + Roslyn selected for Layer 1 evidence).
- "Purpose" section (2 question framing + Cake-hosted GenerateBindings target end-to-end implementation paragraph + "remaining Stage 1 work is public surface completion and production flip" trailing paragraph): STAYS in purified version. The 2 questions (which declarations belong / how each becomes ABI-correct) are durable purpose framing. The "Earlier work under a Cake-hosted `GenerateBindings` target..." paragraph CUTS TO implementation-notes §1 (Purpose — relationship to constitution; mechanism here). The "remaining Stage 1 work is public surface completion and production flip" paragraph CUTS (post-spike-closure: Layer 1 has shipped, so the framing is now Layer 2 forward; matches roadmap Q4D collapse).
- "Authority Order" section (8-item ordered list + 2 trailing paragraphs about historical-plans/superpowers-specs not being policy): STAYS in purified version. The 8 items (pinned SDL headers / packaged native binary exports / current generator impl and tests / SDL dynapi manifests / this constitution / roadmap / ADR-004 / peer bindings) are durable. The parenthetical "(currently Reopened — see spike under `spikes/binding-generators/`)" in item 7 CUTS (PURE CUT — status callout) replaced with neutral "ADR-004 binding-autogen toolchain decision (Reopened — formal amendment deferred to post-Layer-2 per design spec Q13)". Item 3's "during toolchain re-evaluation that means the spike under `spikes/binding-generators/`; the sunset Cake-hosted implementation under `build/_build/Targets/GenerateBindings/` remains historical evidence" CUTS to implementation-notes §1 (relationship to spike subfolder). Trailing paragraphs about historical-plans/superpowers-specs-not-being-policy + code-disagrees-with-this-as-drift STAY (durable authority discipline).
- "Layer Contract" section + 3-layer enumeration + "Internal Raw ABI: Why / How / What" subsection + "Rules" bullets (Public raw IntPtr externs not in v1 preview / SDL2-CS compatibility best-effort / raw ABI mistakes still bugs / declaration that can't be represented honestly deferred / typed handles `nint` not blanket / Raw ABI backends may split DllImport/LibraryImport / compatibility packages OK for old TFMs): STAYS entirely (durable Layer Contract; this is the core principle per spec §Q3C "Layer Contract" preserved-anchor).
- "Generator Home" section + 6-bullet contract list + sunset Cake-impl path-enumeration paragraph: STAYS in purified version. The 6 contract bullets (build infrastructure / committed `.g.cs` / per-family `.generated-stamp` / cross-cutting validators reachable from PreFlight and Pack / persisted data contracts / local invocation via `tools.cs` / Linux-canonical parsing) are durable per-spec §Q3C "evidence gates" preserved-anchor. The "sunset Cake-hosted CppAst implementation lived at `build/_build/Targets/GenerateBindings/` with tests under `build/_build.Tests/Unit/Targets/GenerateBindings/`, validators under `build/_build/Validation/BindingGeneration/`, data contracts under `build/_build/Data/BindingGeneration/`, and local invocation through `dotnet run --file tools.cs -- generate-bindings` inside a pinned Linux builder container" closing paragraph CUTS TO implementation-notes §2 (Generation Determinism Implementation — Generator Home path enumeration).
- "Generator Engine And SDL Policy" section + 7 rules (core pipeline is ABI engine / SDL-specific decisions live behind named policy/profile concepts / target-local until second real generator target exists / M2 may expose SdlPolicy seam / Generic model-building code mustn't accumulate ad hoc SDL2/SDL_ checks once seam exists / satellite profiles designed against real headers / `profile_id` is manifest routing key not behavior switch): STAYS entirely (durable engine-vs-policy boundary; this is the engine/policy/profile principle per spec §Q3C "Generator Engine And SDL Policy" preserved-anchor). The "ClangSharp orchestrator + Roslyn postprocess and CppAst single-pass emitter are both viable engine shapes; the active spike under `spikes/binding-generators/` is selecting between them" sentence in rule 1 CUTS (PURE CUT — toolchain-spike-active status; post-spike-closure reality is selected).
- "Generation Determinism Contract" section + "Determinism Inputs (the pin set)" 8-item enumerated list + "Family Isolation" subsection (3 simultaneous properties) + "Dependency Direction" (forward generation→postprocess + build-time Core ← Image) + "Native Header Resolution Scope" + "Pure-Inputs Discipline" + "Verification Contract": STAYS in purified version (durable determinism principle). HOWEVER per spec §Q3C: the **enumerated 8-item pin set** with vcpkg-version-per-family numbers, ClangSharp tool version anchor + CppAst version anchor framing, RSP/family-config.json path enumeration, `generate_bindings.py` orchestrator naming, plus the **family isolation specifics** with `--family X --execute` cleanup semantics + `git status` byte-untouched-verification + per-family `.g.cs` cross-read framing, plus the **verification commands** with `--family <one> --execute` + `git status` per-family-directory invocation, ALL CUT TO implementation-notes §2 (Generation Determinism Implementation). The constitution keeps the durable contract invariants: (a) generation is deterministic function of pinned inputs to committed output; (b) family isolation guarantees three simultaneous properties (no specifics on cleanup semantics); (c) dependency direction (forward gen→postprocess + build-time Core←Image); (d) native header resolution scope (resolve-via-include-dir, emit-per-file); (e) pure-inputs discipline (no machine paths/timestamps/network/host-OS conditional generation); (f) verification contract requirement (Item-1+ slice MUST verify in exit evidence) — without enumerated commands. The "Slices that change the determinism contract itself MUST update this section in the same change set" maintenance rule STAYS.
- "Manifest Configuration Vs Code-Owned Policy" section + 17-bullet manifest-owned-facts list + "M3 may add `export_macro_names`" note + Raw-ABI-class-name/native-import-name/satellite-core-reference-derivation paragraph + Generator-code-owns-ABI-API-policy bullets + Exception-rule bullets + "Configurable Scope Vs Policy Mechanism" subsection: STAYS entirely (durable manifest-vs-policy boundary; this is the §Q3C "Manifest Configuration Vs Code-Owned Policy" preserved-anchor and the destination for the Phase 0.2 M2/M3 fold targets). NOTE per Phase 0.2 audit: **add** sub-bullets here: (a) "Disabled binding-generation families MUST be resolvable through config/profile checks without invoking parse, model build, emit, validator run, output cleanup, compile-check, or smoke side effects"; (b) "Derivation failures (e.g., primary-binary metadata too wildcarded to prove the import name) MUST fail with a typed error citing the family + profile + derivation mode; silently introducing a new JSON field as workaround is forbidden." Also add new "Generation Process Contracts" sibling-or-sub subsection from Phase 0.2 M2 fold: "Enabled validator ids unknown to the registered validator set MUST fail closed with the unknown id named, not silently no-op" + "Per-family regeneration MUST clean its prior output tree before writing new files; the cleanup MUST be bounded by a path-prefix guard to its family root under `artifacts/generated-bindings-preview/`". Example field names from family-config.json (`opaque_handles.force_opaque_exceptions`, `flags_enums.allow_list`, `clong_methods`, `platform_views`, `all_platform_macros`, `base_rsp`) CUT TO implementation-notes §12 (Config Surface Evolution) per spec.
- "Family Identity" section + 5-row table + parse-views-don't-become-class-names paragraph: STAYS entirely (durable family-identity table per spec §Q3C "Family Identity table" preserved-anchor).
- "Function Surface" section + 6 rules + "Current accepted deferrals" (FILE/`_IO_FILE`/`va_list`/`__va_list_tag`/`SDL_RWFromFP`/`SDL_LogMessageV`/`SDL_vsnprintf`/`SDL_vsscanf`/`SDL_vasprintf` Stage 1): STAYS entirely (durable function-surface policy; consistent with §Q3C "C variadics" + "evidence gates" preserved anchors).
- "BCL-Replaceable Helper Exclusion Policy" section + 3-condition rule + Mechanism + "Standing exclusions (Priority C)" enumeration + Non-rule paragraph: STAYS entirely (per spec §Q3C "BCL-Replaceable Helper Exclusion Policy" preserved-anchor). The `spikes/binding-generators/clangsharp/rsp/per-header/<header>.rsp` path mention in Mechanism is durable per spec content outline §7 (Foreign Type Boundary Implementation).
- "C Variadics" section + Stage 1 policy bullets + `SDL_Log` / `SDL_snprintf` / `SDL_sscanf` examples + required-evidence bullets: STAYS entirely (per spec §Q3C "C variadics" preserved-anchor).
- "Scalar Type Translation" section + 7-row table + "C `long` And `unsigned long`" subsection (Facts + Contract + Priority C hybrid strategy WHY/HOW/WHAT + High-risk SDL2.Core symbols Priority C disposition list): STAYS entirely (durable scalar-mapping policy + C long hybrid contract per spec §Q3C "scalar mapping" preserved-anchor). The "Each output emits the single TFM-appropriate form (no `#if NET6_0_OR_GREATER` directives); TFM gating happens via the csproj's conditional `<Compile Include>` items, mirroring the existing `libraryimport` postprocess split (Compat keeps `[DllImport]`; Modern is rewritten to `[LibraryImport]`)" implementation-mechanism sentence in **How** clause CUTS TO implementation-notes §4 (C `long` Hybrid Implementation).
- "`SDL_bool`" subsection + 4-row table + contract bullets: STAYS entirely (durable SDL2-vs-SDL3 bool policy; consistent with constitution §"Enums" SDL2 int-backed bool rule).
- "`wchar_t`" subsection + Facts + Contract + "Priority C mechanism" WHY/HOW/WHAT: STAYS in purified version. The WHY/HOW/WHAT durable rationale (opaque IS ABI-correct mapping, not Layer 3 repair; no portable BCL primitive; Silk.NET counter-evidence; ppy IntPtr-raw) STAYS. The **How** clause's "ClangSharp-style implementations use RSP-level `--remap` with space-containing variants for libclang byte-exact match (`wchar_t *=nint`, `const wchar_t *=nint` — unquoted, one per line, because System.CommandLine treats each RSP line as one argv element verbatim); CppAst-style implementations use the type classifier's `wchar_t* -> nint` rule plus a wide-string policy override for HIDAPI field/parameter cases. Postprocess `WcharStarToNintRewriter` is available as fallback when the type-system path does not reach a specific case" implementation-mechanism sentence CUTS TO implementation-notes §5 (wchar_t Implementation Mechanism).
- "Opaque Handles" section + 5 rules + Resolved-examples bullets + "Public typed handle struct shape" WHY/HOW/WHAT + "Implementation mechanism (ClangSharp + Roslyn postprocess)" + "Auto-detect criterion (syntactic)" + "Canonical roster (machine-readable)" + "Cross-family force-opaque scope" + "Cross-family handle name resolution (rewriter input set)" + "Force-opaque allow-list delegation" + "Cross-assembly Pattern B contract (`[assembly: DisableRuntimeMarshalling]`)": STAYS in purified version. The Pattern B durable principles (single-pointer-sized field is ABI-equivalent to bare IntPtr; readonly partial struct shape; explicit operator conversions only; field-position rewrite; uniform mechanism for both auto-detected and force-opaque) STAY (per spec §Q3C "Pattern B principle, force-opaque allow-list rule, cross-assembly DisableRuntimeMarshalling rule" preserved-anchor). The "Implementation mechanism (ClangSharp + Roslyn postprocess): The `OpaqueHandleEmitRewriter` ([`spikes/binding-generators/clangsharp/postprocess/OpaqueHandleEmitRewriter.cs`](...), invoked via the `uniform-opaque` postprocess mode) realizes this policy across two input channels..." entire mechanism description paragraph CUTS TO implementation-notes §3 (Opaque Handles Implementation Mechanism). The "Canonical roster (machine-readable)" bulleted enumeration with `family-config.json` paths + audit-method-per-family + drift-watchdog implementation CUTS TO implementation-notes §3 (canonical roster JSON schema) + §12 (Config Surface Evolution). The "Cross-family handle name resolution (rewriter input set)" loader-contract enumeration CUTS TO implementation-notes §3 (Cross-family handle name resolution data pull). The "Force-opaque allow-list delegation" prose-vs-config-must-update-both-sides discipline STAYS in constitution (durable rule). The "Cross-assembly Pattern B contract" `[assembly: DisableRuntimeMarshalling]` contract STAYS (durable cross-assembly rule per spec §Q3C "cross-assembly DisableRuntimeMarshalling rule" preserved-anchor); the SYSLIB1051 + Microsoft P/Invoke source generator implementation citation CUTS TO implementation-notes §9 (Cross-Assembly Pattern B Contract).
- "Foreign Type Boundary Policy" section + WHY + HOW + WHAT + 13-row category table (Vulkan / Direct3D COM / Microsoft GDK / Win32 / Android JNI / C stdlib / Linux X11 / Linux Wayland / Linux KMSDRM / macOS Cocoa / iOS UIKit / Metal / WinRT / OpenGL_EGL_GLES / DirectFB-Mir-Vivante-OS2) + closing paragraphs: STAYS in purified version. The WHY/HOW principle (SDL-owned typed handles vs foreign-types-as-IntPtr boundary; distinguishing SDL-owned from foreign is manual policy decision; peer convergence on IntPtr-at-foreign-boundary; per-header RSP `--remap` for byte-exact match) STAYS (per spec §Q3C "foreign-type boundary principle" preserved-anchor). The "Disposition column" enumeration (Active / Active / Active / Already handled / Already handled / Already handled / Deferred / Deferred / Deferred / Deferred / Deferred / Already handled / Deferred / Not in scope / Excluded) CUTS TO implementation-notes §7 (Foreign Type Boundary Implementation — per-header RSP pattern, foreign-type allow-list mechanism). The header-location-column ("`SDL_vulkan.h:52-53,187-188`" etc.) is mechanism evidence and CUTS TO implementation-notes §7 (per-header RSP file pointer locations). The "Constitution L161-164's accepted deferrals (`SDL_RWFromFP`, ...) cover the C variadic / file-pointer surface independently of this foreign-type allow-list" sentence STAYS but the line-ref CUTS (replaced with section-name ref §"Function Surface" per Rule 3).
- "Structs And Unions" section + 7 rules + `SDL_RWops` contract + `SDL_syswm.h` contract + Function-pointer-fields bullets: STAYS entirely (durable struct/union rules per spec §Q3C "structs/unions rules" preserved-anchor).
- "Enums" section + 4 rules + "`[Flags]` auto-decoration policy" 3 sub-rules + "Known Stage 1 flag enums" enumerated list (SDL2.Core decorated-by-Flags-suffix: `SDL_MessageBoxFlags`, `SDL_MessageBoxButtonFlags`, `SDL_RendererFlags`, `SDL_WindowFlags`; SDL2.Core decorated-by-allow-list-match: `SDL_Keymod`, `SDL_BlendMode`, `SDL_GLcontextFlag`, `SDL_RendererFlip`, `SDL_TextureModulate`; SDL2.Image: `IMG_InitFlags`; SDL2.Mixer: `MIX_InitFlags`; SDL2.Ttf, SDL2.Gfx: none): STAYS in purified version. The 4 rules + 3 sub-rules (Flags suffix or allow-list / heuristics-over-bit-values rejected / composed alias values preserved) STAY (durable enum policy per spec §Q3C "enum policy" preserved-anchor). The "Known Stage 1 flag enums" enumerated 11-name list CUTS TO implementation-notes §8 (`[Flags]` Detection Mechanism — suffix + allow-list policy enumeration).
- "Constants And Macros" section + 9 rules + "Current macro helper-candidate lane" enumerated list (`SDL_BUTTON`, `SDL_VERSION`, `SDL_VERSIONNUM`, `SDL_VERSION_ATLEAST`, `SDL_WINDOWPOS_*` helpers, `SDL_DEFINE_PIXEL*` helpers, `SDL_PIXELTYPE`/`SDL_PIXELORDER`/`SDL_PIXELLAYOUT`, `SDL_BITSPERPIXEL`/`SDL_BYTESPERPIXEL`, `SDL_ISPIXELFORMAT_*`) + Version-macro-note: STAYS entirely (durable macro policy per spec §Q3C "macro policy" preserved-anchor). The "During the ClangSharp Layer 1 spike, the warning-only non-zero-exit classifier is temporary diagnostic glue: accepted macro names must stay visible in generation reports so the public-surface gap is reviewable rather than silently swallowed" reference to spike-temporary implementation detail CUTS to implementation-notes §1 (Purpose — relationship to spike subfolder during sunset).
- "Platform Views" section + 7 rules + "Current SDL2.Core views" enumerated list (Neutral, WindowsDesktop, WinRT, GDK, Linux, MacOS, IOS, Android): STAYS entirely (durable platform-views per spec §Q3C "platform views" preserved-anchor).
- "Evidence Gates" section + 8-bullet exit-evidence list: STAYS entirely (durable evidence-gates per spec §Q3C "evidence gates" preserved-anchor).
- "Current SDL2.Core ABI Status" section + closure-record paragraph + "Resolved or intentionally quarantined categories" 7-item list + Variadic-fmt-only-imports closing paragraph: PURE CUT or CUTS TO implementation-notes §1 (relationship to constitution policy). The closure-record paragraph ("The 2026-05-19 P0 translation blockers were addressed at the policy level and proven feasible by the sunset Cake-hosted CppAst implementation under fixture-backed generator tests. Whichever toolchain the active spike selects, the policy resolutions below remain binding. The spike's ClangSharp + postprocess output currently re-proves raw ABI visibility, SDL.h required surface, dynapi coherence at ~98%, and the Priority C semantic-ABI closure...") and "sunset Cake-hosted CppAst implementation under fixture-backed generator tests" framing + "spike's ClangSharp + postprocess output currently re-proves..." framing CUT entirely (closure-record now lives in git history per spec; post-spike-closure reality is the policies below are the binding facts, the implementation has re-proven them, and the constitution no longer needs to re-narrate that). The 7 resolved-or-quarantined categories (Duplicate handles canonicalized / C `long` hybrid CLong-CULong NET6+ / SDL_RWops opaque / `wchar_t*` opaque nint / `SDL_WINAPI_FAMILY_PHONE` platform-control / SDL2 `SDL_bool` int-backed / Known bitmask enums `[Flags]`) are all already restated as policy in the relevant sections above (Opaque Handles, C `long`, Foreign Type Boundary, wchar_t, Constants And Macros, SDL_bool, Enums Flags) — so these 7 items become PURE CUTS as the policy lives at the canonical section.
- "Maintenance Rule" section: STAYS entirely (durable docs-vs-code maintenance discipline per spec §Q3C "Maintenance Rule" preserved-anchor).

**Verdict:** ready to git mv (purification scope: Q3C aggressive purification per spec. CUT 3 status banners + "Current SDL2.Core ABI Status" closure-record section (PURE CUTS). CUT TO implementation-notes the 8-item Generation Determinism pin set + Family Isolation cleanup semantics + verification commands (→ §2); the OpaqueHandleEmitRewriter mechanism + canonical roster JSON enumeration + cross-family handle name resolution loader (→ §3); the C `long` hybrid emit `<Compile Include>` conditional mechanism (→ §4); the wchar_t RSP `--remap` + WcharStarToNintRewriter mechanism (→ §5); the Foreign Type Boundary disposition column + header-location-column (→ §7); the Known Stage 1 flag enums enumerated 11-name list (→ §8); the Cross-Assembly Pattern B SYSLIB1051 implementation citation (→ §9); the Generator Home sunset Cake-impl path enumeration (→ §11 / §2). PER PHASE 0.2 ADD: M2 unknown-validator + cleanup-prefix-guard contracts as new "Generation Process Contracts" subsection; M3 disabled-satellite-no-side-effects + derivation-failures-fail-typed sub-bullets in §"Manifest Configuration Vs Code-Owned Policy". Estimated reduction: from 645 lines to ~350-400 lines.)

### File: docs/binding-autogen/binding-generator-roadmap.md

**Sections audited:**

- "Status (2026-05-23)" header banner paragraph + "Toolchain re-evaluation (2026-05-23)" callout: PURE CUT (sunset-banner; status callout). The post-spike-closure roadmap is layer-based, not toolchain-spike-active.
- "Goal" section (durable foundation for SDL2.Core / SDL2 satellites Image-Mixer-Ttf-Gfx-Net / future SDL3 / multi-TFM raw ABI / public typed / friendly overloads / package-first evidence; "not a general-purpose binding-generator product; pragmatic SDL binding generator with clean seams, explicit family policy, and enough tests to refactor without gambling"): STAYS entirely (durable program-goal framing). The "selected toolchain (ClangSharp orchestrator + Roslyn postprocess, or single-pass CppAst emitter) lands when the spike concludes" sentence CUTS (PURE CUT — sunset-banner; post-spike-closure: spike has converged on ClangSharp + Roslyn).
- "Operating Rules" section + 10 bulleted rules (work milestone-by-milestone / RED/GREEN for behavior changes / capture baseline before refactoring / prefer embedded `.h` fixture tests / run fast tests inside slice + compile/smoke/package at boundaries / git mv for moves/renames / test topology aligns with production / preserve unique research before deleting notes / manifest carries facts + explicit exceptions / no catch-all Shared/Common/Pipeline/Runner shells): PURE CUT (entire section — per spec §Q4D "Operating Rules wall covered by AGENTS.md + constitution"). All 10 rules duplicate AGENTS.md §"Operating Rules" (git mv, no Shared/Common buckets) + constitution §"Manifest Configuration Vs Code-Owned Policy" (manifest carries facts, code owns policy) + constitution §"Maintenance Rule" (preserve research before deleting) + testing-strategy §"GenerateBindings Snapshot Conventions" (RED/GREEN, capture-before-refactor, embedded `.h` fixtures, fast-tests-inside-slice + compile/smoke/package-at-boundaries) + extraction-guidelines (no Pipeline/Runner shells).
- "Canonical References And Research Baseline" 8-row table (constitution / testing-strategy / oracle-validation playbook / maintenance playbook / ADR-004 / spike / temporary notes / SkiaSharp / Alimer / ppy / Silk.NET / LibGit2Sharp / Vortice.Windows): PURE CUT (entire section — per spec §Q4D "Canonical References table duplicates canonical README"). The references are already enumerated in the new README's Folder Layout + Reading Order tables and in the constitution's Authority Order.
- "Current Baseline" section + 2-subsection narrative ("Sunset Cake-hosted CppAst implementation under `build/_build/Targets/GenerateBindings/`" + 7-bullet feature list with specific Cake-impl path mentions + "Active spike implementation under `spikes/binding-generators/clangsharp/`" + 6-bullet feature list with ~889 functions / 98.1% dynapi / multi-TFM clean / DllImport↔LibraryImport / multi-OS pass / Priority C gap status; closing paragraph about "second comparable evidence pass through Alimer-style single-pass CppAst is the next gate before the spike's toolchain selection lands"): COLLAPSES to single "What's Done" section per spec §Q4D. The "spike has demonstrated one shape... Alimer-style single-pass CppAst would express through TFM-conditioned emission rules" framing CUTS (post-spike-closure: ClangSharp + Roslyn selected; Alimer-comparison-as-next-gate framing is sunset). The durable "what's done" facts (~889 functions across Compat/Modern TFM passes, ~98.1% dynapi coherence, multi-TFM clean across 5 TFMs, DllImport↔LibraryImport postprocess transform, multi-OS pass for SDL_main.h / SDL_system.h with `[SupportedOSPlatform]`) STAYS in the new single "What's Done" section. SDL2.Core Cake-impl baseline 7-bullet list CUTS (sunset-impl historical state, replaced by post-spike-closure ClangSharp + Roslyn Layer 1 closure facts).
- "Milestone 0: Canonical Plan And Research Consolidation": COLLAPSES to "What's Done". M0 completed by previous consolidation (canonical docs created); the consolidation principle survives in spec/AGENTS.md/canonical-docs cross-referencing discipline.
- "Milestone 1: Safety Harness And Baseline" + Status banner + Goal + Detailed-plan-archived link + References + Scope (8 sub-bullets) + Exit-evidence (5 sub-bullets) + Non-goals (3 sub-bullets): COLLAPSES to "What's Done". M1 completed against sunset Cake impl; the archived M1 detailed plan retires with the parking-lot folder (per Phase 0.2 fold target, the "Refactor Safety Harness Pattern" gets folded into testing-strategy as a NEW subsection). Durable layer-based outcome: safety-harness-pattern lives in testing-strategy.
- "Milestone 2: Behavior-Preserving Topology Refactor" + Status banner + Goal + Detailed-plan-archived + References + Target-shape `build/_build/Targets/GenerateBindings/` Cake folder layout + Scope (8 sub-bullets) + Exit-evidence (4 sub-bullets) + Non-goals (3 sub-bullets): COLLAPSES to "What's Done". M2 completed against sunset Cake impl; the parser/model/emitter separation principle lives in constitution §"Generator Engine And SDL Policy". Per Phase 0.2 audit M2 contract bullets fold into constitution §"Generation Process Contracts" new subsection.
- "Milestone 3: ABI Engine, Family Profiles, And Manifest Boundary" + Status banner + Goal + Detailed-plan-archived + References + Approved-scope-Option-A+ paragraph + Profile-direction list (`sdl2-core` / `sdl2-satellite` / `sdl2-gfx` / `sdl3-core` / `sdl3-satellite`) + Engine-owned-concepts + Authority-split + Manifest-owned-facts + Code-owned-policy + "M2 review findings to carry into the detailed M3 plan" 7-bullet list (TypeMappingPolicy split / LegacyBindingTypeRefBridge retirement / ExternalNativeTypePolicy naming / PlatformCatalog.AllPlatformMacros / function-only platform scanning insufficient / Result-shaped record naming / behavior-preservation) + "Satellite reconnaissance findings (installed x64-windows-hybrid headers, 2026-05-20)" 4-family enumeration + Exit-evidence + Non-goals: COLLAPSES to "What's Done" (paused but subsumed by spike; per Phase 0.2 audit the M3 durable kernels fold into constitution + maintenance playbook). The profile-shape enumeration (`sdl2-core` / `sdl2-satellite` / `sdl2-gfx` / `sdl3-core` / `sdl3-satellite`) is already in constitution + AGENTS.md. M2-review-findings 7 bullets — items 1-2 (TypeMappingPolicy split + LegacyBindingTypeRefBridge retirement) are sunset-Cake-impl-specific and retire with M3 closure; items 3-4 (ExternalNativeTypePolicy naming, PlatformCatalog.AllPlatformMacros naming) are toolchain-neutral naming-discipline → fold target for implementation-notes §11 (Hardcoding Rules); item 5 (platform-sensitive ABI layout, not just function-only platform scanning) STAYS as durable insight in the new layer-based roadmap "What's Done" + becomes input for the "SysWM Layout And Satellite Sweep Close" forward milestone; items 6-7 (Result-record naming + behavior-preservation) retire as sunset workflow. Satellite-reconnaissance facts retire (consolidated already in canonical/satellites/ analysis docs).
- "Milestone 4: Raw ABI Projection And Multi-TFM Backends" + Goal + "Priority C closure" callout + References + Design-direction (5 bullets) + 3-bullet Generated-file-list (`Raw/Commands.Common.g.cs` / `Raw/Commands.DllImport.g.cs` / `Raw/Commands.LibraryImport.g.cs`) + "Use `DllImport` for legacy TFMs / Use `LibraryImport` for modern TFMs / Do not casually polyfill / Compatibility packages allowed" + Exit-evidence + Non-goals: COLLAPSES to "What's Done". M4 closed by the spike's Layer 1 raw ABI closure (2026-05-28 across Core/Image/GFX/TTF/Mixer). The "Priority C closure summary" link target retires (Q6 retires `priority-c-closure-summary.md`); the durable Priority C closure decisions (C long hybrid / wchar_t opaque / Pattern B uniform / SDL_GUID substitution / per-header RSP) live in constitution. The Cake-impl-specific `Raw/Commands.Common.g.cs` triplet retires (post-spike-closure file layout: Compat/Modern codegen passes per `ClangSharp.cs`). The "Do not casually polyfill `CLong` / `CULong`" rule STAYS in constitution §"C `long`".
- "Milestone 5: Public Typed Low-Level API Projection" + Goal + References + Scope (7 bullets) + Exit-evidence + Non-goals: RENAMES to "Layer 2 — Typed Public API Projection" per spec §Q4D. Durable forward-milestone content (public methods on manifest-driven public class / public methods call internal raw / typed handles enums structs callbacks remain public generated / unsafe pointer signatures remain when honest / SDL2 bool conversion when predicate-like / platform-only methods carry platform attribution / public API snapshot review before first preview / Layer 2 reuses Pattern B handles from Layer 1) STAYS in the renamed milestone.
- "Milestone 6: Friendly Overload Projection" + Goal + References + Baseline overload patterns (7 bullets) + Compatibility posture (3 bullets) + Exit-evidence + Non-goals: RENAMES to "Layer 3 — Friendly Overload Projection" per spec §Q4D. Durable forward-milestone content (string→UTF-8 / `ReadOnlySpan<byte>` for pre-encoded / `ReadOnlySpan<T>` / `Span<T>` / `out T` / `ref T` / explicit fmt-only / System.Memory or polyfill rule / stack allocation small UTF-8 / pooled arrays larger / no lifetime inference from pointer shape alone) STAYS in the renamed milestone.
- "Milestone 7: SDL2.Core Production Flip And Reproducibility" + Goal + References + Scope (10 bullets) + Exit-evidence: RENAMES to "Production Flip" per spec §Q4D. Durable forward-milestone content (generate into `src/SDL2.Core/Generated/` / commit `.g.cs` files / remove production compile include for `external/sdl2-cs/src/SDL2.cs` / keep `external/sdl2-cs` as oracle / `.generated-stamp` with generator-toolchain-version + vcpkg-state + SDL-library-version + header-set-fingerprint + header-count + parse-views / no wall-clock fields / stale-output validation before expensive native/package work / stale-output diagnostics name family + stale field + remediation / regeneration same inputs = diff-clean / stable LF + deterministic ordering) STAYS in the renamed milestone.
- "Milestone 8: SDL2 SysWM And Satellite Generation" + Goal + References + Scope (9 bullets) + Exit-evidence: RENAMES to "SysWM Layout And Satellite Sweep Close" per spec §Q4D. Note: per spike's Layer 1 closure (2026-05-28), satellites Image/GFX/TTF/Mixer Layer 1 raw ABI have already shipped → the "Enable one SDL2 satellite at a time" Scope sub-list of Image/Mixer/Ttf/Gfx is partially complete (Layer 1 raw ABI yes; production-flip + typed-Layer-2 + friendly-Layer-3 still pending). The durable forward-milestone content (full typed `SDL_SysWMinfo` / `SDL_SysWMmsg` with platform layout proof / minimal forward-declaration stubs for platform handle types / per-satellite enablement / `binding_generation` config moves from placeholder to full config in same slice / satellite outputs emit satellite-owned only / core-owned types referenced from SDL2.Core never redeclared / duplicate core-type guard / symbol-existence validation / per-family package smoke split) STAYS in the renamed milestone.
- "Milestone 9: Smoke And Asset-Backed Testing Expansion" + Goal + References + Scope (8 bullets) + Exit-evidence: RENAMES to "Smoke And Asset-Backed Testing Expansion" per spec §Q4D. Durable forward-milestone content (`tests/smoke-tests/assets/` with fixture policy / replace branding image with tiny generated fixtures / SDL2 core BMP/WAV/RWops real-file / SDL_image PNG/JPEG/WebP/TIFF/AVIF + optional QOI / SDL_ttf real font / SDL_gfx pixel mutation / SDL_mixer real-file after LGPL-free codec contract / manual diagnostics outside CI) STAYS in the renamed milestone.
- "Milestone 10: SDL3 Extension" + Goal + References + Scope (6 bullets) + Exit-evidence: RENAMES to "SDL3 Extension" per spec §Q4D. Durable forward-milestone content (gated on PD-7 / SDL3 package families + native build support first / SDL3 generation only when SDL3 becomes real second consumer / promote shared generator code only when ADR-002 reuse criteria met / SDL3 1-byte bool / `SDL_IOStream` replaces `SDL_RWops` / SDL3 platform macro model / SDL3 namespace + native library identity / re-evaluate SDL3 TFM support) STAYS in the renamed milestone.
- "Continuous Maintenance" section + 8 bullets (keep constitution updated / keep testing-strategy updated / keep maintenance playbook updated / keep oracle-validation playbook updated / ADR-004 holds original toolchain decision / treat peer bindings as evidence not authority / prefer explicit deferral over fake ABI success / run Slopwatch after code/test/project changes): PURE CUT (entire section — per spec §Q4D "Continuous Maintenance list folds into maintenance playbook"). Maintenance playbook is the destination for operational procedure; constitution §"Maintenance Rule" already covers the "keep docs in sync" principle; AGENTS.md §"Slopwatch After Edits" covers Slopwatch; constitution §"Authority Order" already covers "peer bindings as evidence" + "prefer explicit deferral".
- "Retired Active References" section + 6-bullet list (temporary raw ABI/public wrapper/multi-TFM notes / temporary testing-strategy notes / old binding-generator superpowers specs and plans / old local-output-loop and stage-plan transcripts / semantic pipeline + macro constants + macro taxonomy + P0 fix implementation plans / binding-autogen research/spike notes) + "Git history remains the archive for historical audit detail" closing sentence: PURE CUT (entire section — per spec §Q4D "Retired Active References section one-time consolidation note"). The one-time-consolidation rationale is replaced by the Phase 0 audit-and-retire discipline of the new design spec.

**Verdict:** ready to git mv (purification scope: Q4D layer-based restructure per spec. COLLAPSE M0-M4 into single "What's Done" section preserving durable Layer 1 closure facts (~889 functions, ~98.1% dynapi, multi-TFM clean, DllImport↔LibraryImport, multi-OS pass, Priority C closure, Pattern B uniform, C long hybrid, wchar_t opaque, SDL_GUID substitution, per-header RSP organization). RENAME M5→"Layer 2 — Typed Public API Projection"; M6→"Layer 3 — Friendly Overload Projection"; M7→"Production Flip"; M8→"SysWM Layout And Satellite Sweep Close"; M9→"Smoke And Asset-Backed Testing Expansion"; M10→"SDL3 Extension". CUT entirely: "Canonical References And Research Baseline" table; "Operating Rules" wall; "Continuous Maintenance" list; "Retired Active References" section; status banners. Retire M-numbering throughout the doc. Update all internal cross-refs to new section names per Rule 3. Estimated reduction: from 524 lines to ~250 lines.)

### File: docs/binding-autogen/testing-strategy.md

**Sections audited:**

- "Status (2026-05-23)" header banner + "Toolchain re-evaluation (2026-05-23)" callout: PURE CUT (sunset-banner). The testing layer model is toolchain-neutral; the banner's "implementation-specific references describe the sunset Cake-hosted implementation" is replaced by the post-spike-closure framing.
- "Purpose" section + 6-bullet job list + "strategy is not to blindly clone every upstream SDL test" framing: STAYS entirely (durable testing purpose; consistent with §Q5 preserved-anchor). The "For generated raw ABI coverage, this still means porting most upstream SDL2 automation cases that are headless-safe, while adding Janset-owned header guardrails for generated surface breadth" sentence is durable; the spec §Q5/Q9 Coverage Strategy fold-input from testing/raw-abi-upstream-testing-spec aligns with this.
- "Current Baseline" section + 4-bullet baseline (build/_build.Tests / tests/binding-compile-check/SDL2.Core.CompileCheck.csproj / tests/smoke-tests/native-smoke / tests/smoke-tests/package-smoke/PackageConsumer.Smoke) + 2 paragraphs (gap is depth and layering, not existence; smoke fixtures live under test-owned root): STAYS in purified version with sunset paths CUT. The `build/_build.Tests` row CUTS (sunset Cake-impl test home; post-spike-closure: `spikes/binding-generators/clangsharp/tests/abi-tests/AbiTests.csproj` is the active test home, ultimately moving to a root `tests/` project at production promotion per the Generated Raw ABI Runtime Tests section below). The `tests/binding-compile-check/SDL2.Core.CompileCheck.csproj` row CUTS (sunset Cake-impl compile-check; per spec §Q5 "sunset Cake paths" cut directive). The `tests/smoke-tests/*` rows STAY (toolchain-neutral smoke harnesses). The "Gap is depth and layering, not existence" + "Root `assets/`... Smoke fixtures should live under a test-owned root such as `tests/smoke-tests/assets/`" framing STAYS (durable fixture-policy).
- "Layer Model" 9-row table (Build-host unit tests / Binding compile checks / Generated raw ABI runtime tests / Mechanical header guardrails / NativeSmoke / PackageConsumerSmoke / Asset-backed headless functional smoke / Manual diagnostics / Samples) with Validates / Should-be-headless / Uses-real-assets columns: STAYS entirely (per spec §Q5 "Layer Model table" preserved-anchor).
- "Build-Host Unit Tests" section + Current home paths (`build/_build.Tests/Unit/Targets/GenerateBindings/` / `build/_build.Tests/Scenarios/GenerateBindings/` / fixture headers `build/_build.Tests/Fixtures/Data/GenerateBindings/`) + Purpose (3 bullets) + Recommended emphasis (4 bullets): STAYS in purified version with sunset Cake-impl paths CUT. The 3-path Current-home enumeration CUTS (sunset Cake-impl test home; post-spike-closure: tests live under spike's `clangsharp/tests/abi-tests/` and `clangsharp/postprocess/`, ultimately moving to root `tests/` at production promotion). The Purpose 3 bullets + Recommended-emphasis 4 bullets (validate manifest/config adapters, header resolution, parse views, semantic translation, validators, emitters / pin generator behavior during refactors / fast feedback native-free / small embedded `.h` fixtures preferred / constructed objects only for narrow policy branches / snapshot complex generated output only after baseline reviewed / do not require Docker/libclang/vcpkg for ordinary refactor unit tests) STAY (durable fixture-policy + flakiness/scope-rules per §Q5 preserved-anchor).
- "GenerateBindings Snapshot Conventions" subsection + Snapshot-baseline path (`build/_build.Tests/Unit/Targets/GenerateBindings/Snapshots/`) + 4-path Tests-own-snapshot-inputs path-enumeration + 7-bullet Rules (commit `*.verified.*` / never commit `*.received.*` / generated source snapshots LF / ordinary build-host runs native-free except Linux-only CppAst fixture tests / Linux-only semantic fixture snapshots on Linux or exact-SDK Linux container / generated-preview snapshots only after `dotnet run --file tools.cs -- generate-bindings` produced `artifacts/generated-bindings-preview/sdl2-core/` / gate generated-preview snapshot execution with `JANSET_VERIFY_GENERATED_PREVIEW=1` / filtered Windows-only-skipped runs pass MTP `--ignore-exit-code 8`) + "High-value characterization fixtures" 9-bullet list: STAYS in purified version with sunset paths CUT. The `build/_build.Tests/Unit/Targets/GenerateBindings/Snapshots/` snapshot-baseline path + 4-path test-home-enumeration CUTS (sunset Cake-impl test tree; post-spike-closure snapshot conventions belong with the spike's `tests/abi-tests/` infrastructure ultimately moving to root `tests/`). The `tools.cs -- generate-bindings` invocation + `artifacts/generated-bindings-preview/sdl2-core/` artifact-path CUTS (sunset Cake-impl invocation; post-spike-closure: `oracle.cs` + `Janset.SDL2.<Family>/Generated/` artifact-path are the active invocation/output paths). The "ordinary build-host runs native-free except existing Linux-only CppAst fixture tests" rule CUTS (CppAst fixture tests retire with the sunset Cake impl; post-spike-closure: native-free rule remains general). The `JANSET_VERIFY_GENERATED_PREVIEW=1` env-var-gate STAYS as durable opt-in-real-preview pattern (per Phase 0.2 M1 fold target — "Refactor Safety Harness Pattern" subsection codifies it as refactor gate). The MTP `--ignore-exit-code 8` discipline STAYS (durable TUnit/MTP exit-code semantics). The 9-bullet High-value characterization fixtures list (C `long` / SDL2 `SDL_bool` int-backed / `wchar_t*` opaque / duplicate opaque typedef-tag canonicalization / callbacks and function-pointer fields / fixed arrays and anonymous nested union-struct / macro taxonomy + helper candidates + manual excludes-overrides + stale tolerance / required functions-constants from excluded umbrella headers / platform-only declarations and Neutral subtraction) STAYS (durable fixture-policy).
- "Binding Compile Checks" section + Current home (`tests/binding-compile-check/SDL2.Core.CompileCheck.csproj`) + Purpose (3 bullets) + Limits (3 bullets) + Future additions (3 bullets): STAYS in purified version with sunset paths CUT. The `tests/binding-compile-check/SDL2.Core.CompileCheck.csproj` Current home CUTS (per spec §Q5 explicitly listed as "sunset Cake paths" cut directive). The Purpose-Limits-Future-additions content (prove generated C# compiles across TFMs / catch missing files, syntax, duplicates, bad conditional compilation, public/raw layering accidents / fast feedback before runtime smoke / compile-checks don't prove ABI wire correctness / don't prove native asset landing / don't prove wrapper ownership / future raw ABI backend split synthetic samples / future public API snapshots / future check that no public `[DllImport]`/`[LibraryImport]` leak) STAYS (durable compile-check role).
- "Generated Raw ABI Runtime Tests" section + Current spike home (`spikes/binding-generators/clangsharp/tests/abi-tests/AbiTests.csproj`) + Future production home + Purpose (4 bullets) + Ground rules (7 bullets) + Coverage target (3 bullets including 260-280 stable default headless / 300+ feasible with capability-gated lanes / mechanical header guardrails pursue full surface) + High-value upstream sources (P0/P1 list with `testautomation_rect.c` / `testautomation_rwops.c` / `testautomation_surface.c` / `testautomation_pixels.c` / `testautomation_guid.c` / `testautomation_platform.c` / `testautomation_timer.c` / `testautomation_hints.c` / `testautomation_events.c` / `testautomation_audio.c` / dummy-safe `testautomation_video.c` / enabled software-render `testautomation_render.c` / `testautomation_keyboard.c` / dummy-safe `testautomation_mouse.c` / `testautomation_math.c` / `testautomation_stdlib.c` / `testautomation_main.c` / `testautomation_subsystems.c` / virtual-joystick paths from `testautomation_joystick.c`) + Known generated-surface blockers (SDL_syswm / thread creation / function-like macros): STAYS entirely (per spec §Q5 "P0/P1 targets + ABI risk families" + "research-sources URL list" + "Initial Coverage Matrix" preserved-anchor). NOTE per spec §Q9 fold-input: the "Known generated-surface blockers" 3-item list (SDL_syswm / thread creation / function-like SDL macros) becomes input for the testing/raw-abi-current-inventory.md fold (per spec §Q9 "raw-abi-current-inventory deferred-blockers fold into testing-strategy deferred-coverage notes"). The fold ADDS: `SDL_GetErrorMsg` blocker from testing/raw-abi-current-inventory.md.
- "Mechanical Header Guardrails" section + Purpose (3 bullets) + Recommended guardrails (5 bullets) + closing paragraph ("These guardrails belong near the generated raw ABI runtime suite during the spike and can move to the production test topology with it after generator promotion"): STAYS entirely (durable mechanical-header-guardrails role per spec §Q5 preserved-anchor; consistent with the upstream-port "Mechanical header guardrails should pursue as-close-to-full generated surface coverage").
- "NativeSmoke" section + Current home + Purpose + Current good coverage (8 bullets: SDL init dummy audio/video / SDL_image init bits PNG/JPEG/WebP/TIFF/AVIF / one PNG load / SDL_mixer init bits + decoder names / dummy audio driver + Mix_OpenAudio / SDL_ttf init / SDL_gfx primitive draw return code / optional SDL_net init) + Recommended expansion (7 bullets) + closing scope-paragraph: STAYS entirely (durable NativeSmoke role per spec §Q5 preserved-anchor; matches "Initial Coverage Matrix" preserved-anchor).
- "PackageConsumerSmoke" section + Current home + Purpose (6 bullets) + closing-importance-paragraph + Recommended expansion (5 bullets) + High-value examples (10 bullets: `SDL_Init`/`SDL_Quit` / `SDL_GetError`/`SDL_SetError` / create/free surface / `SDL_RWFromFile` / deterministic callback path / BMP/PNG/JPG/WebP/TIFF/AVIF loads / `IMG_Init` + `IMG_Load` + decoder list / `Mix_OpenAudio` dummy + `Mix_LoadWAV` + decoder list / `TTF_OpenFont` + render-to-surface / SDL2_gfx primitive draw into software renderer): STAYS entirely (durable PackageConsumerSmoke role per spec §Q5 preserved-anchor).
- "Asset-Backed Headless Functional Smoke" section + framing-paragraph + Rules (8 bullets) + Recommended-fixture-root tree + Initial fixture candidates (13 bullets): STAYS entirely (durable fixture-policy per spec §Q5 "fixture policy" preserved-anchor).
- "Manual Diagnostic Apps" section + Candidate homes + Candidate apps (10 bullets) + Rules (4 bullets): STAYS entirely (durable manual-diagnostics-policy).
- "Upstream Adoption Policy" section + Use-as-oracle-not-import-plan framing + Adopt-directly-when (4 bullets) + Prefer-local-curated-ports (6 bullets) + closing-Keep-upstream-sample-style-as-manual-inspiration paragraph: STAYS entirely (per spec §Q5 "Upstream Adoption Policy" preserved-anchor). Per spec §Q9 fold-input expects testing/raw-abi-upstream-testing-spec.md "Upstream Feasibility Summary" merge with this section's SDL2 Core/Satellite Research Summaries.
- "SDL2 Core Research Summary" subsection (mixed upstream tree / 4 categorical lists: one substantial automated harness `testautomation` / several standalone noninteractive CTest entries / many manual-sample-diagnostic programs / small real assets / Good managed-binding candidates 8 bullets / Good native-smoke candidates 5 bullets / Manual-sample candidates 4 bullets): STAYS entirely (per spec §Q5 "SDL2 Core + Satellite Research Summaries (upstream test inventory)" preserved-anchor).
- "SDL2 Satellite Research Summary" subsection (SDL_image testimage strongest automated upstream / 4-bullet SDL_mixer playwave-playmus + SDL_ttf showfont-glfont + SDL_net showinterfaces-chat + SDL2_gfx software rendering): STAYS entirely (per spec §Q5 preserved-anchor).
- "Initial Coverage Matrix" 6-row table (SDL2 Core / SDL2_image / SDL2_mixer / SDL2_ttf / SDL2_gfx / SDL2_net) with NativeSmoke / PackageConsumerSmoke / Manual Diagnostics columns: STAYS entirely (per spec §Q5 "Initial Coverage Matrix" preserved-anchor).
- "Implementation Backlog" section + 9 slices (Slice 1: Dedicated Smoke Fixture Root / Slice 2: Tiny PNG Replacement / Slice 3: SDL2 Core File Fixtures / Slice 4: SDL_image Required Format Set / Slice 5: SDL_ttf Real Font Render / Slice 6: SDL_gfx Pixel Assertions / Slice 7: SDL_mixer Real Audio/Music Fixtures / Slice 8: Per-Family Package Smoke Split / Slice 9: Manual Diagnostic Apps): STAYS entirely (durable smoke-expansion backlog; per spec §Q5 "Implementation Backlog stays inside"). Per spec §Q9 fold-input adds NEW subsection "Raw ABI Upstream Port Backlog" from testing/raw-abi-upstream-testing-plan.md multi-agent slice tables.
- "Refactor Gate Mapping" 10-row table mapping M1-M10 milestones to Primary Gate / Secondary Gate columns: CUTS in current form (per spec §Q5 "Refactor Gate Mapping M-numbering: rewrite to layer-based"). The mapping principle (use lightest gate that answers the question for the current milestone; mapping milestones to primary/secondary gates) STAYS but rewrites to layer-based names. New table rows: "What's Done (Layer 1)" → "Build-host unit/scenario tests, snapshots, compile-check, oracle"; "Layer 2 — Typed Public API" → "API snapshot, compile-check / Package smoke minimal generated calls"; "Layer 3 — Friendly Overload" → "Pattern tests, compile-check / Package smoke string/path/resource pairs"; "Production Flip" → "Project build, stamp tests / Package-first smoke"; "SysWM Layout And Satellite Sweep Close" → "Family compile/package smoke / Duplicate core-type guard, symbol existence"; "Smoke And Asset-Backed Testing Expansion" → "NativeSmoke and PackageConsumerSmoke / Fixture provenance review"; "SDL3 Extension" → "SDL3 compile/package smoke / SDL3-specific policy review".
- "Flakiness And Scope Rules" 8-bullet list (don't conflate generator / C# compile / native feature / NuGet layout / runtime ABI smoke; don't require libclang/vcpkg/native for ordinary refactor unit tests; don't port upstream wholesale; don't require real display/audio/input/network in CI gates; don't use strict pixel assertions for lossy formats; don't use large branding assets as fixtures; don't make mixer MIDI/codec-specific hard gates until policy settles; don't block generator architecture refactors on deep smoke expansion): STAYS entirely (per spec §Q5 "flakiness rules" preserved-anchor).
- "Research Sources" section (upstream SDL sources URLs: SDL2 core test tree / SDL2 core test CMake wiring / SDL2 core testautomation runner / SDL2 core automation suite registry / SDL2 core test license / SDL_image 2.8 test tree / SDL_image 2.8 test CMake wiring / SDL_image 2.8 test runner / SDL_image examples / SDL_mixer 2.8 samples / SDL_ttf 2.24 samples / SDL_net SDL2 examples; .NET/NuGet interop sources: native library loading / native files in NuGet packages; Comparable .NET binding projects URLs: SkiaSharp tests / SkiaSharp content / LibGit2Sharp tests + native load app / Vortice.Windows tests + samples / Silk.NET): STAYS entirely (per spec §Q5 "research-sources URL list" preserved-anchor).

**Fold inputs from testing/ folder (incoming per Phase 2.3 execution):**

- `testing/raw-abi-upstream-testing-spec.md` "Categories" table (`AbiSmoke` / `AbiUpstreamPort` / etc.) → folds into Layer Model table or new "Test Categories" subsection.
- `testing/raw-abi-upstream-testing-spec.md` "Coverage Strategy" + ABI risk families → folds into "Generated Raw ABI Runtime Tests" section, augmenting the P0/P1 source list and ground rules.
- `testing/raw-abi-upstream-testing-spec.md` "Upstream Feasibility Summary" (260-280 from 310 enabled, P0/P1 source list) → folds into "Generated Raw ABI Runtime Tests" + "SDL2 Core Research Summary"; the 260-280 / 310 / 300+ numbers are already present in the existing Coverage-target subsection.
- `testing/raw-abi-upstream-testing-plan.md` multi-agent slice tables → folds into "Implementation Backlog" as NEW "Raw ABI Upstream Port Backlog" subsection (added after Slice 9).
- `testing/raw-abi-current-inventory.md` "Known Generated Binding Blockers" (SDL_syswm, thread creation, function-like macros, `SDL_GetErrorMsg`) → folds into "Generated Raw ABI Runtime Tests" → "Known generated-surface blockers" subsection (already contains first 3 items; ADDS `SDL_GetErrorMsg`).

**Verdict:** ready to git mv (purification scope: Q5/Q9 aggressive purification per spec. CUT 3 status banners (PURE CUTS). CUT sunset Cake-impl paths: `build/_build.Tests/Unit/Targets/GenerateBindings/Snapshots/` snapshot-baseline path + 4-path test-home-enumeration + `tests/binding-compile-check/SDL2.Core.CompileCheck.csproj` Current-home + `dotnet run --file tools.cs -- generate-bindings` invocation + `artifacts/generated-bindings-preview/sdl2-core/` artifact-path + "Linux-only CppAst fixture tests" framing. REWRITE "Refactor Gate Mapping" M1-M10 table to layer-based row names (What's Done / Layer 2 / Layer 3 / Production Flip / SysWM Layout And Satellite Sweep Close / Smoke And Asset-Backed Testing Expansion / SDL3 Extension). FOLD IN per Phase 2.3: testing/raw-abi-upstream-testing-spec Categories table + Coverage Strategy + Upstream Feasibility Summary; testing/raw-abi-upstream-testing-plan multi-agent slice tables → NEW "Raw ABI Upstream Port Backlog" subsection; testing/raw-abi-current-inventory Known Generated Binding Blockers → augment "Generated Raw ABI Runtime Tests" → "Known generated-surface blockers" with `SDL_GetErrorMsg`. PER PHASE 0.2 ADD: NEW "Refactor Safety Harness Pattern" subsection codifying snapshot-first / embedded-`.h`-fixture / fake-orchestration / opt-in-real-preview as refactor gate. Estimated change: from 645 lines (current) to ~550-650 lines after CUTS + FOLDS combine.)

### File: spikes/binding-generators/docs/llm-handoff.md

**Sections audited:**

- "TL;DR — 60-second status" 7-bullet quick-state list (project framing / toolchain evidence path / Priority C closure date + commit chain / Item 5 Mixer Layer 1 closure / next scope = Layer 2 / branch state / approval gate): sunset-specific session-handoff state — duplicate of `next-iteration-plan.md` §"Current Status" and `priority-c-closure-summary.md` closure summary. The "next scope = Layer 2" pointer + "branch state = push gate pending Deniz approval" + "Branch is `spike/binding-autogen-sdl2-gfx`" are moment-in-time session-handoff facts → no fold needed (becomes obsolete on first Layer 2 commit; next-iteration-plan.md is the canonical forward-status sink).
- "Reading order" 9-row table (this file / priority-c-closure-summary / next-iteration-plan / generator-spike-goals / iteration-2-comparison / clangsharp-failure-buckets / constitution / roadmap / ppy clone): duplicate of reading sequences in `spikes/binding-generators/README.md` (which gets refreshed in Phase 1 to point at the new canonical/ subfolder topology) → no fold needed; the constitution + roadmap L-line citations (Layer Contract L34-50, Opaque Handles L325-363, etc.) become stale on constitution purification per Phase 1 Rule 3 (replace L-lines with section names).
- "Working preferences (read this — saves you grief)" 9-bullet list (no conclusion jumping / no scope creep / no "let me wrap up" framing / no timing pressure / no magic / follow explicit paths / no motive assumptions / no yes-man / memory notes apply) + closing approval-gate paragraph: **fully duplicate of user MEMORY.md** entries (no conclusion jumping ↔ `feedback_no_workarounds_shortcuts`; no scope creep ↔ AGENTS.md operating rules; no timing pressure ↔ `feedback_no_timing_pressure_or_motive_assumptions`; no magic ↔ `feedback_no_workarounds_shortcuts`; follow explicit paths ↔ `feedback_follow_explicit_paths`; no motive assumptions ↔ `feedback_no_timing_pressure_or_motive_assumptions`; memory notes apply ↔ tautological self-reference). The "wait for explicit approval" rule is in AGENTS.md §"Approval Gate" → no fold needed; this is duplicate user-preference content that the next-LLM session loads from MEMORY.md, not from a checked-in file.
- "Grand vision (what we're building)" three-layer ASCII diagram + multi-TFM target paragraph + multi-OS-target paragraph + "Where this is going" paragraph: duplicate of constitution §"Layer Contract" + AGENTS.md §"Release Strategy and PD-7 Gate" multi-TFM facts → no fold needed; the constitution Layer Contract is the authority.
- "Code map — where things live" full ASCII tree (`spikes/binding-generators/{README, Directory.Build.props, docs/, scope/, clangsharp/{slnx, generate_bindings.py, oracle.cs, rsp/, policy/, shims/, postprocess/, tests/abi-tests/, src/Janset.SDL2.Core/, src/Janset.SDL2.Image/}, alimer-style/, output/reports/, references/}`) + "Production code worth knowing" sub-tree pointing at sunset Cake-impl paths (`build/_build/Targets/GenerateBindings/`): sunset-specific spike workspace layout — **auto-discoverable from repo** by reading the actual `spikes/binding-generators/clangsharp/` tree. The sunset Cake-impl `Production code worth knowing` sub-tree (`build/_build/Targets/GenerateBindings/PlatformViews/PlatformCatalog.cs`, `ModelBuilding/Types/TypeMappingPolicy.cs`, etc.) becomes obsolete with the Cake-impl retirement. → no fold needed; the spike README refresh in Phase 1 lands an updated workspace tour.
- "Reference projects (in `references/`, gitignored)" subsection: `ppy-SDL3-CS/` north-star orchestrator details (4-bullet ppy generator-script structure with line ranges; Roslyn SG ref; per-header .rsp ref; companion .cs files) + `alimer-bindings-sdl/` reference paragraph — duplicate of `canonical/references/ppy-reference-analysis-2026-05-25.md` (per spec §Q11 the ppy-reference-analysis doc moves to canonical/references/) and `output/reports/iteration-2-comparison.md` → no fold needed.
- "Approach decision history (don't relitigate)" subsection + 5-row metrics table (Functions emitted / Dynapi hit rate / Iterations to compile-clean / Multi-TFM errors before fix / Multi-TFM after fix) + "Decision turning point" paragraph + "Alimer scaffold retained" paragraph + "ADR-004 Reopened" paragraph: duplicate of `output/reports/iteration-2-comparison.md` (decision evidence) + ADR-004 status (handled by Q13 neutral-banner ADR-004 update) → no fold needed.
- "Slice progress" 12-row table (Slice 1 / Slice 2 / Slice 3 / Slice 4 / Priority A / Priority B / Priority C / Slice 5 / Item 1 / Iteration 2 / Item 2 / Item 3 / Item 4 / Item 5 / Slice 6+): historical closure record duplicate of `next-iteration-plan.md` "Closed Slices" + `satellite-expansion-roadmap.md` Item closures + `priority-c-closure-summary.md`. Closure facts ⇒ `git log --follow` is the archive (every closure has a commit chain cited). → no fold needed.
- "What got built and why — slice-by-slice rationale" 4 subsections (Slice 1 separate-projects rationale / Slice 2 postprocess-vs-Roslyn-SG rationale + StripVarargsRewriter scope justification / Slice 3 platform-pass header bucket table + ppy alignment / Critical SDL2 vs SDL3 distinction): closure-record rationale — **partially durable**: the "SDL2 platform macros are host-compiler-defined; SDL3 macros are SDL-specific" + "Cake AllPlatformMacros undefines exist for cross-contamination protection" insights are platform-architecture wisdom already covered by constitution §"Platform Views" + cross-checked by Phase 0.1 implementation-notes §2 (Generation Determinism Implementation) plan. The Slice 1/2/3 rationale narratives are sunset-specific closure record. → no fold needed.
- "Current status and blockers" subsection + "What is now fixed" 9-bullet list (Item 5 closure evidence) + "Known tradeoff: Mixer runtime callback/audio smoke is intentionally deferred" + "Known macro-policy follow-up: ClangSharp value-macro generation evaluates through current generation host" (endian macros / `AUDIO_S16SYS` little-endian-emit / `SDL_BYTEORDER` / `SDL_PIXELFORMAT_*32`) → fold target for "Mixer runtime callback/audio smoke" lives in `next-iteration-plan.md` §"Review Follow-up Backlog" (per Phase 0.2 satellite-expansion-roadmap fold target → next-iteration-plan.md Phase 4 restructure). The endian-macro evaluation issue is durable cross-cutting design risk → already captured at `spikes/binding-generators/docs/satellites/sdl2-endian-platform-macro-consolidation.md` (cross-check confirmed). → no fold needed.
- "What is still not solved" Oracle report repair queue 3-row table (Priority A / B / C with status Done/Closed): closure record → `git log --follow` archive. "Remaining items to watch" 4-bullet list (Windows-local non-Windows views need `--use-platform-header-shims` / ClangSharp `CXError_Failure` declspec / GDK `[SupportedOSPlatform("windows")]` / Multi-OS `SDL_system.h` parse warnings): durable production-flip blockers and known-tradeoff list → all already captured in `next-iteration-plan.md` §"Review Follow-up Backlog" (Production Flip Gates + Accepted Tradeoffs subsections per the Phase 0.2 fold target). → no fold needed.
- "Concrete generated evidence" 5-bullet list of Generated/Modern/Platforms/... output paths: closure-record evidence → `git log --follow` archive. → no fold needed.
- "Header-scope correction" 8-header list (`SDL_main.h`, `SDL_system.h`, `SDL_thread.h`, `SDL_rwops.h`, `SDL_syswm.h`, `SDL_stdinc.h`, `SDL_platform.h`, `begin_code.h`) carrying platform-sensitive header inventory: durable platform-completeness scope evidence. **Already partially covered** by constitution §"Platform Views" (current SDL2.Core views: Neutral, WindowsDesktop, WinRT, GDK, Linux, MacOS, IOS, Android) + roadmap §"Milestone 8 / SysWM Layout And Satellite Sweep Close" (will rename per spec §Q4D). The full 8-header platform-sensitive inventory is durable production-flip-blocker evidence → already in `next-iteration-plan.md` §"Review Follow-up Backlog" Production Flip Gates. → no fold needed.
- "Recommended next moves" 6-bullet list (Layer 2 planning / Mixer callback policy / TTF runtime smoke / shim flag for Windows-local / empty-platform-output guard / Alimer comparison evidence): forward-looking session handoff → duplicate of `next-iteration-plan.md` §"Next Iteration Goals" framing. The "Mixer callback lifetime/rooting policy" + "TTF runtime font asset smoke" follow-ups land in `next-iteration-plan.md` Review Follow-up Backlog (per Phase 0.2 fold target). → no fold needed.
- "Notable patterns to keep using" subsection + "Constitution citations are how decisions get authority" L-line citation list (Layer Contract L34-50 / `System.Memory` L46 / DllImport/LibraryImport L48 / `SDL_bool` L57 / Family Identity L130-136 / variadic deferrals L156 / C variadics fmt-only L162-176 / C `long` L208-212 / SDL2 `SDL_bool` L228-235 / SDL_syswm L292-294 / macro pipeline L324-348 / multi-TFM L379): pure sunset-specific citation list — line numbers become stale on constitution purification (per Phase 1 Rule 3, L-lines replaced with section names). The principle "cite constitution to claim authority" is in constitution §"Maintenance Rule" + AGENTS.md. → no fold needed (citations themselves become invalid after constitution purification; the discipline is in AGENTS.md).
- "Cake oracle as comparison floor" + sunset Cake-impl paths (`build/_build/Targets/GenerateBindings/`, `RawAbiCommandEmitter.cs:49-59`, `PlatformCatalog.cs:18-77`): sunset Cake-impl reference → obsolete with Cake-impl retirement; the Cake oracle as comparison evidence concept survives in `canonical/binding-output-oracle-validation.md` §"Source Hierarchy" → no fold needed.
- "Explore agent for evidence" + "Multi-TFM build is the truth gate" + "Commands you'll run constantly" 4-command list (regenerate / build all 5 TFMs / oracle / per-TFM ABI runtime smoke / postprocess single-step rerun) + postprocess pipeline order comment (`platform-delta → strip-varargs → libraryimport → flags-detect → guid-substitute → clong-dispatch → uniform-opaque`): the **postprocess pipeline order** is durable cross-cutting fact → fold target per Phase 0.2 spec is `binding-generator-implementation-notes.md` §13 (Postprocess Pipeline Mechanism Detail). Already captured for fold from `satellite-expansion-roadmap.md`'s same 7-step enumeration; this handoff confirms identical ordering. → no additional fold needed (this section confirms identical ordering with the already-targeted satellite-expansion-roadmap.md fold). The commands themselves are auto-discoverable from `spikes/binding-generators/README.md` + `next-iteration-plan.md`.
- "Anti-patterns I fell into (don't repeat)" 5-numbered list (Recommending without evidence / Adding rewriters when a config flag would do / Two-pass when one-pass would do / Ignoring host-compiler default macros / Glossing over partial failures): mostly session-specific reflection. **Partially durable:** "Ignoring host-compiler default macros" (SDL2's `_WIN32`/`__APPLE__` are host-default-defined; SDL2 needs explicit undefines unlike SDL3) is platform-architecture wisdom → already covered by constitution §"Platform Views" + slated for implementation-notes §2 (Generation Determinism Implementation evidence section per Phase 0.1 audit). → no fold needed.
- "Cross-references and authoritative sources" 14-row table (ABI/API policy / Milestone plan / Priority C closure record / Priority C durable policy / Priority C follow-up backlog / Semantic ABI research / Opaque-handle roster / Toolchain ADR / Build host pattern / Active slice plan / Spike charter / Decision evidence / RSP fix history / Family-aware raw ABI evidence / User memory / Agent contract): duplicate of `spikes/binding-generators/README.md` cross-ref table + canonical/README.md Folder Layout + AGENTS.md authority list → no fold needed.
- "When you finish your session, **update this file**" closing instruction: sunset-specific session-handoff lifecycle → obsolete when llm-handoff.md retires per Q6. → no fold needed.

**Verdict:** ready to git rm. Empty unique-info kernel as expected per task description. Working preferences are in MEMORY.md; code map is auto-discoverable; cross-refs are absorbed into spike README refresh + canonical/README.md; closure records are in git history; durable platform-architecture insights (SDL2 host-compiler-defined platform macros / postprocess pipeline order / 8-header platform-sensitive scope) already have other destinations from prior audits (constitution §"Platform Views", implementation-notes §13, next-iteration-plan §"Review Follow-up Backlog").

### File: spikes/binding-generators/docs/priority-c-closure-summary.md

**Sections audited:**

- "Status" + "Branch" + "Closure commit chain" 1-line header block (Slice C-A `adb64d0` / Slice C-B `45fdab6` / Slice C-C `bb638f9` / DisableRuntimeMarshalling `d0016de` / this closure summary): closure-record metadata → `git log --follow` is the archive → no fold needed.
- "Original Six Risks" 6-row table (R1 wchar_t / R2 C long / R3 SDL_RWops / R4 SDL_SysWMinfo / R5 SDL_SysWMmsg / R6 opaque-handle tag leak) with Risk / Constitution authority / Resolution / Evidence columns: **closure-record evidence**. The Risk descriptions + Resolution mechanisms (e.g., "`rsp/base.rsp` literal-space `--remap` entries (`wchar_t *=nint` + `const wchar_t *=nint`)", "`ClongDualDispatchRewriter` mode-aware emit", "`OpaqueHandleEmitRewriter` force-opaque allow-list", "`SDL_hid_device_=SDL_hid_device` in `rsp/per-header/SDL_hidapi.rsp`") are durable implementation mechanisms already slated for fold from constitution per Phase 0.1: implementation-notes §3 (Opaque Handles Implementation Mechanism), §4 (C `long` Hybrid Implementation), §5 (wchar_t Implementation Mechanism), §7 (Foreign Type Boundary Implementation). The Constitution authority L-line cites (§"wchar_t" L300-323, §"C `long`" L246-283 + §"BCL-Replaceable Helper Exclusion Policy" L169-208, §"Structs And Unions" L416-420 + §"Opaque Handles" L352-363, §"Opaque Handles" L325-340) become stale on constitution purification (Phase 1 Rule 3: L-lines → section names). → no fold needed (mechanisms already targeted from constitution; evidence is in git history).
- "Additional Resolutions" 6-bullet list (SDL_GUID → System.Guid substitution mechanism / Foreign Type Boundary Policy summary / BCL-Replaceable Helper Exclusion Policy summary / Pattern B uniform 3-position rewrite / DisableRuntimeMarshalling cross-assembly contract / Handles.g.cs canonical home with 17 handles enumeration): **closure-record evidence with durable mechanism details**. Already covered: SDL_GUID substitution is in constitution §"Current SDL2.Core ABI Status" (which Phase 0.1 audit identified for CUT — durable rule is constitution §"Foreign Type Boundary Policy" + implementation-notes §6 (SDL_GUID Substitution); already a fold target); Foreign Type Boundary Policy is constitution §"Foreign Type Boundary Policy"; BCL-Replaceable Helper Exclusion Policy is constitution §"BCL-Replaceable Helper Exclusion Policy"; Pattern B uniform 3-position rewrite is constitution §"Opaque Handles" + implementation-notes §3; DisableRuntimeMarshalling cross-assembly contract is constitution §"Opaque Handles" Cross-assembly Pattern B contract + implementation-notes §9; the **17-handle enumeration (14 auto-detected: SDL_AudioStream, SDL_Cursor, SDL_GameController, SDL_Haptic, SDL_Joystick, SDL_Renderer, SDL_Sensor, SDL_Texture, SDL_Thread, SDL_Window, SDL_cond, SDL_hid_device, SDL_mutex, SDL_sem + 3 force-opaque: SDL_RWops, SDL_SysWMinfo, SDL_SysWMmsg)** is data-pull evidence pointing at `family-config.json` `families.core.opaque_handles.auto_detect_well_known` + `force_opaque_exceptions` — already targeted for implementation-notes §3 (canonical roster JSON schema description) and §12 (Config Surface Evolution) per Phase 0.1 audit. → no fold needed.
- "Architecture Snapshot" 6-bullet list (Spike home / Current 7-step postprocess pipeline / Per-header RSP organization / Roster policy data / 5 TFM matrix / AbiTests harness): closure-record state. **The 7-step postprocess pipeline order** (`platform-delta → strip-varargs → libraryimport (Modern only) → flags-detect → guid-substitute → clong-dispatch → uniform-opaque`) is already targeted for implementation-notes §13 (Postprocess Pipeline Mechanism Detail) per Phase 0.2 satellite-expansion-roadmap fold. The **per-header RSP organization** (ppy/SDL3-CS pattern: `rsp/base.rsp` + `rsp/sdl2-core.rsp` + `rsp/per-header/<header>.rsp`) is targeted for implementation-notes §7 (Foreign Type Boundary Implementation) + constitution §"BCL-Replaceable Helper Exclusion Policy" Mechanism (already mentions the `spikes/binding-generators/clangsharp/rsp/per-header/<header>.rsp` path). The **roster policy data** (`clangsharp/policy/opaque-handle-roster.json` triangulated to SDL2 headers + wiki.libsdl.org + ClangSharp Modern output, with `auto_detect_well_known` (14) + `force_opaque_exceptions` (3) + `excluded_candidates` (11 with rationale)) is targeted for implementation-notes §12 (Config Surface Evolution) — note that since Iteration 2 (per satellite-expansion-roadmap.md) the roster JSON paths consolidated into `clangsharp/config/family-config.json`. The **5 TFM matrix** + csproj `<Compile Include Condition>` routing is targeted for implementation-notes §4 (C `long` Hybrid Implementation) per Phase 0.1 constitution audit. The **AbiTests harness** description (`clangsharp/tests/abi-tests/`, `SDL_ThreadID` non-zero validation, Modern `CULong`+`LibraryImport` and Compat managed wrapper + `RuntimeInformation` dispatch + Win32 `[DllImport]` paths) is durable AbiTests scope; already in `canonical/testing-strategy.md` §"Generated Raw ABI Runtime Tests" (which mentions `spikes/binding-generators/clangsharp/tests/abi-tests/AbiTests.csproj` as the current spike home — Phase 0.1 testing-strategy audit confirms this). → no fold needed.
- "Verification Evidence (2026-05-24 closure run)" subsection + "Oracle (Step B.2)" command + 6-row findings table (all 0) + "Multi-TFM compile (Step B.3)" 3-row table + "AbiTests runtime smoke (Steps B.4 + B.5)" 5-row pass-table + "Slopwatch (Step B.6)" 0-issues output + "Regen idempotency (Step B.1)" empty-diff result: closure-record evidence → `git log --follow` archive. The "Post-review note (2026-05-25)" addendum (AbiTests now also call `SDL_GetThreadID(SDL_Thread.Null)` and assert it matches `SDL_ThreadID()` on host path; full 7-RID runtime proof remains production CI gate) is durable production-gate framing → already targeted for `next-iteration-plan.md` §"Review Follow-up Backlog" Production Flip Gates (per the satellite-expansion-roadmap fold target). → no fold needed.
- "Next Steps" 3-bullet list (Plan Task 18 / Plan Task 19 / Post-Priority-C horizon: Layer 2 typed low-level public API slice): closure-specific session next-steps → obsolete; Layer 2 horizon is now the canonical forward direction in `next-iteration-plan.md` and roadmap §"Layer 2 — Typed Public API Projection" (per Phase 0.1 roadmap audit rename target). → no fold needed.
- "References" 8-row cross-ref list (constitution / durable policy / active follow-up plan / research / oracle evidence / roster JSON / Handles.g.cs paths): duplicate of `spikes/binding-generators/README.md` cross-ref + canonical/README.md Folder Layout → no fold needed; the L-line citations become stale on constitution purification.

**Verdict:** ready to git rm. Empty unique-info kernel as expected per task description. Durable policy (Pattern B uniform 3-position rewrite, C long hybrid, wchar_t opaque, SDL_GUID substitution, Foreign Type Boundary Policy, BCL-Replaceable Helper Exclusion Policy, DisableRuntimeMarshalling cross-assembly contract) lives in constitution; implementation mechanisms (RSP `--remap` patterns, `ClongDualDispatchRewriter`, `OpaqueHandleEmitRewriter`, per-header RSP organization, roster JSON schema, 5 TFM `<Compile Include Condition>` routing, AbiTests harness shape) are already fold targets for implementation-notes §§3-9 + §12-13 from prior audit entries (constitution + satellite-expansion-roadmap); 17-handle enumeration is data-pull evidence into `family-config.json` not standalone narrative; closure verification evidence is in git history (commit chain Slice C-A `adb64d0`, Slice C-B `45fdab6`, DisableRuntimeMarshalling `d0016de`, closure summary `e62bf92`).

### File: spikes/binding-generators/docs/generator-spike-goals.md

**Sections audited:**

- "Status" + "Date" header block: sunset-specific spike-evidence-in-progress framing → obsolete on spike closure → no fold needed.
- "Original Decision Question (now answered)" 1-question + 1-answer paragraph (ppy-style ClangSharp orchestrator + Microsoft.CodeAnalysis postprocess + production-shaped multi-TFM library projects all under one solution): toolchain selection answer — already captured by ADR-004 (Reopened state per Q13 neutral banner; formal amendment deferred to post-Layer-2) and by `output/reports/iteration-2-comparison.md` decision evidence. → no fold needed.
- "Output Contract (carried into the prototype)" 7-bullet list (internal raw ABI surface / public generated handles-enums-structs-callbacks-constants / opaque SDL handles as `readonly` structs over `nint` / multi-TFM raw ABI / header-based generation / satellite outputs reference core-owned types / multi-TFM compile across 5 TFMs): duplicate of constitution §"Layer Contract" (Internal Raw ABI / Public typed low-level API / Friendly overloads) + §"Family Identity" + §"Opaque Handles" + §"Generation Determinism Contract" → no fold needed.
- "Approaches That Were Compared" 2-row Alimer-vs-ClangSharp comparison table + "Decision evidence" cross-ref + "defining factor was multi-TFM coverage" paragraph: duplicate of `output/reports/iteration-2-comparison.md` + ADR-004 evidence section → no fold needed.
- "What The Spike Tested" 4-bullet list (51-header Core + 1-header Image not 3-header demo / both vs Cake oracle / both vs dynapi 845-symbol baseline / multi-TFM compile): sunset-specific spike test scope → no fold needed; the durable testing-scope principles (compile across 5 TFMs, dynapi hit-rate evidence) are in `canonical/testing-strategy.md` §"Generated Raw ABI Runtime Tests" + roadmap §"What's Done" (post Phase 0.1 roadmap-audit collapse).
- "What The Spike Did NOT Cover" 7-bullet list with status emojis (Slice 1 done / Slice 2 done / Slice 3 partial / Slice 4 folded / Slice 5 deferred / Slice 6 deferred / Slice 7 deferred): closure-record slice status → duplicate of `llm-handoff.md` §"Slice progress" table + `next-iteration-plan.md` slice tracking → no fold needed (closure record archived by git history).
- "Hardcoding Rules (unchanged — these survived the spike intact)" subsection + "Good code-owned policy" 7-bullet list (C primitive + SDL typedef width mapping / SDL2 `SDL_bool` shape / C `long` / `wchar_t`-FILE-`va_list`-Vulkan-D3D / C# keyword escaping / macro taxonomy / SDL-specific substitutions) + "Good config-owned facts" 7-bullet list (library/family identity / input headers / output namespace and public class name / native library import name / owned prefixes or explicit owned symbols / excluded/deferred declarations / manually required declarations or constants) + "Bad patterns" 4-bullet list (`SDL2` library identity hardcoded in emitter while also in config / `/SDL2/` path checks scattered through unrelated policies / `SDL_` prefix rules duplicated across macro-declaration-type ownership / turning manifest JSON into mini ABI policy language): **DURABLE — fold target per spec content outline → `binding-generator-implementation-notes.md` §11 (Hardcoding Rules)**. This is the explicit task fold requirement. The content is durable hardcoding discipline that survived the spike intact, codifying the manifest-vs-policy boundary at implementation granularity. The Good-vs-Bad framing (code-owns ABI/C policy, config-owns family identity + excludes, never-duplicate-the-same-fact-in-two-places) extends constitution §"Manifest Configuration Vs Code-Owned Policy" (which states the policy at principle level) with concrete examples of acceptable hardcoding and concrete bad patterns.
- "Methodology Rules (still active)" 5-bullet list (adapt ppy wholesale / keep Python readable, loops/if/switch, no DI/profile-registry/planner frameworks / keep Microsoft.CodeAnalysis postprocess as standalone console app not Roslyn SG / reference repos under `references/` are evidence not authority / move from spike layout to production-shaped layout as soon as it earns it): partially durable. **Already covered:** "keep Python orchestrator readable" + "no DI/profile-registry/planner frameworks" duplicate AGENTS.md operating rules + extraction-guidelines (no Pipeline/Runner/Planner shells); "reference repos under `references/` are evidence not authority" duplicates constitution §"Authority Order" (peer bindings as evidence not authority); "move from spike layout to production-shaped layout as soon as it earns it" is sunset-specific (Layer 1 production-flip is now scoped per roadmap §"Production Flip"). The "adapt ppy wholesale" north-star framing is durable and is the rationale for the ClangSharp + Roslyn postprocess architecture → already implicit in implementation-notes §1 (Purpose — relationship to constitution) + §2 (Generation Determinism Implementation). "keep Microsoft.CodeAnalysis postprocess as standalone console app, not a Roslyn source generator" is a durable mechanism rule (Constitution requires committed output with reproducibility stamp, and Roslyn SG can only add files not modify existing ones) — fold target: implementation-notes §13 (Postprocess Pipeline Mechanism Detail). → **additional fold needed:** add the "postprocess pipeline runs as standalone console app, not Roslyn SG; rationale: Roslyn SG cannot modify existing generated files, only add new ones; the transform pattern (`DllImport` → `LibraryImport` etc.) requires file rewriting" rule to implementation-notes §13.
- "Promotion Path" paragraph (Slices 1-5 move prototype from compile-clean Core + Image to production-shape candidate; spike concludes when prototype has multi-TFM raw ABI with LibraryImport split + multi-OS pass + public typed projection + friendly overloads; Roadmap M7 production flip is separate; spike directory retires at that point): sunset-specific spike promotion plan → obsolete; the Layer 1 closure (2026-05-28) marks the prototype graduation; production-flip is now roadmap §"Production Flip" (renamed M7 per Phase 0.1 audit). → no fold needed.
- "Cross-References" 3-row list (constitution / roadmap M4-M5-M6-M7 / ADR-004): duplicate of canonical/README.md Folder Layout → no fold needed.

**Verdict:** additional fold needed (two folds). (1) Fold "Hardcoding Rules" section in full into `binding-generator-implementation-notes.md` §11 (Hardcoding Rules), preserving the Good code-owned policy 7-bullet list, Good config-owned facts 7-bullet list, and Bad patterns 4-bullet list — this is the explicit task requirement and provides concrete extension to constitution §"Manifest Configuration Vs Code-Owned Policy" at implementation granularity. (2) Fold the "Methodology Rules" bullet "Keep Microsoft.CodeAnalysis postprocess as a standalone console app, not a Roslyn source generator" with rationale (Roslyn SG can only add files, not modify existing ones; reproducibility-stamp requirement) into implementation-notes §13 (Postprocess Pipeline Mechanism Detail). Everything else is sunset-specific spike rationale safe to retire (toolchain decision already in ADR-004; output contract already in constitution; methodology rules duplicate AGENTS.md/extraction-guidelines; promotion path superseded by Layer 1 closure 2026-05-28 + roadmap §"Production Flip").

### File: spikes/binding-generators/docs/oracle-evidence-design.md

**Sections audited:**

- "Status" + "Scope" header block (Draft approved for planning / Spike-local validation for `spikes/binding-generators/clangsharp/`): sunset-specific design-doc-status → obsolete on oracle.cs ship → no fold needed.
- "Goal" paragraph (build spike-local oracle/evidence loop; first implementation target is .NET 10 file-based app at `oracle.cs`; replaces fragile Python regex extraction with Roslyn-based structured evidence; family-first reporting across ClangSharp / Cake / SDL2-CS / dynapi): sunset-specific design statement → the oracle.cs is now built; this is closure-record. The Roslyn-based-structured-evidence-over-regex principle is durable methodology already in canonical `binding-output-oracle-validation.md` §"Source Hierarchy" + Methods (Roslyn analysis: "Source-level policies such as effective public raw extern leaks") → no fold needed.
- "Non-Goals" 6-bullet list (no `generate_bindings.py` changes / no public typed APIs / no friendly overloads / no ppy manual companion feedback / no Cake target wiring / no final ClangSharp-vs-CppAst recommendation from this evidence alone): sunset-specific design-scope → obsolete → no fold needed.
- "Context" 8-bullet pre-existing-gap enumeration (Raw ABI classes leak public / `SDL.h` required surface missing / `SDL_RWops` full layout / SysWMinfo + SysWMmsg full typed unions / C `long` Windows-local shapes / `wchar_t*` ushort* in shared output / SDL2_image `namespace SDL2` instead of `SDL2.Image` / legacy Python oracle-comparison Core-only regex-based): sunset-specific pre-Priority-C gap snapshot → all gaps now closed per `priority-c-closure-summary.md` + Priority A/B/C closure commits. → no fold needed.
- "Architecture Decision" subsection + command shape `dotnet run --file spikes/binding-generators/clangsharp/oracle.cs -- --family ... --write-report` + 5-bullet rationale (generated output is C# so Roslyn is right parser / file-based apps avoid project-file modification / `tools.cs` validates .NET 10 file-based workflow / `generate_bindings.py` remains Python orchestrator / oracle can later migrate to production if spike graduates): durable architecture decision now realized; the **file-based-Roslyn-app pattern** is a durable methodology rule. **Already covered:** canonical `binding-output-oracle-validation.md` mentions Roslyn analysis as a method (L313 "Roslyn analysis | Source-level policies such as effective public raw extern leaks | More work; best for narrow checks"); the rationale for file-based-vs-project app is sunset-specific spike avoidance of `.csproj`/`.sln` modification → no fold needed. The command shape is auto-discoverable from `spikes/binding-generators/README.md` and `spikes/binding-generators/clangsharp/oracle.cs` itself.
- "Evidence Sources" 7-row table (ClangSharp Compat / ClangSharp Modern / Cake generated preview / SDL2 dynapi exports / SDL2-CS / Native exports / Pinned headers) with Source / Families / Authority / Scope columns: **DURABLE — Source Hierarchy spec**. **Cross-check with canonical `binding-output-oracle-validation.md` §"Source Hierarchy":** that doc has a similar source-hierarchy table covering pinned headers / dynapi / shipped exports / SDL2-CS as oracles → **partial duplicate**. The oracle-evidence-design.md table adds family-keyed granularity (per-source family applicability: Core-only for dynapi/Cake-preview-today, all-families for ClangSharp/SDL2-CS/native-exports/pinned-headers) and authority labels (`candidate-output`/`generator-preview`/`runtime-symbol-name`/`compatibility-signal`/`header-source`). The family-keyed granularity is durable contribution → spec content outline §"binding-output-oracle-validation.md" should already cover the Source Hierarchy table; per Phase 0 audit it moves to `canonical/binding-output-oracle-validation.md` with toolchain-neutral methodology preserved. → no fold needed (the canonical playbook already owns Source Hierarchy methodology).
- "Structured Model" subsection + "Source Model" 5-field record (`sourceId`/`family`/`authority`/`path`/`status`) + "Function Model" 12-field record (managed method name / native entry point / native library / containing namespace+type / accessibility / import kind / return type / parameter list / `NativeTypeName` evidence / platform attributes / preprocessor guard / source file path) + "Type Model" enumerated initial type model (enums + explicit underlying type / structs / nested structs + field list / opaque struct candidates / `[StructLayout]` + `[FieldOffset]` evidence / function pointer fields and parameters / constants from `const`/`static readonly`/expression-bodied UTF-8 span properties): **DURABLE — Structured Model spec for oracle JSON serialization**. Already implicit in `canonical/binding-output-oracle-validation.md` reporting templates ("Hard Bugs"/"Likely Bugs"/"Compatibility Risks" sections with symbol + evidence rows). The detailed Function-Model + Type-Model field enumeration is **realized in `oracle.cs` source code itself** (FunctionEvidence/ConstantEvidence/TypeEvidence records per the oracle-evidence-implementation-plan task content). Code is the canonical authority for the in-memory model shape → no fold needed; this is implementation detail captured in code.
- "Roslyn Extraction Policy" subsection + 6-bullet syntax-extraction handles list (block/file-scoped namespaces / class-struct-enum-field-property-method-delegate declarations / `DllImport`/`LibraryImport`/`SupportedOSPlatform`/`UnmanagedCallConv`/`StructLayout`/`FieldOffset`/`NativeTypeName` attributes / `const` fields + `static readonly` fields + expression-bodied `ReadOnlySpan<byte>` properties / nested declarations / preprocessor directive trivia around platform attributes) + 3-bullet semantic-resolution-used-for list (`DllImport(LibName)` const-backed values / attribute identity normalization / constant values where syntax text insufficient) + 3-bullet preprocessor profiles list (Compat for legacy / Modern with `NET5_0_OR_GREATER` active when checking platform attributes): **DURABLE — Roslyn Extraction Policy spec**. Already realized in `oracle.cs` source code (CSharpEvidenceExtractor + CSharpParseOptions.WithPreprocessorSymbols pattern per oracle-evidence-implementation-plan Tasks 1-3). The methodology principle "use syntax as default extractor; semantic resolution only where it earns its cost" is durable Roslyn-discipline → already in `canonical/binding-output-oracle-validation.md` Methods table mention of "Roslyn analysis". → no fold needed (code is canonical for extraction mechanism; methodology principle is in oracle-validation playbook).
- "Report Shape" subsection + Markdown template `# ClangSharp Oracle Evidence / ## Inputs / ## Family: sdl2-core / ### Surface Counts / ### Raw ABI Constitution Checks / ### Function Evidence Matrix / ### Accepted Deferrals / ### Evidence Gaps` + family-first principle: durable report-shape spec → cross-check with `canonical/binding-output-oracle-validation.md` "Reporting templates" section (lines 359-401 area: `## Hard Bugs / ## Likely Bugs / ## Compatibility Risks` template). Partial overlap — the **family-first ordering with per-family Surface Counts + Raw ABI Constitution Checks + Function Evidence Matrix + Accepted Deferrals + Evidence Gaps** is durable family-aware extension beyond the playbook's family-agnostic template. → no fold needed (the family-first reporting is realized in `oracle.cs` Task 5 renderer; the playbook's template is the general shape; per Q11 spec the canonical/binding-output-oracle-validation.md inherits the methodology).
- "Classification labels" 6-row list (`Hard Bug` / `Likely Bug` / `Accepted Deferral` / `Compatibility Risk` / `Evidence Missing` / `Out Of Scope`) with definition column: **DURABLE — fold target per spec content outline → `binding-generator-implementation-notes.md` §10 (Oracle Classification Labels)**. This is the explicit task fold requirement. **Cross-check with `canonical/binding-output-oracle-validation.md` §"Classification"** (around L127-131): that playbook covers `Hard Bug` / `Likely Bug` / `Compatibility Risk` / `Design Signal` / `Accepted Delta` / `Follow-up` (6 labels) but **does NOT include `Evidence Missing` / `Out Of Scope`** and uses `Accepted Delta`/`Design Signal`/`Follow-up` instead of oracle-evidence-design.md's `Accepted Deferral`/`Evidence Missing`/`Out Of Scope`. The two label sets are **different and partially incompatible**: the oracle-evidence-design.md set is what `oracle.cs` actually emits in `oracle-evidence-clangsharp.md`; the playbook's set is the general multi-oracle review methodology. **Additional clarification needed in the fold:** implementation-notes §10 should codify the oracle.cs-specific 6-label set (Hard Bug / Likely Bug / Accepted Deferral / Compatibility Risk / Evidence Missing / Out Of Scope), and note the relationship to the playbook's broader 6-label set (Hard Bug / Likely Bug / Compatibility Risk / Design Signal / Accepted Delta / Follow-up). The two sets share Hard Bug / Likely Bug / Compatibility Risk; oracle.cs adds Evidence Missing + Out Of Scope (mechanical evidence-availability + family-applicability labels) and uses Accepted Deferral (synonym for playbook's Accepted Delta for policy-deferred items). The playbook's Design Signal + Follow-up labels are out-of-scope for oracle.cs (they apply to multi-oracle review by humans, not mechanical raw-ABI checks).
- "Initial Raw ABI Checks" subsection + 5 check-category subsections (Public Raw ABI Leak with `SDLNative`/`SDL_imageNative` examples / Missing SDL.h Required Surface with `SDL_Init`/`SDL_InitSubSystem`/`SDL_INIT_*` constants examples / Deferred Layout Violations with `SDL_RWops`/`SDL_SysWMinfo`/`SDL_SysWMmsg` examples / Platform-Sensitive Scalar Risks with C `long` int / unsigned long uint / `wchar_t*` ushort* examples / Family Identity Drift with SDL2_image `namespace SDL2` example): **DURABLE — Initial Raw ABI Checks spec**. **Cross-check with `canonical/binding-output-oracle-validation.md`:** the canonical playbook does not enumerate these 5 specific raw ABI check categories at this granularity; it covers them under generic "Source Hierarchy" + "Check methods" framing. The 5 check categories are realized as `RawAbiCheck` records in `oracle.cs` Task 4 (Implement Raw ABI Constitution Checks). The check categories themselves are durable raw-ABI-correctness gates → realized in code. Per Phase 0.2 audit, these check IDs (`raw-abi-public-class` / `raw-abi-public-import` / `required-function-missing` / `required-constant-missing` / `deferred-layout-sdl-rwops` / `deferred-layout-sdl-syswminfo` / `deferred-layout-sdl-syswmmsg` / `platform-sensitive-long` / `platform-sensitive-wchar` / `family-namespace-drift`) are durable oracle-check IDs that the next-iteration oracle (if it changes) must preserve. → no fold needed (the 5 check categories are realized in `oracle.cs` code which is the canonical authority; the canonical playbook covers the methodology at a higher level).
- "Relationship To ppy" subsection + 5-bullet ppy-aligned-in-generate_bindings.py list (header-by-header generation / RSP policy layering / future per-header `.rsp` lookup / future validation feedback into generation / future manual symbol/typedef feedback only when companion code exists and earns its place) + "Do not force oracle validation into Python just for aesthetic parity" closing paragraph: sunset-specific spike architecture decision (Python generation / Roslyn validation separation) → durable methodology principle "use the right tool for the job" is in AGENTS.md/extraction-guidelines; ppy-pattern adherence is in `canonical/references/ppy-reference-analysis-2026-05-25.md` → no fold needed.
- "Later Hooks" 6-bullet list (ppy manual `[Constant]` feedback / ppy `[Typedef]` feedback / `Unsafe_` string-return remap / public typed method projection / friendly string/span/ref/out overloads / native export extraction through `dumpbin`/`nm`/`otool`): sunset-specific deferred-work list → most folded into roadmap §"Layer 2 — Typed Public API Projection"/§"Layer 3 — Friendly Overload Projection" (renamed M5/M6 per Phase 0.1) or `next-iteration-plan.md` §"Review Follow-up Backlog". The "native export extraction through `dumpbin`/`nm`/`otool`" is durable production-CI-gate evidence channel → already in `canonical/binding-output-oracle-validation.md` Source Hierarchy (`Native exports | Runtime symbol-name evidence | Per-RID exported names`). → no fold needed.
- "Verification Plan" 5-numbered list (run file-based app / confirm report lists current known raw ABI gaps / run with `--family sdl2-core --family sdl2-image --write-report` / run `git diff --check` / run Slopwatch with repo-specific excludes from `AGENTS.md`): sunset-specific spike verification sequence → realized once for oracle.cs landing; verification discipline is in AGENTS.md → no fold needed.
- "Open Design Questions" 3-numbered list (emit both JSON+Markdown or Markdown only / Item 1 S1-1 retires legacy Python oracle-comparison once `oracle.cs` is active / native export discovery report-only vs fail-on-missing) + "Current recommendation" 3-bullet list: sunset-specific design-question status → questions 2-3 resolved by Item 1 + oracle.cs Task 5; question 1 (JSON+Markdown vs Markdown-only) is realized as Markdown-only in current oracle.cs per implementation plan Task 5. → no fold needed.

**Verdict:** additional fold needed (one fold). Fold the "Classification labels" 6-row list (Hard Bug / Likely Bug / Accepted Deferral / Compatibility Risk / Evidence Missing / Out Of Scope, with definitions) into `binding-generator-implementation-notes.md` §10 (Oracle Classification Labels) — this is the explicit task fold requirement. The fold should additionally codify the relationship to `canonical/binding-output-oracle-validation.md` §"Classification" 6-label set (Hard Bug / Likely Bug / Compatibility Risk / Design Signal / Accepted Delta / Follow-up): note that the two label sets share Hard Bug + Likely Bug + Compatibility Risk; oracle.cs uses Accepted Deferral (synonymous with playbook's Accepted Delta) + Evidence Missing + Out Of Scope (mechanical evidence-availability + family-applicability labels not present in the multi-oracle-review playbook); playbook's Design Signal + Follow-up labels are out of scope for mechanical raw-ABI checks. Everything else in oracle-evidence-design.md is either (a) sunset-specific design rationale now realized in `oracle.cs` source code, (b) duplicate of canonical `binding-output-oracle-validation.md` Source Hierarchy / Methods / Reporting Templates methodology, or (c) closure-record evidence with Priority A/B/C all-closed status. The Source Hierarchy / Structured Model / Roslyn Extraction Policy / Report Shape / Initial Raw ABI Checks subsections all cross-check vs `canonical/binding-output-oracle-validation.md` for duplicate coverage at the methodology level; the per-check-id + per-field-name granularity is realized in code (`oracle.cs`) which is the canonical authority for the mechanical contract.

### File: spikes/binding-generators/docs/oracle-evidence-implementation-plan.md

**Sections audited:**

- "For agentic workers" sub-skill banner ("REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development...") + "Goal" / "Architecture" / "Tech Stack" header block: sunset-specific implementation-plan-metadata → obsolete on oracle.cs ship → no fold needed.
- "File Structure" subsection + "Create" 1-row + "Modify" 3-row + "Generated / Updated By Command" 1-row + "Do Not Touch In This Plan" 4-row list: sunset-specific implementation-plan-scope → realized as `oracle.cs` shipped + spike README updated + handoff/plan updated. → no fold needed.
- "Implementation Notes" 6-bullet list (`#:property TargetFramework=net10.0` + `#:property PublishAot=false` / `#:package Microsoft.CodeAnalysis.CSharp@4.12.0` matching `Janset.SDL2.PostProcess.csproj` / self-contained no project file / built-in `--self-test` mode not test project / native export extraction report-only `not-wired` / retain legacy Python oracle-comparison until oracle.cs has equal-or-better coverage): sunset-specific implementation-plan-discipline → realized in `oracle.cs` source; the `#:package` version-pin pattern is durable .NET 10 file-based-app convention → already in `tools.cs` (cross-cutting Janset pattern). → no fold needed.
- "Task 1: Bootstrap `oracle.cs` With CLI And Self-Test Harness" 4-step task block + 60-line `oracle.cs` boilerplate code-block (file-based directives + OracleOptions parsing + SelfTests harness + Fixtures.MixedGeneratedSource + record stubs): sunset-specific implementation walkthrough → realized in shipped `oracle.cs`. The TDD pattern (RED test first → minimal extraction → GREEN verify) is durable test-driven-development discipline → already in `superpowers:test-driven-development` skill + AGENTS.md operating rules. → no fold needed.
- "Task 2: Extract Generated Binding Evidence From Real Files" 5-step task block + `OracleOptions` extension + `FamilyConfig` record + sdl2-core/sdl2-image config-hardcoding: sunset-specific implementation walkthrough → realized in shipped `oracle.cs`. → no fold needed.
- "Task 3: Add Dynapi, Manifest, Cake Preview, And SDL2-CS Evidence Lanes" 5-step task block + `DynapiParser.Parse(Fixtures.DynapiExports)` example + `ManifestRequiredSurfaceReader.Read(repoRoot)` example + SDL2-CS file-extraction reuse pattern: sunset-specific implementation walkthrough → realized in shipped `oracle.cs`. The `Cake preview as Core-only and generator-preview evidence` label is sunset-specific Cake-impl framing; post-spike-closure the Cake preview retires per Q10 (parking-lot folder retires). → no fold needed.
- "Task 4: Implement Raw ABI Constitution Checks" 5-step task block + `RawAbiCheck` record signature + 10-check enumeration (`raw-abi-public-class` / `raw-abi-public-import` / `required-function-missing` / `required-constant-missing` / `deferred-layout-sdl-rwops` / `deferred-layout-sdl-syswminfo` / `deferred-layout-sdl-syswmmsg` / `platform-sensitive-long` / `platform-sensitive-wchar` / `family-namespace-drift`): sunset-specific implementation walkthrough → realized in shipped `oracle.cs`. The 10 check IDs are durable mechanical-oracle-check IDs → already in oracle-evidence-design.md "Initial Raw ABI Checks" subsection (per the prior audit entry) and realized in `oracle.cs` code. → no fold needed.
- "Task 5: Render Family-Aware Markdown Report" 6-step task block + `OracleReport` renderer self-test fixture + "Hard Bug / Evidence Missing" classification mentions + report-section enumeration (Inputs table / per-family source status table / per-family surface counts / per-family raw ABI constitution checks grouped by classification / per-family function matrix / evidence gaps) + bounded-row discipline ("Show all `Hard Bug` and `Likely Bug` checks. For large symbol sets, show counts plus the first 50 sorted symbols and a remaining count line."): sunset-specific implementation walkthrough → realized in shipped `oracle.cs`. The bounded-row discipline ("show all Hard Bug / Likely Bug; cap large symbol sets at first 50 sorted + remaining count") is durable report-rendering discipline → not currently captured in `canonical/binding-output-oracle-validation.md`; the playbook covers the classification labels but not the row-bounding-discipline. **Possible fold consideration:** the bounded-row discipline could fold into the canonical playbook as an implementation hint, but it's narrow report-rendering implementation detail that is realized in code; the playbook's job is methodology-level, not row-rendering specifics. → no fold needed (code-level discipline; not durable across implementation swaps).
- "Task 6: Update Spike Docs And Run Verification" 8-step task block (update README quick command / update handoff and plan only if stale / run self-tests / run oracle report generation / run generated-output build truth gate / run whitespace check / run Slopwatch with repo-specific excludes / present commit gate summary): sunset-specific implementation workflow → AGENTS.md §"Approval Gate" + §"Slopwatch After Edits" + §"Operating Rules" cover the discipline. → no fold needed.
- "Self-Review Checklist" 4-bullet list (spec coverage / placeholder scan / type consistency / scope check): sunset-specific implementation-plan self-review → AGENTS.md §"Operating Rules" + superpowers:writing-plans skill cover the discipline. → no fold needed.

**Verdict:** ready to git rm. Empty unique-info kernel as expected per task description. Every task block is sunset-specific implementation walkthrough realized in shipped `oracle.cs` source code (the canonical artifact). The 10 raw ABI check IDs + the file-based app pattern + the TDD RED/GREEN discipline are durable but already captured elsewhere (oracle-evidence-design.md "Initial Raw ABI Checks" — handled in prior audit entry; AGENTS.md operating rules; superpowers:test-driven-development skill; `tools.cs` cross-cutting .NET 10 file-based app pattern; `canonical/binding-output-oracle-validation.md` methodology). Closure-record evidence lives in git history; the bounded-row report-rendering discipline is code-level implementation detail not durable across implementation swaps.

### File: spikes/binding-generators/docs/satellite-expansion-roadmap.md

**Sections audited:**

- "Date" + "Status" + "Branch" + "North star" header block (Status: SDL2 Core/Image/GFX/TTF/Mixer Layer 1 closed 2026-05-28; historical closure record + follow-up backlog; new forward work starts from Layer 2 planning; Branch `spike/binding-autogen-sdl2-gfx`): closure-record metadata → `git log --follow` archive → no fold needed.
- "Principle: Assumptions Are Explicit, Docs Are First-Class" subsection: sunset-specific roadmap principle (current best understanding vs binding design; deep-dive contradiction updates assumption in-place; inline code comments self-contained; phase numbers/ADR/migration timing belong in docs not logic-bearing code comments): partially duplicate of AGENTS.md operating rules + extraction-guidelines. The "docs are first-class artifacts" principle is durable → already in AGENTS.md + constitution §"Maintenance Rule". → no fold needed.
- "Cross-Cutting: Configuration, Policy, Shims, Oracle Standardization" subsection + 4 sub-sections (`config/family-config.json` + `clangsharp/shims/` + `clangsharp/compare_oracle.py` Remove + `clangsharp/oracle.cs` multi-family alignment): **DURABLE — fold target per Phase 0.2 audit prompt → `binding-generator-implementation-notes.md` §12 (Config Surface Evolution + Postprocess Pipeline Mechanism Detail)**. Specifically: (a) `config/family-config.json` is the unified config surface (single source of truth for binding-generator scope: per-family identities, header lists, opaque-handle + flags-enum rosters, clong methods lists, global platform views). The schema decision history (consolidation of stale roster JSONs, required-surface JSONs, `.headers.txt` scope files into single config) is durable Config Surface Evolution narrative. (b) `clangsharp/shims/platform-headers/` synthetic Windows-local Linux/macOS/iOS parse shims (3 files: `endian.h`, `AvailabilityMacros.h`, `TargetConditionals.h`) are spike-only iteration unblockers, opt-in via `--use-platform-header-shims` flag. → fold target for implementation-notes §2 (Generation Determinism Implementation — shim-flag-as-Windows-local-iteration-unblocker-not-production-substitute). (c) `compare_oracle.py` retired (replaced by `oracle.cs`) — closure-record → `git log --follow` archive. (d) `oracle.cs` 5-family alignment (FamilyConfigs extended with `Sdl2Ttf`, `Sdl2Mixer`, `Sdl2Gfx`; `RawAbiChecks` engine family-parameterized so adding families is data not logic change) — durable family-aware engine principle → already realized in shipped `oracle.cs` Task 6 extension; the family-parameterization principle is durable architectural rule → already in constitution §"Generator Engine And SDL Policy" ("core pipeline is an ABI engine for parsed declarations... SDL-specific decisions live behind named policy/profile concepts").
- "Item 1: Per-Library Generation Infrastructure" subsection + Status (Closed 2026-05-26) + Goal + 5-criterion Success Criteria + 5-subsection Current Understanding (FAMILY_CONFIG routing / Include directory wiring / Postprocess pipeline per-family independence / `OpaqueHandleEmitRewriter` family-keyed owner/consumer mode / `ClongDualDispatchRewriter` / `[Flags]` detection) + Reference Docs + Reference Code: **closure-record + durable cross-cutting facts**. Closure date 2026-05-26 + closure commits → `git log --follow` archive. **Durable facts that need fold targets:** (a) `FAMILY_CONFIG` routing extended with `ttf`/`mixer`/`gfx` entries kept dormant in `selected_families("all")` until each expansion item activates them — sunset-specific Iteration 1 mechanism; the durable principle "dormant family entries activate via aggregate-family selection list" is implementation-notes §12 (Config Surface Evolution) territory. (b) Postprocess pipeline per-family independence (each of 7 conceptual steps operates on one family's `Generated/{Compat,Modern}/` tree; `generate_bindings.py` orchestrator invokes per-family via `run_postprocess(family, ...)`; only cross-family flow is data-only roster pull defined by Constitution) — durable Postprocess Pipeline Mechanism Detail → fold target implementation-notes §13. (c) `OpaqueHandleEmitRewriter` family-keyed owner/consumer mode: auto-detection is structural (empty partial struct + pointer use at any ABI signature position; does NOT require `SDL_` prefix so satellite-owned `TTF_Font`/`Mix_Music` are covered when families activate); rewriter reads namespace from `.g.cs` file being processed and emits `Handles.g.cs` into that namespace; `force_opaque_exceptions` remain under `families.core` in `family-config.json`; satellite-owned handles (`TTF_Font`, `Mix_Music`) listed under owning family sections; `--owner-mode` determines which families emit own `Handles.g.cs` (owner: core/ttf/mixer; consumer: image/gfx); structural auto-detection without prefix matching is accepted Item 1 policy — durable Opaque Handles Implementation Mechanism extension → already targeted for implementation-notes §3 (Opaque Handles Implementation Mechanism) per Phase 0.1 constitution audit; this Item-1 closure adds the **family-keyed owner/consumer mode** + **structural auto-detection without prefix matching** + **per-family `Handles.g.cs` emit driven by `.g.cs` file's namespace** specifics; **additional fold needed:** these three specifics need to land in implementation-notes §3. (d) `ClongDualDispatchRewriter`: mode key `clong-dispatch`; `AffectedMethodNames` covers `SDL_ThreadID`, `SDL_GetThreadID`, and TTF's 5 C `long` functions (4 index params + 1 FontFaces return); parameter-position rewrite logic present; rewriter inspects return + parameter native type names; applied per-family + skips families with no matching function names — durable C `long` Hybrid Implementation extension → already targeted for implementation-notes §4 per Phase 0.1; **additional fold needed:** the parameter-position rewrite + per-family-skip mechanism specifics need to land in implementation-notes §4. (e) `[Flags]` detection: power-of-two value heuristic rejected (Constitution L448 friction; `SDL_bool` `SDL_FALSE=0`/`SDL_TRUE=1` would false-positive under naive heuristic; no peer validation — only Alimer auto-detects in production with name-suffix + hardcoded allow-list; ppy uses manual companion files incompatible with no-magic-companion philosophy; Silk.NET 2.X drops `[Flags]` entirely); landed policy = name-suffix detection (`EndsWith("Flags")` catches `IMG_InitFlags`/`MIX_InitFlags`/`SDL_RendererFlags`/etc.) + family-keyed allow-list for composite-alias-bearing enums (`SDL_Keymod`, `SDL_BlendMode`, `SDL_GLcontextFlag`, `SDL_RendererFlip`, `SDL_TextureModulate`) — durable `[Flags]` Detection Mechanism rationale + peer-evidence catalog → already targeted for implementation-notes §8 (`[Flags]` Detection Mechanism) per Phase 0.1 constitution audit; **additional fold needed:** the peer-comparison rationale (Alimer / ppy / Silk.NET behaviors) + power-of-two-heuristic-rejection-reasoning need to land in implementation-notes §8 as durable why-this-approach evidence.
- "Iteration 2 — Config Surface Unification" subsection + Status (Closed 2026-05-27) + Branch + Position + Binding constraints + Problem + 18-source configuration inventory (9 external + 9 code-internal) + What this iteration does NOT decide + Reference docs: **closure-record + durable Configuration Surface Evolution narrative**. Closure date 2026-05-27 + spec/plan cross-refs → `git log --follow` archive (spec/plan docs themselves retire per other Phase 0 audit entries). **Durable facts that need fold targets:** the 18-source configuration inventory snapshot (9 external + 9 code-internal data structures) is the **before-state** of Iteration 2; the **after-state** is `config/family-config.json` as single source of truth. The Configuration Surface Evolution narrative needs to land in implementation-notes §12 → already targeted per Phase 0.1 constitution audit + the Cross-Cutting subsection above; **additional fold needed:** the 18-source inventory table (or at minimum the after-state list of facts now consolidated in `family-config.json`: per-family identities + header lists + opaque-handle rosters + flags-enum rosters + clong methods lists + global platform views + RSP tier organization) is durable Config Surface Evolution evidence → implementation-notes §12 (Config Surface Evolution). The 3 risks (drift between source types / mixed config-vs-policy footprint / discoverability for new agents) are durable maintenance discipline insights → fold target implementation-notes §12 or `canonical/binding-generator-maintenance.md` "family-config.json maintenance" subsection (per spec §Q11 NEW maintenance playbook content outline includes "family-config.json schema maintenance"). The "manifest not touched" + "no determinism regression" + "ships before any expansion item" iteration constraints are sunset-specific Iteration 2 framing → obsolete on iteration close.
- "Item 2: SDL_image Bug Fixes" subsection + Status (Closed 2026-05-27) + 4-criterion Success Criteria + 5-subsection Current Understanding: closure-record → `git log --follow` archive. Durable facts: `IMG_InitFlags` at `SDL_image.h:95-103` with power-of-two values (JPG=0x01, PNG=0x02, TIF=0x04, WEBP=0x08, JXL=0x10, AVIF=0x20); same bug confirmed in Mixer's `MIX_InitFlags` — both handled by Item 1 S1-6 `flags-detect` postprocess step + suffix rule. **Cross-check satellite reconnaissance:** `sdl2-image-cross-check.md` confirms the IMG_InitFlags bug + power-of-two values + documentation framing (L11, L46, L89-119). **No reconnaissance gap.** → no fold needed (closure record archived; the `[Flags]` detection mechanism is targeted for implementation-notes §8 per the Cross-Cutting subsection audit above).
- "Item 3: Layer 1 Closure — SDL2_gfx" subsection + Status (Closed 2026-05-27) + Goal + 5-criterion Success Criteria + 11-subsection Current Understanding (102 functions across 4 headers / SDL2-CS compatibility baseline / Export macro risk per-header scope macros / No satellite-owned opaque handles / No C long callbacks unions platform-conditioned code / `M_PI` duplicate risk / Mixed naming 67 bare + 35 SDL_-prefixed / Assumption: 4 `--define-macro` insurance entries sufficient) + Family Identity (Namespace: SDL2.Gfx; Internal raw ABI class: SDL2_gfxNative; Native library: SDL2_gfx; Managed project: Janset.SDL2.Gfx) + Reference Docs + Reference Code + New Files to Create (rsp/sdl2-gfx.rsp / Janset.SDL2.Gfx.csproj / Support/DisableRuntimeMarshalling.cs / config/family-config.json families.gfx.headers[]): closure-record → `git log --follow` archive. **Durable facts cross-checked:** (a) 102 functions across 4 headers — `sdl2-gfx-header-analysis.md` rows 7-12 confirm (5 framerate + 59 gfxPrimitives + 30 imageFilter + 8 rotozoom = 102). **No reconnaissance gap.** (b) Per-header `SDL2_*_SCOPE` export macros — `sdl2-gfx-header-analysis.md` rows 17-20 confirm 4 scope macros. **No reconnaissance gap.** (c) `FPSmanager` 20-byte struct layout — `sdl2-gfx-header-analysis.md` rows 123-132 confirm. **No reconnaissance gap.** (d) `M_PI` duplicate emission risk — `sdl2-gfx-header-analysis.md` rows 181-213 confirm conditional definition in both `SDL2_gfxPrimitives.h:34-35` and `SDL2_rotozoom.h:40-41`. **No reconnaissance gap.** (e) Family identity (namespace SDL2.Gfx, raw class SDL2_gfxNative, native library SDL2_gfx) — already in constitution §"Family Identity" table per Phase 0.1 constitution audit. → no additional fold needed (closure-record archived; family identity in constitution; durable facts confirmed in satellite reconnaissance docs).
- "Item 4: Layer 1 Closure — SDL2_ttf" subsection + Status (Closed 2026-05-27) + Goal + 7-criterion Success Criteria + Peer Visual Notes (SDL2-CS uses `IntPtr` for `TTF_Font` / SDL2-CS binds C `long` as C# `long` ignoring Win64 / SDL2-CS includes no-SDLCALL glyph kerning + omits newer SDF / Janset excludes 3 deprecated Layer 1 functions + `TTF_SetError`/`TTF_GetError` macro aliases / `Silk.NET.SDL` 2.23.0 has 0 `TTF_` symbols — not useful peer evidence) + 8-subsection Current Understanding (88 functions / single header / 5 C `long` surface = 4 index params + 1 `FontFaces` return / Satellite-owned opaque `TTF_Font` no `SDL_` prefix / C `long` dispatch depends on Item 1 / No-SDLCALL functions 4 non-deprecated / Deprecated functions 3 to exclude / Error macros 2 excluded / Function-like helpers `TTF_VERSION`-style follow-up / No callbacks unions platform-conditioned / Assumption 32 rendering functions returning `SDL_Surface*` via Core ProjectReference) + Family Identity + Reference Docs + Reference Code + New Files to Create: closure-record → `git log --follow` archive. **Durable facts cross-checked:** (a) 88 functions in single header — implicit count from `sdl2-ttf-header-analysis.md`. (b) 5 C `long` surface (4 params + 1 return) — `sdl2-ttf-header-analysis.md` rows 34-40 + 55 + 110-129 confirm. **No reconnaissance gap.** (c) Satellite-owned opaque `TTF_Font` no `SDL_` prefix — `sdl2-ttf-header-analysis.md` row 141 confirms. **No reconnaissance gap.** (d) No-SDLCALL functions: `TTF_GetFontKerningSizeGlyphs`, `TTF_GetFontKerningSizeGlyphs32`, `TTF_SetFontSDF`, `TTF_GetFontSDF` — `sdl2-ttf-header-analysis.md` confirms (the doc covers no-SDLCALL functions). **No reconnaissance gap.** (e) Family identity (namespace `SDL2.Ttf`, raw class `SDL_ttfNative`, native library `SDL2_ttf`) — already in constitution §"Family Identity" table. (f) Cross-family error-macro-alias-exclusion (`TTF_SetError` / `TTF_GetError`) — `sdl2-satellite-error-function-consolidation.md` is the full cross-family analysis. **Durable cross-cutting fact:** error macro aliases across satellites (Image / TTF / Mixer all have `<FAM>_SetError`/`<FAM>_GetError`/`<FAM>_ClearError` aliases to Core SDL error APIs; Janset excludes them from raw output; SDL2-CS keeps them as manual redirects). This consolidation analysis is durable cross-family policy → implementation-notes §7 (Foreign Type Boundary Implementation) territory or maintenance playbook §"new-family addition checklist" → already implicit in spike satellite analysis docs that move to `canonical/satellites/` per Q11; the cross-family-error-macro-exclusion pattern needs explicit codification in implementation-notes §8 or §11. **Additional fold consideration:** the cross-family-error-macro-exclusion pattern as a policy rule may want a constitution §"Function Surface" sub-bullet (`satellite families exclude <FAM>_SetError/<FAM>_GetError/<FAM>_ClearError aliases to Core SDL error APIs; these are not satellite-owned functions and would create duplicate raw ABI surface`). This is the constitution-level rule; the implementation-notes-level entry is the exclusion-list mechanism + per-header RSP entries. → **additional fold needed (low priority):** add satellite-error-macro-exclusion policy to constitution §"Function Surface" or §"BCL-Replaceable Helper Exclusion Policy" as durable cross-family rule.
- "Item 5: Layer 1 Closure — SDL2_mixer" subsection + Status (Closed 2026-05-28) + Goal + 7-criterion Success Criteria + 8-subsection Current Understanding (97 functions / single header / no C long, wchar_t, variadics, unions, platform-conditioned code / Satellite-owned opaque Mix_Music same pattern as TTF_Font / Callback typedefs 6 total ClangSharp handles natively / Mix_Chunk plain 4-field POD / [Flags] gap MIX_InitFlags / Error macros 4 excluded / Version macros / Callback lifecycle risk / Assumption 7 functions with 8 callback-typed parameters all SDLCALL → __cdecl) + Deferred Follow-up: Mixer Callback Lifetime Smoke + Peer Visual Notes + Family Identity + Reference Docs + Reference Code + New Files to Create: closure-record → `git log --follow` archive. **Durable facts cross-checked:** (a) 97 functions in single header — `sdl2-mixer-header-analysis.md` confirms. (b) Satellite-owned opaque `Mix_Music` — `sdl2-mixer-header-analysis.md` row 67 confirms. **No reconnaissance gap.** (c) Callback typedefs 6 total — `sdl2-mixer-header-analysis.md` row 17 confirms 6 typedefs + 7 functions with 8 callback-typed parameters (Mix_RegisterEffect has two callbacks at SDL_mixer.h:1367). **No reconnaissance gap.** (d) `MIX_InitFlags` `[Flags]` decoration — `sdl2-mixer-header-analysis.md` rows 96 + 239 + 247 confirm power-of-two bitmask values + caught by suffix rule. **No reconnaissance gap.** (e) Mix_Chunk plain 4-field POD (no union) — `sdl2-mixer-header-analysis.md` confirms (contradicting earlier speculation noted in roadmap). **No reconnaissance gap.** (f) Error macro exclusion (Mix_SetError / Mix_GetError / Mix_ClearError / Mix_OutOfMemory) — covered by `sdl2-satellite-error-function-consolidation.md` (already noted in Item 4 audit). (g) **Mixer callback lifetime/rooting policy follow-up** (Compat delegate rooting; Modern `delegate* unmanaged[Cdecl]` authoring rules; dummy-audio setup; cleanup/unregistration policy) is durable forward-work item → already in `next-iteration-plan.md` §"Review Follow-up Backlog" (per Phase 0.2 satellite-expansion-roadmap fold target → next-iteration-plan.md Phase 4 restructure). → no fold needed (closure record archived; durable callback lifetime follow-up in next-iteration-plan).
- "Implementation Order & Dependencies" subsection + ASCII tree (Iteration 1: Item 1 closed → Iteration 2 closed → enables Items 2-5 sequential) + closing paragraph (Iteration 2 landed between Item 1 and Item 2; Items 3/4/5 closed sequentially; Item 2 stayed documented Image regression-test closure; SDL2 Core/Image/GFX/TTF/Mixer Layer 1 raw ABI expansion wave closed; remaining work moves to follow-up tracks below): closure-record dependency narrative → `git log --follow` archive → no fold needed.
- "What This Roadmap Does NOT Cover" 8-bullet list (Layer 2 deferred — M5 / Layer 3 deferred — M6 / Production flip src/Janset.SDL2.\*/ deferred — M7 / `.generated-stamp` deferred — M7 / 7-RID CI matrix for AbiTests deferred — production CI gate / SDL2_net not in scope / Alimer-style CppAst comparison evidence separate slice per ADR-004 / Package smoke against real native packages production gate): **DURABLE — fold target per Phase 0.2 audit prompt → `next-iteration-plan.md` via Phase 4 restructure**. All 8 items are forward-work items deferred from this Layer 1 expansion wave: Layer 2 (typed public API) + Layer 3 (friendly overloads) + Production flip + reproducibility-stamp + 7-RID CI matrix + SDL2_net + Alimer CppAst comparison + package smoke against real native packages. Already targeted for next-iteration-plan.md Phase 4 restructure per Phase 0.2 satellite-expansion-roadmap fold target (Production Flip Gates + Layer 2 follow-ups + Layer 3 follow-ups + Accepted Tradeoffs subsections); cross-checked against current `next-iteration-plan.md` content which already has `## Review Follow-up Backlog — 2026-05-25` with Handoff Blockers / Production Flip Gates / Layer 2 / Layer 3 / Accepted Tradeoffs subsections (per grep results in working notes). **Additional fold needed:** items not yet captured by name in current next-iteration-plan.md backlog — specifically "SDL2_net bindings not in scope for this expansion wave" + "Alimer-style CppAst comparison evidence separate slice per ADR-004" + "Package smoke against real native packages production gate" — should land in the next-iteration-plan.md Phase 4 restructure (or be confirmed already there during the actual Phase 2 / Phase 3 fold work).

**Verdict:** additional fold needed (multiple targets). (1) Fold "Cross-Cutting: Configuration, Policy, Shims, Oracle Standardization" subsection content into `binding-generator-implementation-notes.md` §12 (Config Surface Evolution: `config/family-config.json` as unified config surface with single-source-of-truth narrative for per-family identities, header lists, opaque-handle + flags-enum rosters, clong methods lists, global platform views) + §13 (Postprocess Pipeline Mechanism Detail: 7-step pipeline order `platform-delta → strip-varargs → libraryimport (Modern only) → flags-detect → guid-substitute → clong-dispatch → uniform-opaque`, per-family invocation discipline, only cross-family flow is data-only roster pull, postprocess runs as standalone console app not Roslyn SG) + §2 (Generation Determinism Implementation: `--use-platform-header-shims` flag as Windows-local iteration unblocker, not production substitute; 3 shim files for `endian.h`/`AvailabilityMacros.h`/`TargetConditionals.h`). (2) Fold Item 1 "Current Understanding" durable mechanism specifics into existing implementation-notes fold targets: family-keyed owner/consumer mode + structural-auto-detection-without-prefix-matching + per-family `Handles.g.cs` emit driven by `.g.cs`'s namespace → §3 (Opaque Handles Implementation Mechanism); `ClongDualDispatchRewriter` parameter-position rewrite + per-family-skip + return-and-parameter native-type-name inspection → §4 (C `long` Hybrid Implementation); `[Flags]` detection peer-comparison rationale (Alimer name-suffix + hardcoded allow-list, ppy manual companion files incompatible with no-magic philosophy, Silk.NET 2.X drops `[Flags]` entirely) + power-of-two-heuristic-rejection-reasoning (Constitution L448 friction, `SDL_bool` false-positive risk) → §8 (`[Flags]` Detection Mechanism). (3) Fold Iteration 2 18-source configuration inventory snapshot (9 external + 9 code-internal data structures listed) + 3-risk rationale (drift between source types / mixed config-vs-policy footprint / discoverability for new agents) into implementation-notes §12 (Config Surface Evolution) as durable before-and-after Config Surface Evolution narrative. (4) Fold "What This Roadmap Does NOT Cover" 8-bullet list into `next-iteration-plan.md` Phase 4 restructure (Review Follow-up Backlog) — confirm items already present: Layer 2 deferred + Layer 3 deferred + Production flip deferred + reproducibility-stamp deferred + 7-RID CI matrix deferred + Accepted Tradeoffs — and add items if not yet present: SDL2_net bindings not in scope (Accepted Tradeoffs subsection) + Alimer-style CppAst comparison evidence separate slice per ADR-004 (Accepted Tradeoffs subsection) + Package smoke against real native packages (Production Flip Gates subsection). (5) Lower-priority fold: satellite-error-macro-exclusion policy (cross-family pattern: `<FAM>_SetError`/`<FAM>_GetError`/`<FAM>_ClearError` aliases to Core SDL error APIs excluded across Image/TTF/Mixer raw output) → constitution §"Function Surface" or §"BCL-Replaceable Helper Exclusion Policy" as durable cross-family rule (only if not already implicit in either; otherwise sunset-specific implementation-notes §7 territory). **Satellite reconnaissance cross-check (per task special-care instruction):** Item 3 GFX 102 functions + 4 scope macros + FPSmanager 20-byte struct + M_PI duplicate risk all confirmed in `sdl2-gfx-header-analysis.md`. Item 4 TTF 88 functions + 5 C `long` surface + no-SDLCALL functions + TTF_Font satellite-owned opaque all confirmed in `sdl2-ttf-header-analysis.md`. Item 5 Mixer 97 functions + 6 callback typedefs + Mix_Music satellite-owned + Mix_Chunk POD + MIX_InitFlags power-of-two all confirmed in `sdl2-mixer-header-analysis.md`. Item 2 Image IMG_InitFlags bug + power-of-two values confirmed in `sdl2-image-cross-check.md`. **Zero gaps confirmed (matches Phase 0.2 audit assertion).** Everything else in satellite-expansion-roadmap.md is closure-record narrative archived in git history.

### Files: spikes/binding-generators/docs/items/{item-1-plan, iteration-2-plan, item-3-plan, item-4-plan, item-5-plan}.md

**Sections audited (all 5 plans):**

- Plan headers (Goal / Architecture / Tech Stack / Date / Status / Branch / Spec / Parent roadmap / Approval gate cross-refs): execution scaffolding metadata; work shipped; no durable kernel.
- "How to read this plan" / "Slice ordering rationale" / "Per-slice common requirements" / "Spec deltas — read before executing" / "Hard constraints" / "Branching & commit workflow" / "Environment preconditions" / "Determinism gate" / "File structure" preambles: TDD discipline + commit-sized-slice + bite-sized-step + Constitution-authority + plan-doc reference discipline + work-branch-squash workflow + per-phase determinism gate are operating-rule scaffolding, already covered by AGENTS.md and the consolidation design spec; no durable kernel.
- Slice/Phase task lists (S1-0 through S1-7 in item-1-plan; Phase 0 through Phase 5 in iteration-2-plan; Tasks 1-N in item-3/4/5-plan) — including RED/GREEN steps, exact code blocks, exact PowerShell/Python/bash commands, expected outputs, validation greps, file-create checklists, Slopwatch invocations, Determinism Contract D.1-D.5 reproducible probes, byte-equivalence checks: execution scaffolding; work shipped 2026-05-26 through 2026-05-28; no durable kernel — the durable mechanism behind each task lives in the corresponding spec (audited separately below) and in the canonical fold targets identified by those spec audits (constitution + implementation-notes §§3/4/8/12/13 + next-iteration-plan + satellites).
- "Slice exit evidence" / "Approval-gate exception" / "Self-Review Checklist" closing blocks: per-slice process gates and squash-landing approval workflow; sunset-specific execution scaffolding; no durable kernel.
- item-5-plan "Status: Closed 2026-05-28" header note + `[x]`-marked checkboxes (executed steps preserved as history): closure-record + executed-task evidence — durable closure date already captured in roadmap §"Item 5: Layer 1 Closure — SDL2_mixer" Status row (already audited above) and in git history (`08b7e74` commit); plan-doc closure metadata is execution scaffolding → no fold needed.

**Verdict (all 5):** ready to git rm — empty unique-info kernel per spec §Q7. All durable mechanism kernels referenced by these plans land in their companion specs (audited individually below). All operating-rule scaffolding is already covered by AGENTS.md, the consolidation design spec, and the canonical fold targets (constitution + implementation-notes + next-iteration-plan + satellites). Plans retire with the items/ folder.

### File: spikes/binding-generators/docs/items/item-1-per-library-generation-spec.md

**Sections audited:**

- §1 Goal: Item 1 infrastructure goal (make `generate_bindings.py` + 7-step postprocess pipeline + `oracle.cs` per-family deterministic and family-agnostic; GFX/TTF/Mixer dormant in `--family all` until expansion items activate; FAMILY_CONFIG dynamic; `OpaqueHandleEmitRewriter` discovers satellite-owned handles without name-prefix gate; `ClongDualDispatchRewriter` rename + parameter-position support; new 7th `flags-detect` step; oracle multi-family extension): closure-record + closed-iteration framing → `git log --follow` archive; the durable kernel (family-agnostic pipeline + dormant-activation pattern) is already promoted via Iteration 1 / Iteration 2 closure into the constitution §"Generator Engine And SDL Policy" + §"Manifest Configuration Vs Code-Owned Policy" and into the audit roadmap §"Cross-Cutting" subsection landing on implementation-notes §§12 + 13 → no fold needed (kernel already targeted via roadmap audit).
- §2 Authority Order: Constitution Authority Order restated for the iteration; duplicate of constitution §"Authority Order" and AGENTS.md cross-reference → no fold needed.
- §3 Non-Goals: Item 1 non-goals (no Layer 2/3, no production flip, no `.generated-stamp`, no new family output, no 7-RID matrix, no SDL2_net, no compare_oracle.py evolution, no companion `[Constant]`/`[Typedef]` regex feedback loop, no function-like public macro helper completion, no generated no-op file cleanup): sunset-specific iteration-scoping → mostly closure-record → `git log --follow` archive. The "function-like macro helper" and "generated no-op file" notes are durable open-work items already in `next-iteration-plan.md` per roadmap audit → no fold needed.
- §4 Current-State Baseline (Evidence): §4.1 orchestrator concerns + §4.2 postprocess pipeline + §4.3 OpaqueHandleEmitRewriter shape + §4.4 ThreadIdDualDispatchRewriter shape + §4.5 Oracle + §4.6 Reference satellite shape (Janset.SDL2.Image template) + §4.7 Cross-cutting status: **pre-Iteration-1 baseline snapshot** with code line numbers (FAMILY_CONFIG L327-344, selected_families L776-779, etc.). Sunset-specific snapshot — code state has since changed under Iteration 2's config-surface unification. The durable elements (Image template csproj 5-TFM split + Generated/{Compat,Modern}/ layout + Support/DisableRuntimeMarshalling.cs + Support/NativeTypeNameAttribute.cs) are already in current implementation, will be referenced from `canonical/binding-generator-maintenance.md` §7 (Adding a New SDL Satellite Family checklist) per spec §Q11 maintenance-playbook content outline → no fold needed (Image-template kernel already targeted for maintenance playbook).
- §5.1 Dormant `all` activation set: activation contract (Items 3/4/5 each add their family identifier to `selected_families("all")` as part of their slice; gated by same-item exit evidence not a separate slice): sunset-specific iteration-1 mechanism; the durable principle "dormant family entries activate via aggregate-family selection list" is already targeted for implementation-notes §12 (Config Surface Evolution) per roadmap audit → no fold needed.
- §5.1.1 CLI unit amendment — complete family artifact: `generate_bindings.py` operates at complete-family-artifact level; public controls are `--family/--execute/--vcpkg-triplet/--use-platform-header-shims`; Compat/Modern internal backends always run together; execute-mode cleans selected family's `Generated/` root; bootstrap/full subset retired; one production header list per family in `FAMILY_CONFIG[family]["headers"]`; required SDL.h surface validation runs for execute-mode Core: **durable CLI contract** → already targeted for implementation-notes §13 (Postprocess Pipeline Order) per roadmap audit + maintenance playbook generation-workflow section per Q11. **Additional fold needed (low-priority confirmation):** the "execute-mode cleans selected family's `Generated/` root" + "Compat/Modern always run together as internal backends" details belong in implementation-notes §13 or maintenance playbook §"Version-Bump Procedures" (the regen-workflow narrative); confirm these specifics land in the new docs.
- §5.2 Family-blind opaque auto-detection: **drops `StartsWith("SDL_")` prefix gate**; detection becomes purely structural (empty `public partial struct X { }` ∩ `X*` at any raw ABI signature position); `BuildHandlesFileContent` takes family namespace as parameter; roster JSON migrates to **family-keyed schema 2.0** (top-level `families` object; per-family `library_version`/`last_audited`/`auto_detect_well_known`/`force_opaque_exceptions`/`excluded_candidates`); satellite-owned handles `TTF_Font`/`Mix_Music` explicitly rostered under family sections — no asymmetry between Core and satellites; drift watchdog per-family with consumer short-circuit; cross-family `force_opaque_exceptions` (`SDL_RWops`, `SDL_SysWMinfo`, `SDL_SysWMmsg`) live exclusively in `families.core.force_opaque_exceptions` resolved by-value via ProjectReference; `--handles-namespace` CLI flag; cross-assembly contract `[assembly: DisableRuntimeMarshalling]` under `#if NET7_0_OR_GREATER` per owner-mode csproj; **peer divergence note**: ppy/SDL3-CS and Silk.NET 2.X keep satellite handles as empty `partial struct X` used via `X*` pointer; SDL2-CS uses `IntPtr` with `/* IntPtr refers to a TTF_Font* */` comments; zero peers use typed Pattern B by-value handles for satellites; Janset's Pattern B is deliberate divergence: **DURABLE — kernel covering (a) structural-auto-detection-without-prefix criterion + (b) family-keyed schema 2.0 + (c) cross-family handle name resolution for Pattern B by-value uniformity + (d) drift watchdog per-family + (e) per-family `Handles.g.cs` emit driven by namespace param + (f) `--handles-namespace` CLI plumbing rationale (Janset's nested-namespace divergence) + (g) peer-divergence catalog**. Constitution §"Opaque Handles" already covers (a) auto-detect criterion + (b) family-keyed roster + (c) cross-family handle name resolution per Item 1's S1-0a commit. **Additional fold needed:** the mechanism-level details — namespace-as-parameter to `BuildHandlesFileContent`, the `--handles-namespace SDL2.Ttf` CLI flag wiring, consumer short-circuit logic, ProjectReference-based cross-family by-value resolution mechanism, peer-divergence catalog (ppy/SDL3-CS + Silk.NET 2.X + SDL2-CS comparison) — land in `binding-generator-implementation-notes.md` §3 (Opaque Handles Implementation Mechanism) per design spec §"New Documents — Content Outlines". This matches the §Q7 concern about item-1-spec's OpaqueHandleEmitRewriter auto-detect criterion + owner/consumer mode design.
- §5.3 ClongDualDispatchRewriter — true Roslyn node-level mutation: rename `threadid-dispatch` → `clong-dispatch` (postprocess mode key + file name + class name); expand `AffectedMethodNames` with 5 TTF symbols (`TTF_OpenFontIndex`/`TTF_OpenFontIndexRW`/`TTF_OpenFontIndexDPI`/`TTF_OpenFontIndexDPIRW`/`TTF_FontFaces`) as data only; add parameter-position rewrite support via **true Roslyn syntax mutation** (move mutation point from `VisitMethodDeclaration` to `VisitClassDeclaration`; build replacement members via `SyntaxFactory` calls; inherits Roslyn's normalization automatically; composes cleanly with future attribute-shape changes); `BuildModernMembers` / `BuildCompatMembers` shape; **signed-vs-unsigned discriminator inspects `[NativeTypeName(...)]` annotation**: TTF index params are signed `long`, `SDL_threadID`/`SDL_ThreadID` returns are `unsigned long`; risk = byte-identical output between text-substitution and Roslyn `SyntaxFactory` formatting non-trivial → acceptable one-time formatting churn; **peer evidence**: alimer-bindings-sdl validates Modern path (`CLong`/`CULong` emitted directly via Roslyn `SyntaxFactory`); ppy/SDL3-CS and SDL2-CS ship Constitution-rejected anti-pattern (`uint`/`int` for unsigned long / long on LLP64 — silently wrong on Linux LP64); Microsoft's `RuntimeInformation.IsOSPlatform` dispatch pattern is the only Compat-path precedent: **DURABLE — kernel covering (a) text-substitution → Roslyn-node-level migration rationale + (b) parameter-position rewrite mechanism via `VisitClassDeclaration.Members` + (c) signed-vs-unsigned discriminator via NativeTypeName annotation + (d) peer evidence catalog (alimer Modern-only / ppy+SDL2-CS anti-pattern / MS RuntimeInformation precedent) + (e) byte-identical formatting churn acceptance rule**. Constitution §"C `long` And `unsigned long`" already covers the hybrid emit policy. **Additional fold needed:** the mechanism details (VisitClassDeclaration-level mutation, SyntaxFactory MethodDeclaration construction, NativeTypeName-driven discriminator, byte-identical formatting churn rule) land in `binding-generator-implementation-notes.md` §4 (C `long` Hybrid Implementation). The peer-evidence catalog lands in §4 as durable why-this-approach evidence.
- §5.4 `[Flags]` detection — new 7th postprocess step: alimer-style **name-suffix + family-keyed allow-list** detection rule; rewriter does NOT analyze enum values (pure name operation); already-decorated enums pass through; **power-of-two value heuristic rejected for three reasons**: (1) Constitution friction (L448 pre-edit) — do not invent `[Flags]` solely because values are powers of two; (2) `SDL_bool` false-positive risk — `SDL_FALSE=0`, `SDL_TRUE=1` both qualify under naive check; strengthening with "≥2 distinct non-zero values" fixes specific case but doesn't address future shapes; (3) no peer validation — only alimer auto-detects, uses precisely name-suffix + hardcoded allow-list (`CsCodeGenerator.Enum.cs:256-264`); ppy/SDL2-CS use manual companion-file annotations; Silk.NET 2.X drops `[Flags]` entirely; **`policy/flags-enum-roster.json` schema 2.0** family-keyed (top-level `families` object; per-family `library_version`/`allow_list[]`) — symmetry with opaque-handle roster; Item 1 ships rewriter + new pipeline step + roster JSON; Item 2 verifies on `IMG_InitFlags`; Core gains `[Flags]` on enums qualified by §5.4 (suffix: `SDL_MessageBoxFlags`/`SDL_MessageBoxButtonFlags`/`SDL_RendererFlags`/`SDL_WindowFlags`; allow-list: `SDL_Keymod`/`SDL_BlendMode`/`SDL_GLcontextFlag`/`SDL_RendererFlip`/`SDL_TextureModulate`); `SDL_bool` does NOT gain `[Flags]`; pipeline order `flags-detect` runs after `libraryimport` and before `guid-substitute` (enum-only, no compositional constraint — ordering for readability not correctness): **DURABLE — kernel covering (a) name-suffix + family-keyed allow-list detection rule + (b) three-reason power-of-two-rejection rationale (Constitution friction / SDL_bool false-positive / no peer validation) + (c) peer-evidence catalog (alimer-detects / ppy-manual / Silk.NET-drops) + (d) family-keyed roster schema 2.0 symmetric with opaque roster + (e) pipeline order rationale (post-libraryimport, pre-guid-substitute, enum-only no compositional constraint)**. Constitution §"Enums" already covers the auto-decoration policy + family-keyed allow-list (per Item 1 S1-0a commit). **Additional fold needed:** the why-this-approach mechanism rationale (three-reason power-of-two-rejection + peer-evidence catalog + pipeline-order readability rationale) lands in `binding-generator-implementation-notes.md` §8 (`[Flags]` Detection Mechanism). Already covered by Cross-Cutting subsection audit fold target above — this audit confirms the spec's three-reason rationale + peer catalog are the durable kernel for implementation-notes §8.
- §5.5 Owner-mode resolution (no functional change — documented for clarity): `generate_bindings.py:1267` passes `--owner-mode owner|consumer` explicitly; `owner_mode = "owner" if family in ("core", "ttf", "mixer") else "consumer"` (GFX/Image consumer, TTF/Mixer become owners when activated); substring-based fallback in `UniformOpaqueOwnerMode.IsOwnerDirectoryByPath` remains as safety net only — explicit `--owner-mode` is the contract; each owner-mode family additionally passes `--handles-namespace SDL2.<Family>`: **DURABLE — kernel covering (a) explicit-CLI-flag-over-substring-fallback contract + (b) per-family owner/consumer assignment table + (c) handles-namespace-per-owner-family wiring**. **Additional fold needed:** the explicit-CLI-flag contract + per-family owner/consumer assignment land in `binding-generator-implementation-notes.md` §3 (Opaque Handles Implementation Mechanism) as part of the owner-mode subsection or in maintenance playbook §6 (Configuration Surface Map) as a per-family `owner_mode` flag documented. The Iteration 2 config-surface unification has since moved this from CLI flag to `families.<family>.owner_mode` boolean in `family-config.json` — confirm landing reflects post-Iteration-2 state, not pre-Iteration-1 baseline state.
- §5.6 Oracle multi-family extension (data-only): `oracle.cs FamilyConfigs` extended with `Sdl2Ttf`/`Sdl2Mixer`/`Sdl2Gfx` records (FamilyConfig 5-arg record shape: family-id, display-name, namespace, raw-class-name, cake-preview-path, compat-path, modern-path, sdl2-cs-path, dynapi-flag); `OracleRunner.KnownFamilies` extended to 5-entry array; **no new RawAbiChecks logic** — engine is already family-parameterized; `required-function-missing`/`required-constant-missing` (L1164) Core-only by design (manifest required surface is Core-only); satellites whose generation directories don't yet exist return `SourceStatus.Missing` (existing `LoadOptionalCSharpEvidence` path); Item 1 exit gate "0 findings across Priority C categories for sdl2-core + sdl2-image": **DURABLE — kernel covering (a) family-parameterized RawAbiChecks engine + (b) Core-only required-surface check carve-out + (c) `SourceStatus.Missing` for not-yet-generated satellites**. Iteration 2 §Q7 deferred oracle from config-surface unification — oracle retains its own `FamilyConfigs`/`KnownFamilies` records. The family-parameterized engine + missing-source handling are durable cross-family principles → already in constitution §"Generator Engine And SDL Policy" (family-parameterized engine is the durable architectural rule). The `SourceStatus.Missing` for not-yet-generated satellites is durable evidence-loading mechanism → fold target `binding-output-oracle-validation.md` playbook (which moves to `canonical/` per Q11) or implementation-notes §10 (Classification Labels) — confirm whether the playbook explicitly covers `SourceStatus.Missing` handling. **Additional fold needed (low-priority):** confirm oracle-validation playbook documents `SourceStatus.Missing` evidence-loading + family-parameterized RawAbiChecks engine as the durable cross-family pattern.
- §5.7 Per-family header lists/RSP/csproj/Support — defer creation to Items 3/4/5: Item 1 does NOT create scope/sdl2-{ttf,mixer,gfx}.headers.txt + rsp/sdl2-{ttf,mixer,gfx}.rsp + src/Janset.SDL2.{Ttf,Mixer,Gfx}/{Janset.SDL2.X.csproj,Support/DisableRuntimeMarshalling.cs}; rationale = Item 1 is infrastructure for these families' arrival; data files belong to items that bring families to life; adding empty stubs invites drift: sunset-specific iteration-scoping → closure-record → `git log --follow` archive. The per-family-creation-checklist principle is durable → already targeted for `canonical/binding-generator-maintenance.md` §7 (Adding a New SDL Satellite Family) per Q11 maintenance-playbook content outline → no fold needed.
- §6 Success Criteria (10-criterion table): success criteria #1 byte-identical-Core+Image-output-except-`[Flags]`-additions + #2 family-isolation-determinism + #3 multi-TFM build clean (Core+Image, 5 TFMs) + #4 oracle 5-family report + #5 FAMILY_CONFIG ttf/mixer/gfx entries reachable but dormant + #6 OpaqueHandleEmitRewriter.DiscoverAutoDetectedHandles returns TTF_Font/Mix_Music structurally + #7 ClongDualDispatchRewriter Core output byte-identical (modulo Roslyn formatting) + #8 IMG_InitFlags `[Flags]` after regen + #9 Slopwatch clean + #10 compare_oracle.py removed: sunset-specific iteration exit gates → closure-record → `git log --follow` archive. The success-criterion structure (byte-identical regression baseline + family-isolation + multi-TFM build clean + oracle clean + Slopwatch clean) is durable verification-gate pattern → already covered by Constitution §"Generation Determinism Contract" + testing-strategy + roadmap "Operating Rules" → no fold needed.
- §7 Cross-Cutting Standardization (4-item table): remove `compare_oracle.py` (sunset); KEEP `sdl2-core-sdlh-required.json` (load-bearing); remove `comparison-report-template.md` if exists and unreferenced; confirm `policy/opaque-handle-roster.json` family-keyed schema 2.0; confirm `shims/platform-headers/` untouched: closure-record cross-cutting standardization → `git log --follow` archive. Already covered by roadmap §"Cross-Cutting" audit fold target → no fold needed.
- §8 Assumption Resolutions vs roadmap (7-row table): all 7 assumption resolutions are closure-record reconciliation between roadmap "Current Understanding" and Item 1 actual implementation — sunset-specific iteration-scoping → `git log --follow` archive. The resolution outcomes (FAMILY_CONFIG stats+write_report logic touches needed; satellite headers flat in include/SDL2/; no cross-family postprocess coupling; structural auto-detect without prefix is sufficient; name-based AffectedMethodNames + structural sensor; Compat dual-DllImport works for parameter positions via Roslyn; power-of-two rejected for name-suffix + allow-list) are already in constitution + targeted for implementation-notes §§3/4/8 per the §5.2/§5.3/§5.4 audits above → no fold needed.
- §9 Risks (5-row table): Roslyn `SyntaxFactory` formatting churn (low; single regen+commit) + `[Flags]` roster misses future bitmask without suffix (low; intentionally value-blind, add to allow-list when evidence proves) + satellite-owned handle struct gets fields added upstream breaking structural auto-detect (low; drift report extends to owner-mode satellite zero-handle warning) + selected-driven enumeration breaks parity (low; success criterion #1 catches) + `compare_oracle.py` removal breaks hidden caller (low; grep zero non-historical references): durable risk catalog with mitigations → mostly absorbed into constitution + implementation-notes per §5.x audits above. The "drift report extends to owner-mode satellite zero-handle warning" is a forward-work item → fold target `next-iteration-plan.md` (forward backlog) or implementation-notes §3 (Opaque Handles Implementation Mechanism) as drift-watchdog mechanism extension. **Additional fold needed (low-priority):** drift-watchdog-owner-mode-zero-handle-warning extension lands in implementation-notes §3 or next-iteration-plan forward backlog.
- §10 References (Authority/Parent docs/Header analyses/Code anchors/Knowledge base): cross-reference index; line-number-bearing code anchors (FAMILY_CONFIG L327-344, etc.) are pre-Iteration-2 baseline — superseded by current code state and Iteration 2's config-surface unification → sunset-specific reference list → no fold needed (cross-refs to constitution + ADR-004 + parent docs are absorbed by the doc consolidation Phase 1 cross-ref refresh).
- §11 Out of Scope: cross-reference to roadmap §"What This Roadmap Does NOT Cover" — duplicate → no fold needed (roadmap audit already targets next-iteration-plan Phase 4 restructure).

**Verdict:** additional fold needed (multiple targets). (1) `OpaqueHandleEmitRewriter` mechanism specifics from §5.2 (BuildHandlesFileContent namespace parameterization + `--handles-namespace` CLI plumbing + structural-auto-detection-without-prefix-matching + consumer short-circuit + per-family drift watchdog + peer-divergence catalog ppy/Silk.NET/SDL2-CS) → `binding-generator-implementation-notes.md` §3 (Opaque Handles Implementation Mechanism). This is the spec §Q7 concern explicitly named for cross-checking against §3. (2) `ClongDualDispatchRewriter` mechanism specifics from §5.3 (VisitClassDeclaration-level mutation rationale + SyntaxFactory MethodDeclaration construction + NativeTypeName-driven signed-vs-unsigned discriminator + byte-identical formatting churn acceptance + peer-evidence catalog alimer/ppy/SDL2-CS/MS RuntimeInformation) → `binding-generator-implementation-notes.md` §4 (C `long` Hybrid Implementation). (3) `[Flags]` detection rationale from §5.4 (three-reason power-of-two-rejection + peer-evidence catalog + pipeline-order rationale) → `binding-generator-implementation-notes.md` §8 (`[Flags]` Detection Mechanism) — confirms Cross-Cutting subsection audit fold target. (4) Owner-mode resolution mechanism from §5.5 (explicit-CLI-flag-over-substring-fallback contract + per-family owner/consumer assignment + handles-namespace-per-owner wiring; reflecting post-Iteration-2 state where this became a `family-config.json` boolean rather than CLI flag) → `binding-generator-implementation-notes.md` §3 (Opaque Handles Implementation Mechanism) owner-mode subsection. (5) Oracle `SourceStatus.Missing` evidence-loading + family-parameterized RawAbiChecks engine (§5.6) → confirm coverage in `canonical/binding-output-oracle-validation.md` playbook (Phase 1 destination per Q11). (6) Drift-watchdog-owner-mode-zero-handle-warning extension (§9) → implementation-notes §3 or next-iteration-plan forward backlog. The pre-Iteration-2 `--handles-namespace` CLI flag has since been promoted into `families.<family>.namespace` in `family-config.json` per Iteration 2 config-surface unification — implementation-notes §3 should reflect post-Iteration-2 mechanism, not pre-Iteration-1 baseline (otherwise the doc captures stale CLI plumbing that no longer exists in the spike).

### File: spikes/binding-generators/docs/items/iteration-2-config-surface-unification-spec.md

**Sections audited:**

- §1 Problem Statement + §1.1 Non-Goals: 25-source (originally 18) configuration dispersion across Python `FAMILY_CONFIG` + C# `oracle.cs FamilyConfigs` + `Program.cs ResolveFamilyFromOutputDir` + `UniformOpaqueFamilyIdentity.cs` + `UniformOpaqueOwnerMode.cs` (4 copies of family directory mapping); 3 concrete risks (drift between source types / discoverability tax for new agents / expansion tax for Items 3-5 multiplying mess); non-goals (no `build/manifest.json`, no per-header RSP lookup change, no csproj/`.slnx` change, no TTF/Mixer/GFX activation, config seeded mechanically): **DURABLE — kernel covering (a) 25-source pre-unification config inventory across Python+C#+JSON+RSP+text+csproj + (b) 4-copy family-directory-mapping drift surface + (c) 3-risk rationale (drift / discoverability / expansion tax) + (d) mechanical-seed-not-hand-copy discipline + (e) iteration constraint catalog (no manifest touch / per-header RSP unchanged / config seeded mechanically)**. Constitution §"Manifest Configuration Vs Code-Owned Policy" covers the manifest-vs-policy boundary at the *policy* level. The 25-source inventory + 4-copy drift surface + 3-risk rationale are mechanism-level durable evidence → **Additional fold needed:** all of §1 + §1.1 lands in `binding-generator-implementation-notes.md` §12 (Config Surface Evolution) per design spec §"New Documents — Content Outlines" — this is the destination explicitly named for iteration-2-spec's 18-source pre-unification inventory per the §Q7 concern. This audit confirms the spec §Q7 second concern: the iteration-2-spec has unique pre-unification-inventory + drift-surface evidence that needs implementation-notes §12.
- §2 Verified Config Surface Inventory + §2.1 Current state (25-source table) + §2.2 Not moving (15-row table): **the canonical 25-source inventory**: 19 absorbed-into-new-config items (Core required surface + opaque-handle roster + flags-enum roster + 2 header lists + FAMILY_CONFIG + PLATFORM_SENSITIVE_HEADERS + ALL_PLATFORM_MACROS + SDL2_PLATFORM_VIEWS + ClongDualDispatchRewriter.AffectedMethodNames + 4 family dir→ID/namespace→ID mappings duplicated across C# files + owner directory detection + owner_mode_for_family + Platform views C# copy + oracle.cs FamilyConfigs deferred + oracle.cs KnownFamilies deferred + postprocess default namespace fallback) + 15 not-moving items (CODEGEN_CONFIG cross-cutting + PRODUCTION_CODEGEN_PASSES codegen flow + selected_families CLI flow + postprocess_steps_for_codegen pipeline orchestration + vcpkg_triplet runtime environment + ClangSharp tool version manifest + per-header RSP contents filesystem data + ClongDualDispatchRewriter emit logic policy mechanism + C long native type classification ABI knowledge + FlagsAttributeRewriter suffix rule policy mechanism + GuidSubstitutionRewriter substitution logic + OpaqueHandleEmitRewriter struct template + StripVarargsRewriter / DllImportToLibraryImportRewriter policy mechanism + Platform header shims parse aids + vcpkg.json native build input + csproj TFM patterns + Directory.Build.props/.slnx build infrastructure): **DURABLE — kernel covering the comprehensive 25-source inventory + the not-moving rationale (mechanism vs scope; ABI knowledge stays in code; build infrastructure orthogonal)**. **Additional fold needed:** §2 + §2.1 + §2.2 tables land in `binding-generator-implementation-notes.md` §12 (Config Surface Evolution) as the durable before-state evidence for the manifest-vs-code-owned-policy boundary documented by Constitution §"Manifest Configuration Vs Code-Owned Policy". This is the §Q7 second concern destination explicitly cross-checked.
- §3 Classification Table (7-row config-owns vs code-owns matrix): family identity / opaque handles / flags enums / C long / platform views / required surface / postprocess identity — for each row, the config-owned "which" data vs code-owned "how" mechanism: **DURABLE — kernel codifying the manifest-vs-code-owned-policy boundary at the row-by-row mechanism level**. Constitution §"Manifest Configuration Vs Code-Owned Policy" + §"Configurable Scope Vs Policy Mechanism" cover this boundary at the policy level. The 7-row classification table is the durable matrix that operationalizes the policy → **Additional fold needed:** §3 lands in `binding-generator-implementation-notes.md` §12 (Config Surface Evolution) as the durable classification matrix that maps the constitution policy boundary to specific facts and mechanisms. This is one of the most-load-bearing durable artifacts in the iteration-2-spec.
- §4 Chosen Design + §4.1 Schema (full 510-line sample JSON tree with all 5 families populated) + §4.2 Field reference per-family section (16-row table) + §4.3 Field reference global section (3-row table) + §4.4 Derivation rules (9-row table) + §4.5 Clarifications (owner_mode semantics + required_surface order sensitivity + array ordering policy 12-row table + platform defines exact argv strings): **the full `family-config.json` schema 1.0 definition** including all field types/requirements/descriptions + derivation rules (generated output root / family ID string / family detection from output path / family detection from namespace / handles namespace / uniform-opaque `--owner-mode` flag / family-level RSP path / header file include directory / ClangSharp `--file` argument) + ordering-policy table (which arrays are order-sensitive vs not + why) + platform-defines-are-verbatim-argv-strings rule: **DURABLE — kernel covering the schema spec + derivation rules + ordering policy + verbatim-argv discipline**. The schema sample itself is mostly already realized in the live `family-config.json` on disk (per Iteration 2 closure). The **field reference + derivation rules + ordering policy + verbatim-argv discipline** are durable maintenance evidence → **Additional fold needed:** §4.2 (per-family field reference) + §4.3 (global field reference) + §4.4 (derivation rules) + §4.5 (clarifications including array ordering policy + verbatim-argv discipline) land in `canonical/binding-generator-maintenance.md` §3 (Configuration Surface Map) per Q11 maintenance-playbook content outline. The §4.1 sample JSON is illustrative — the live config in `clangsharp/config/family-config.json` is the actual reference. The schema-spec narrative + ordering-policy table + verbatim-argv rule are durable maintenance discipline. Cross-check the maintenance playbook content outline (Q11 §3) confirms: "Configuration Surface Map — `family-config.json` schema with every editable field documented" — confirmed fold target.
- §5 Rejected Alternatives (5.1 Minimal keep-layout / 5.2 Keep separate roster JSONs / 5.3 Flatten roster metadata into string arrays / 5.4 Put clong_type_names in config): rejected-alternatives rationale (documentation cannot fix maintainability bugs / discoverability tax of 6+ files / audit traceability requires full object shape / ABI knowledge must stay in code per Constitution §"Configurable Scope Vs Policy Mechanism"): **DURABLE — kernel codifying why-this-design rationale**. The §5.4 "put clong_type_names in config" rejection (ABI knowledge stays in code) is the key durable rationale that operationalizes Constitution §"Configurable Scope Vs Policy Mechanism" for one specific instance (C long type classification stays in code, not config). **Additional fold needed:** §5 rejected-alternatives rationale lands in `binding-generator-implementation-notes.md` §12 (Config Surface Evolution) as durable design-decision evidence. Particularly §5.4 (clong_type_names rejection) reinforces the manifest-vs-code-owned-policy boundary at the per-fact level.
- §6 File Ownership And Source-of-Truth Rules + §6.1 RSP identity facts (config authoritative for `library_name` + `raw_class`; `--libraryPath` + `--methodClassName` removed from family RSPs; orchestrator derives) + §6.2 Source-of-truth table (13-row config-vs-code source-of-truth matrix) + Rule (if a fact appears in both config and code after migration, config is authoritative and code copy is a bug): **DURABLE — kernel codifying the single-source-of-truth discipline for every config-vs-code fact + the RSP-identity-removal mechanism + the duplicate-fact-is-bug rule**. Constitution §"Manifest Configuration Vs Code-Owned Policy" + §"Configurable Scope Vs Policy Mechanism" cover the discipline at the policy level. The 13-row source-of-truth matrix + duplicate-is-bug rule are durable operationalization → **Additional fold needed:** §6 lands in `binding-generator-implementation-notes.md` §12 (Config Surface Evolution) or `canonical/binding-generator-maintenance.md` §3 (Configuration Surface Map) — the source-of-truth matrix is mechanism-level evidence that operationalizes the constitution policy. The RSP-identity-removal mechanism (orchestrator derives `--methodClassName`/`--libraryPath` from config) lands in maintenance playbook §4 (RSP File Maintenance) per Q11 maintenance content outline.
- §7 Migration Boundaries + §7.1 Files allowed to change + §7.2 Files explicitly forbidden + §7.3 Files to delete after migration is verified: sunset-specific Iteration 2 migration scope (15-row allowed-to-change table; 7-row forbidden table; 5-row files-to-delete-after-parity table): closure-record migration scope → `git log --follow` archive. The forbidden-files list (manifest / vcpkg / dotnet-tools / RSP non-identity content / shims / csproj / .slnx) is durable migration discipline → already covered by Constitution §"Manifest Configuration Vs Code-Owned Policy" + roadmap §"What This Roadmap Does NOT Cover" → no fold needed.
- §8 Verification Gates (§8.1 Determinism / §8.2 Family isolation / §8.3 Parity validation / §8.4 Post-migration evidence): determinism verification (regen idempotency / config-change idempotency / full pin set unchanged) + family isolation verification (targeted-family-only writes / per-family equivalence / config does not break per-family independence) + parity validation (8-assertion sequence: load → header parity → opaque parity → flags parity → required-surface parity → platform views parity → family identities parity → old-files-only-deleted-after-all-parity-asserts-pass) + post-migration evidence (solution build clean / oracle report / AbiTests 792/792 / Slopwatch / self-tests / header-order validation no-dup-order-or-name): closure-record verification scope → `git log --follow` archive. The verification-gate pattern (determinism + family isolation + parity + evidence) is durable cross-iteration migration discipline → already covered by Constitution §"Generation Determinism Contract" + testing-strategy → no fold needed. The header-order validation (no duplicate `order` or duplicate `name` within a family's `headers[]` is hard failure) is durable config schema validation → fold target `canonical/binding-generator-maintenance.md` §3 (Configuration Surface Map) header-order discipline subsection. **Additional fold needed (low-priority):** header-order validation discipline + parity-validation 8-assertion checklist land in maintenance playbook §3 as durable config-schema-validation pattern.
- §9 Exit Criteria (12-criterion list including stale-reference cleanup + constitution updates): closure-record exit gates → `git log --follow` archive. Criterion #12 (Constitution §"Configurable Scope Vs Policy Mechanism" updated; Constitution §"Determinism Inputs" updated to replace old roster JSON paths with `config/family-config.json`) is durable constitution-update evidence → already in constitution per Iteration 2 closure → no fold needed.
- §10 Future Evolution: Spike → Production + §10.1 Why This Design Consolidates Aggressively (ASCII pipeline tree spike-now-to-production-M7) + §10.2 What Retires When The Spike Closes + §10.3 What Survives Into Production + §10.4 Design Principle (spike-local config that mirrors manifest shape is intentional not redundant; M7 migration path = validate spike output → approve manifest schema → delete spike config → enable Cake target reading manifest): **DURABLE — kernel covering (a) spike-to-production transition narrative + (b) what-retires-when-spike-closes table (generate_bindings.py retired / postprocess/Program.cs CLI shell retired / config/family-config.json absorbed into build/manifest.json under expanded schema / scope/.txt already retired by Iteration 2 / policy/.json roster files already retired by Iteration 2 / spike src/ promoted to `src/SDL2.<Family>/` via git mv) + (c) what-survives-into-production table (C# rewriter classes promoted candidates / RSP files unchanged / Generated/{Compat,Modern}/ layout same pattern / Pattern B handle shape + C long policy + [Flags] policy unchanged / oracle promoted as Cake validation target candidate) + (d) design principle (spike-local config mirrors manifest shape is intentional dry-run-for-M7-manifest-absorption)**. This is the durable forward-work narrative for the M7 production flip — it informs the production flip roadmap (post-Layer-2 closure). **Additional fold needed:** §10 lands in `next-iteration-plan.md` Phase 4 restructure (Production Flip layer) per design spec §Q4D forward-milestone restructure + `next-iteration-plan.md` Layer 2 active iteration scope. The spike-to-production transition narrative is durable production-flip evidence — confirm `next-iteration-plan.md` Production Flip subsection captures this transition narrative or fold §10 verbatim.
- §11 References (Constitution sections + roadmap + items + priority-c-closure + next-iteration-plan): cross-reference index → no fold needed (cross-refs absorbed by Phase 1 cross-ref refresh).

**Verdict:** additional fold needed (multiple targets — the iteration-2-spec is the doc with the largest unique-kernel surface among the 5 specs, matching §Q7's second explicit concern). (1) §1 + §1.1 25-source pre-unification config inventory + 4-copy drift surface + 3-risk rationale + iteration-constraint catalog → `binding-generator-implementation-notes.md` §12 (Config Surface Evolution) — the destination spec §Q7 explicitly names for this iteration-2-spec content. (2) §2 + §2.1 + §2.2 comprehensive 25-source absorbed-into-config inventory + 15-source not-moving rationale → `binding-generator-implementation-notes.md` §12 as before-state evidence. (3) §3 7-row config-owns-vs-code-owns classification matrix → `binding-generator-implementation-notes.md` §12 as durable boundary operationalization matrix. (4) §4.2 per-family field reference + §4.3 global field reference + §4.4 derivation rules + §4.5 clarifications (owner_mode semantics + required_surface order sensitivity + 12-row array-ordering policy + verbatim-argv discipline) → `canonical/binding-generator-maintenance.md` §3 (Configuration Surface Map) per Q11. (5) §5 rejected alternatives rationale (especially §5.4 clong_type_names rejection) → `binding-generator-implementation-notes.md` §12. (6) §6 single-source-of-truth discipline + 13-row source-of-truth matrix + duplicate-is-bug rule + RSP-identity-removal mechanism → `binding-generator-implementation-notes.md` §12 + `canonical/binding-generator-maintenance.md` §3 + §4 (RSP File Maintenance). (7) §10 spike-to-production transition narrative + what-retires-vs-what-survives tables + design principle → `next-iteration-plan.md` Phase 4 restructure (Production Flip layer). (8) §8.3 parity-validation 8-assertion checklist + header-order validation discipline → `canonical/binding-generator-maintenance.md` §3 (Configuration Surface Map) header-order subsection and §8 (Drift Detection Between Versions) parity-assertion pattern. This spec has the broadest unique-kernel surface in the items/ folder; all kernels must land in implementation-notes §12 + maintenance playbook §3/§4/§8 + next-iteration-plan Production Flip layer.

### File: spikes/binding-generators/docs/items/item-3-sdl2-gfx-layer-1-spec.md

**Sections audited:**

- §1 Problem Statement: GFX as lowest-risk satellite (102 functions / 4 headers / 1 transparent blittable struct `FPSmanager` / no opaque handles / no C `long` / no callbacks / no unions / no platform-conditioned code; only generation risk = per-header export macros resolved with 4 `--define-macro` insurance entries): closure-record problem framing → `git log --follow` archive; durable family identity facts (102/4/FPSmanager/no-handles/no-C-long/no-callbacks) cross-checked against `sdl2-gfx-header-analysis.md` rows 7-12 (function count + 4-scope-macro table) + rows 123-132 (FPSmanager 20-byte struct) + rows 139-146 (no opaque handles / no satellite-owned handles) + rows 152-164 (ABI risk table). **No reconnaissance gap.** All kernel facts already in satellite analysis → no fold needed.
- §2 Design — §2.1 Family Identity table (config key `"gfx"` / namespace `SDL2.Gfx` / raw class `SDL2_gfxNative` / native library `SDL2_gfx` / managed project `Janset.SDL2.Gfx` / owner mode `false` — consumer): durable family identity → already in `family-config.json` per Iteration 2 closure + already in constitution §"Family Identity" table per Phase 0.1 audit + cross-checked against `sdl2-gfx-header-analysis.md` (header sections + RSP recommendations). **No reconnaissance gap.** → no fold needed.
- §2.2 Header Inventory (4 functional headers ordered: SDL2_framerate.h / SDL2_gfxPrimitives.h / SDL2_imageFilter.h / SDL2_rotozoom.h; excluded SDL2_gfxPrimitives_font.h data-only): durable header inventory → cross-checked against `sdl2-gfx-header-analysis.md` rows 70-117 (per-header function inventories) + row 116 (SDL2_gfxPrimitives_font.h excluded). **No reconnaissance gap.** → no fold needed.
- §2.3 Family RSP (`rsp/sdl2-gfx.rsp` with 4 `--define-macro` scope-macro insurance entries + `--exclude M_PI` cross-platform determinism rule): **DURABLE — kernel covering (a) per-header SDL2_*_SCOPE export-macro insurance mechanism + (b) M_PI exclude rationale (host pre-defines on Windows so ClangSharp never emits; cross-platform determinism requires explicit exclude; BCL provides System.Math.PI)**. Cross-checked against `sdl2-gfx-header-analysis.md` rows 11-59 (export macro analysis with 4 scope macros + preprocessor resolution rationale + insurance strategy with `--define-macro` entries) + rows 181-213 (M_PI conditional definition in both SDL2_gfxPrimitives.h:34-35 and SDL2_rotozoom.h:40-41). **No reconnaissance gap.** All kernel facts already in satellite analysis. The RSP file itself is durable shipped artifact (lives in `rsp/sdl2-gfx.rsp`) → no fold needed.
- §2.4 Config Changes (`families.gfx.headers[]` populated with 4-header ordered list): closure-record config diff → already in live `family-config.json` per Item 3 closure + cross-checked against satellite analysis → no fold needed.
- §2.5 Orchestrator Change (`selected_families("all")` adds `"gfx"`): closure-record orchestrator activation → no fold needed.
- §2.6 C# Project (csproj copy-from-Image pattern with name swaps; Support/DisableRuntimeMarshalling.cs from Image; slnx add): durable per-family-creation pattern → already targeted for `canonical/binding-generator-maintenance.md` §7 (Adding a New SDL Satellite Family checklist) per Q11 → no fold needed.
- §2.7 Postprocess (7-step pipeline GFX-safe; all steps no-op or untouched on GFX surface): closure-record postprocess routing → durable per-family postprocess-routing-table is already targeted for `binding-generator-implementation-notes.md` §13 (Postprocess Pipeline Order) per roadmap audit + per Iteration 1 closure audit → no fold needed.
- §2.8 Oracle (add GFX entry to hardcoded `FamilyConfigs`/`KnownFamilies`; oracle.cs refactor remains deferred per 2026-05-27 Deniz decision): closure-record oracle-extension diff → no fold needed (deferred per Iteration 2 §Q7).
- §3 Non-Goals: closure-record iteration-scoping → no fold needed.
- §4 Verification Gates (§4.1 Determinism / §4.2 Build / §4.3 Oracle / §4.4 AbiTests / §4.5 Slopwatch / §4.6 Self-tests): closure-record exit gates → `git log --follow` archive; durable verification-gate pattern already covered by Constitution §"Generation Determinism Contract" + testing-strategy → no fold needed.
- §5 Exit Criteria: closure-record exit gates → no fold needed.

**Verdict:** ready to git rm — empty unique-info kernel after satellite-reconnaissance cross-check. All durable kernel facts (family identity / header inventory / RSP scope-macro insurance + M_PI exclude / FPSmanager struct / no opaque handles / no C long / no callbacks / no platform-conditioned code) are confirmed already covered by `sdl2-gfx-header-analysis.md` rows 7-12 + 17-59 + 70-117 + 123-146 + 152-164 + 181-213 + RSP-recommendations. **Zero reconnaissance gaps.** Per spec §Q7, this family-specific spec retires entirely with the items/ folder; the satellite analysis (which moves to `canonical/satellites/sdl2-gfx-header-analysis.md` per Q8) is the durable evidence destination.

### File: spikes/binding-generators/docs/items/item-4-sdl2-ttf-layer-1-spec.md

**Sections audited:**

- §1 Problem Statement: TTF as first satellite exercising both major cross-family mechanisms introduced during Item 1 (satellite-owned opaque handle `TTF_Font` + retained C `long` ABI surface `TTF_OpenFontIndex*` + `TTF_FontFaces`); Layer 1 raw ABI only; public typed wrappers/friendly overloads/error redirect helpers/font asset policy/package integration out of scope: closure-record problem framing → `git log --follow` archive.
- §2 Evidence Summary — §2.1 Live Header Audit (12-row evidence table: version 2.24.0 / 88 public `extern...DECLSPEC` declarations / opaque `TTF_Font` typedef at SDL_ttf.h:165 / C long parameters TTF_OpenFontIndex* at :231/:286/:337/:400 / C long return TTF_FontFaces at :689 / 4 non-deprecated no-SDLCALL functions at :2168/:2185/:2204/:2217 / 3 deprecated functions at :2145/:2268/:2291 / error macros TTF_SetError at :2224 + TTF_GetError at :2231 / Core type dependencies SDL_RWops 4 functions + SDL_Surface 32 render returns / no platform-conditioned API or layout): durable evidence catalog with exact source line numbers → cross-checked against `sdl2-ttf-header-analysis.md` rows 11 (88 functions claim) + 30-40 (font opening with 4 C-long params) + 55 (TTF_FontFaces C-long return) + 95-101 (4 non-deprecated no-SDLCALL functions at :2168/:2185/:2204/:2217) + 87-101 (3 deprecated functions including TTF_GetFontKerningSize at :2145 + TTF_SetDirection at :2268 + TTF_SetScript at :2291) + 141-143 (TTF_Font opaque handle Pattern B) + 161-163 (no callback typedefs / no foreign types) + 169-191 (ABI Risk Assessment with 4 risks). **No reconnaissance gap.** All kernel evidence already in satellite analysis.
- §2.2 SDL Wiki Cross-Check (`https://wiki.libsdl.org/SDL2_ttf/TTF_Font` + `TTF_OpenFontIndex` + `TTF_FontFaces`): durable wiki cross-reference → cross-checked against satellite analysis (implicit wiki cross-validation in `opaque-handle-roster.json` family `ttf` `auto_detect_well_known[].wiki_url` entries). **No reconnaissance gap.** → no fold needed (wiki URLs in roster section of `family-config.json` already serve this).
- §2.3 Peer Binding Cross-Check (SDL2-CS `external/sdl2-cs/src/SDL2_ttf.cs`: IntPtr for TTF_Font / C# `long` for `TTF_OpenFontIndex*` ABI-wrong on Windows LLP64 / TTF_FontFaces as `IntPtr` with `IntPtr is actually a C long! This ignores Win64!` inline comment / `TTF_GetFontKerningSizeGlyphs*` with `CallingConvention.Cdecl` / omits TTF_SetFontSDF/TTF_GetFontSDF / implements TTF_GetError/TTF_SetError as C# redirects; ppy/SDL3-CS confirms opaque TTF_Font + explicit Cdecl pattern; alimer covers Core/Image only — no TTF precedent): **DURABLE — kernel covering (a) SDL2-CS C-long-as-C#-long Win64-incorrect anti-pattern + (b) SDL2-CS IntPtr-for-TTF_Font baseline + (c) error macro implemented as C# redirect pattern + (d) ppy/SDL3-CS opaque-TTF_Font validation + (e) alimer-no-TTF-precedent gap**. The C-long-anti-pattern + IntPtr-baseline are durable peer-evidence catalog → already covered by item-1-spec §5.3 audit fold target (`binding-generator-implementation-notes.md` §4 C `long` Hybrid Implementation peer-evidence catalog). The TTF_GetError/TTF_SetError C# redirect pattern + cross-family error-macro-alias-exclusion are already covered by `sdl2-satellite-error-function-consolidation.md` per roadmap audit. **No additional fold needed** beyond what's already targeted via item-1-spec §5.3 + roadmap audit.
- §3 Design — §3.1 Family Identity table (config key `"ttf"` / namespace `SDL2.Ttf` / raw class `SDL_ttfNative` / native library `SDL2_ttf` / managed project `Janset.SDL2.Ttf` / owner mode `true` — TTF_Font satellite-owned): durable family identity → already in `family-config.json` + constitution → no fold needed.
- §3.2 Header Inventory (single header `SDL_ttf.h`; no platform-sensitive headers): durable header inventory → cross-checked against satellite analysis (no platform-conditioned API). **No reconnaissance gap.** → no fold needed.
- §3.3 Family RSP (`rsp/sdl2-ttf.rsp` with `--exclude` block for 2 error macros + 3 deprecated functions): durable family RSP shipped artifact → cross-checked against satellite analysis §6 (RSP Recommendations) + §3 (Risk 2 deprecated + Risk 3 cross-family macro aliases). **No reconnaissance gap.** → no fold needed.
- §3.4 No-SDLCALL Calling Convention Decision (do NOT add `rsp/per-header/SDL_ttf.rsp` initially; rationale = ClangSharp's own tests show normal C functions emit `[DllImport(..., CallingConvention = CallingConvention.Cdecl)]` by default in Compat output; GFX already proved declarations without normal `extern DECLSPEC ... SDLCALL` shape still emit Cdecl in this spike; postprocess `libraryimport` pass converts Modern output to `[UnmanagedCallConv(CallConvs = new[] { typeof(CallConvCdecl) })]` based on existing `[DllImport]` calling convention; mandatory verification = generated Compat output for 4 non-deprecated no-SDLCALL functions must contain `CallingConvention = CallingConvention.Cdecl` + Modern output must contain `CallConvCdecl` after `libraryimport`; if gate fails add per-header RSP using ClangSharp `--with-callconv` as follow-up correction): **DURABLE — kernel covering (a) no-SDLCALL-defaults-to-Cdecl-on-ClangSharp evidence rationale + (b) GFX-as-precedent reasoning + (c) postprocess libraryimport pass converts to UnmanagedCallConv + (d) verify-not-preemptively-add-per-header-RSP discipline + (e) `--with-callconv` correction-mechanism fallback**. This is durable per-header-RSP-discipline mechanism evidence not currently in satellite analysis (the satellite analysis flags the 4 no-SDLCALL functions as "MEDIUM risk" with "explicit bind/exclude decision needed" but does not codify the no-preemptive-RSP-decision rationale). **Additional fold needed (low-priority):** §3.4 no-SDLCALL-defaults-to-Cdecl rationale + verify-not-preemptively-add-per-header-RSP discipline land in `canonical/binding-generator-maintenance.md` §4 (RSP File Maintenance) "when to add an entry" subsection per Q11 maintenance content outline (the maintenance playbook content outline names "type-remap discipline (literal-space `--remap` for libclang byte-exact match)" and "additive-only semantics for keyed entries" — add a sibling "verify-not-preemptively-add-per-header-RSP discipline" subsection codifying the GFX-precedent + ClangSharp-default + libraryimport-conversion rationale that prevents speculative per-header RSP additions).
- §3.5 Opaque Handle Policy (`TTF_Font` satellite-owned Pattern B; owner mode emits `Generated/{Compat,Modern}/Handles.g.cs` in namespace `SDL2.Ttf` with `public readonly partial struct TTF_Font`; Core handles SDL_RWops + SDL_Surface + SDL_Color + SDL_version + SDL_bool consumed through ProjectReference; do not add TTF_Font to Core; do not emit TTF_Font in consumer projects): durable opaque-handle policy + Pattern-B-by-value cross-family resolution → already in constitution §"Opaque Handles" Cross-family handle name resolution per Item 1 S1-0a commit + targeted for implementation-notes §3 per item-1-spec §5.2 audit. **No additional fold needed.**
- §3.6 C `long` Policy (existing `clong-dispatch` postprocess applies because `families.ttf.clong_methods[]` already populated; expected Modern output uses `CLong`; Compat uses managed dispatcher + private Win32/Unix64 `[DllImport]` helpers; no `ClongDualDispatchRewriter` code change expected): durable C-long-applies-to-TTF-via-existing-rewriter → already in constitution §"C `long` And `unsigned long`" + targeted for implementation-notes §4 per item-1-spec §5.3 audit. **No additional fold needed.**
- §3.7 C# Project pattern (multi-target 5 TFMs / AllowUnsafeBlocks / EnableDefaultCompileItems false / RootNamespace SDL2 / ProjectReference to Core / TFM split Compat/Modern / Support/DisableRuntimeMarshalling.cs + Support/NativeTypeNameAttribute.cs — required because attribute is internal per assembly; GFX proved every satellite needs local copy): durable per-family-creation pattern → already targeted for `canonical/binding-generator-maintenance.md` §7 (Adding a New SDL Satellite Family checklist) per Q11. The `NativeTypeNameAttribute.cs` per-assembly-internal-copy rule is durable per-family-creation discipline → fold target maintenance playbook §7. **Additional fold needed (low-priority):** confirm maintenance playbook §7 checklist explicitly lists Support/NativeTypeNameAttribute.cs as required per-satellite copy.
- §3.8 Orchestrator Activation (`selected_families("all")` adds `"ttf"`; update Python self-test expectation): closure-record orchestrator-activation diff → no fold needed.
- §3.9 Solution Wiring (add csproj to `.slnx` under `/src/` after GFX): closure-record solution wiring → no fold needed.
- §3.10 Oracle (`FamilyConfigs.Sdl2Ttf` already present; KnownFamilies includes TTF; minimal data/path correction only; do not refactor): closure-record oracle-verification scope → no fold needed.
- §3.11 Peer Visual Checks (SDL2-CS comparison + Silk.NET fallback policy): closure-record peer-visual-check workflow → no fold needed (audit workflow durable in `canonical/binding-output-oracle-validation.md`).
- §3.12 Tests and Runtime Smoke (Item 4's first-level gates = generation+build+oracle+self-tests; runtime ABI smoke needs redistributable font asset + native SDL2_ttf dependency copy strategy — deferred; minimum runtime smoke candidates for follow-up = `TTF_Init`/`TTF_WasInit`/`TTF_Quit` lifecycle / `TTF_OpenFont` + `TTF_CloseFont` with small redistributable `.ttf` test asset / `TTF_OpenFontIndex(..., 0)` parameter-position C `long` smoke / `TTF_FontFaces` return-position C `long` smoke): **DURABLE — kernel covering (a) Layer 1 closure does NOT require runtime ABI smoke + (b) 4-candidate runtime smoke catalog for follow-up + (c) redistributable-font-asset + native-dependency-copy strategy as runtime smoke prerequisite**. The 4-candidate runtime smoke catalog is durable forward-work item → already targeted for `next-iteration-plan.md` Layer 2/Layer 3 follow-ups per roadmap audit. **No additional fold needed** beyond confirming next-iteration-plan includes TTF runtime smoke candidates in forward backlog.
- §4 Non-Goals + §5 Verification Gates + §6 Exit Criteria: closure-record iteration scoping → `git log --follow` archive; durable verification-gate pattern already covered → no fold needed.

**Verdict:** additional fold needed (one low-priority target). §3.4 no-SDLCALL-defaults-to-Cdecl rationale + verify-not-preemptively-add-per-header-RSP discipline → `canonical/binding-generator-maintenance.md` §4 (RSP File Maintenance) "when to add an entry" subsection per Q11. Otherwise: all durable kernel facts (88 functions / single header / 5 C long surface / no-SDLCALL functions / TTF_Font satellite-owned / SDL2-CS anti-pattern / no callbacks / no foreign types) are confirmed already covered by `sdl2-ttf-header-analysis.md` rows 11/30-40/55/87-101/95-101/141-143/161-163/169-191. **Zero major reconnaissance gaps.** Per spec §Q7, this family-specific spec retires entirely with the items/ folder; the satellite analysis (which moves to `canonical/satellites/sdl2-ttf-header-analysis.md` per Q8) is the durable evidence destination.

### File: spikes/binding-generators/docs/items/item-5-sdl2-mixer-layer-1-spec.md

**Sections audited:**

- §1 Problem Statement: Mixer as highest-risk satellite in this expansion wave because it introduces callback typedefs and callback-accepting APIs that can outlive the call that registers them; Layer 1 raw ABI only; Item 5 must prove generated Mixer signatures ABI-correct + compile across spike TFM matrix + do not regress existing active families; public typed wrappers/friendly overloads/callback lifetime/rooting helpers/audio-device runtime smoke/package integration/production source promotion out of scope: closure-record problem framing → `git log --follow` archive.
- §2 Evidence Summary — §2.1 Live Header Audit (13-row evidence table: version 2.8.1 / 97 public `extern DECLSPEC ... SDLCALL` declarations / **all functions use SDLCALL no no-SDLCALL anomaly like TTF** / opaque `Mix_Music` typedef at SDL_mixer.h:269 / transparent struct `Mix_Chunk` 4-field POD at :231-236 / enums `MIX_InitFlags` bitmask at :109-118 + `Mix_Fading` at :241-245 + `Mix_MusicType` at :250-264 / 6 callback typedefs Mix_MixCallback + Mix_MusicFinishedCallback + Mix_ChannelFinishedCallback + Mix_EffectFunc_t + Mix_EffectDone_t + Mix_EachSoundFontCallback / 7 callback-typed functions / 8 callback parameters Mix_SetPostMix + Mix_HookMusic + Mix_HookMusicFinished + Mix_ChannelFinished + Mix_RegisterEffect 2 callback params + Mix_UnregisterEffect + Mix_EachSoundFont / **callback lifetime risk** documented in header at :1127-1130/:1168-1171/:1210-1221/:1247-1257 — callbacks fire inside SDL_mixer audio callbacks or arbitrary background thread contexts / error macros Mix_SetError + Mix_GetError + Mix_ClearError + Mix_OutOfMemory at :2803-2822 / legacy aliases MIX_MAJOR_VERSION + MIX_MINOR_VERSION + MIX_PATCHLEVEL + MIX_VERSION at :63-66 / object-like value macros MIX_CHANNELS + MIX_DEFAULT_FREQUENCY + MIX_DEFAULT_FORMAT + MIX_DEFAULT_CHANNELS + MIX_MAX_VOLUME + MIX_CHANNEL_POST + MIX_EFFECTSMAXSPEED / function-like version macros not Layer 1 raw ABI / **zero C long, wchar_t, variadics, unions, platform-conditioned API found**): durable evidence catalog with exact source line numbers → cross-checked against `sdl2-mixer-header-analysis.md` rows 11-12 (97 functions claim with all SDLCALL) + 17-22 (callback-risk + double-return + double-param + const-char* + SDL_bool tables) + 60-65 (Mix_Music opaque handle) + 70-92 (Mix_Chunk 4-field POD with offset/size table) + 96-100 (MIX_InitFlags + Mix_Fading + Mix_MusicType enums) + 102-113 (6 callback typedefs with full SDLCALL signatures) + 125-149 (ABI Risk Assessment with 5 risks). **No reconnaissance gap.** All kernel evidence already in satellite analysis. The callback-lifetime-risk header line citations (:1127-1130/:1168-1171/:1210-1221/:1247-1257) are durable evidence not currently in satellite analysis explicitly → **Additional fold consideration:** the 4 specific header line numbers for callback-lifetime-risk could land in `canonical/satellites/sdl2-mixer-header-analysis.md` §3 (ABI Risk Assessment Risk 1) as durable evidence anchor — currently the satellite analysis says "header documents audio-thread callback constraints" without citing the 4 specific line ranges. **Additional fold needed (low-priority):** add the 4 callback-lifetime-risk header line ranges (:1127-1130/:1168-1171/:1210-1221/:1247-1257) to satellite analysis §3 Risk 1.
- §2.2 SDL Wiki Cross-Check (`https://wiki.libsdl.org/SDL2_mixer/Mix_Music` + `Mix_Chunk` + `Mix_RegisterEffect`): durable wiki cross-reference → already covered by `family-config.json` `families.mixer.opaque_handles.auto_detect_well_known[]` wiki_url entries → no fold needed.
- §2.3 Peer Binding Cross-Check (SDL2-CS IntPtr comments for `Mix_Music*` and `Mix_Chunk*` / Mix_Chunk as managed struct with IntPtr abuf + uint alen + byte volume / `[Flags]` on MIX_InitFlags but constant roster older than 2.8.1 omits MIX_INIT_WAVPACK / `[UnmanagedFunctionPointer(CallingConvention.Cdecl)]` delegates with SDL2-CS-specific names MixFuncDelegate/MusicFinishedDelegate/ChannelFinishedDelegate/SoundFontDelegate / Mix_GetError/Mix_SetError/Mix_ClearError as C# redirects + does not expose Mix_OutOfMemory / convenience string wrappers + manual SDL_MIXER_VERSION(out SDL_version) helper belongs to higher layers; ppy/SDL3-CS provides current ClangSharp callback evidence but not SDL2_mixer API compatibility — SDL3_mixer is substantially different API; ppy generates `delegate* unmanaged[Cdecl]<...>` for callback-accepting functions in Modern path): **DURABLE — kernel covering (a) SDL2-CS IntPtr-comment baseline + (b) SDL2-CS stale-constant-roster gap (omits WAVPACK) + (c) SDL2-CS specific-delegate-name convention + (d) ppy/SDL3-CS `delegate* unmanaged[Cdecl]` callback-shape evidence for Modern path**. The ppy callback-shape evidence is durable peer-evidence for callback-typedef generation → fold target `binding-generator-implementation-notes.md` §3 (Opaque Handles Implementation Mechanism) or a new §"Callback Typedef Generation" subsection — currently implementation-notes content outline doesn't include callback-typedef-generation mechanism explicitly. **Additional fold needed (low-priority):** ppy/SDL3-CS callback-shape evidence (`delegate* unmanaged[Cdecl]<...>` Modern + `[UnmanagedFunctionPointer(CallingConvention.Cdecl)]` Compat) lands in `binding-generator-implementation-notes.md` as a callback-typedef-generation-mechanism subsection (new — not currently in content outline §§1-13). Currently the §1-13 outline doesn't have an explicit callback-typedef section; callbacks are mentioned as a foreign-type-boundary concern in §7 (Foreign Type Boundary Implementation) but not as their own mechanism — this is a content-outline gap for the implementation-notes design spec to consider.
- §3 Design — §3.1 Family Identity (config key `"mixer"` / namespace `SDL2.Mixer` / raw class `SDL_mixerNative` / native library `SDL2_mixer` / managed project `Janset.SDL2.Mixer` / owner mode `true` — Mix_Music satellite-owned): durable family identity → already in `family-config.json` + constitution → no fold needed.
- §3.2 Header Inventory (single header `SDL_mixer.h`; no platform-sensitive headers): durable → cross-checked → no fold needed.
- §3.3 Family RSP (`rsp/sdl2-mixer.rsp` with `--exclude` block for 4 error macros Mix_SetError/Mix_GetError/Mix_ClearError/Mix_OutOfMemory; **do not create `rsp/per-header/SDL_mixer.rsp` initially** — header has no foreign types/platform-conditioned/C long/wchar_t/variadics/callconv anomaly; add per-header RSP only if generated output proves concrete correctness gap): durable family-RSP-discipline → already covered by item-4-spec §3.4 audit fold target (maintenance playbook §4 verify-not-preemptively-add-per-header-RSP discipline) → no additional fold needed.
- §3.4 Opaque Handle Policy (`Mix_Music` satellite-owned Pattern B; owner mode emits `Generated/{Compat,Modern}/Handles.g.cs` in namespace `SDL2.Mixer` with `public readonly partial struct Mix_Music`; Core handles SDL_RWops + SDL_version + SDL_bool + Uint8/Uint16/Uint32/Sint16 + audio constants consumed through ProjectReference; do not add Mix_Music to Core; do not force-opaque Mix_Chunk — public transparent Mixer-owned data struct not opaque handle): durable opaque-handle policy + Pattern-B-by-value cross-family resolution + force-opaque-exception-for-transparent-struct-distinguished discipline → already in constitution + targeted for implementation-notes §3 per item-1-spec §5.2 audit. **No additional fold needed.**
- §3.5 `Mix_Chunk` Struct Policy (4-field POD: allocated int / abuf `Uint8*` → `byte*` / alen Uint32 → uint / volume Uint8 → byte; expected layout sequential with pointer-size-dependent padding 12 bytes x86 + 24 bytes x64; generated output must not treat Mix_Chunk as opaque + must not emit union or explicit-layout lie): durable transparent-struct policy + opaque-vs-transparent-distinction → cross-checked against `sdl2-mixer-header-analysis.md` rows 70-92 (Mix_Chunk transparent POD with offset/size table). **No reconnaissance gap.** → no fold needed.
- §3.6 Callback Policy (6 callback typedefs with full SDLCALL signatures; expected Layer 1 output Compat = delegate declarations with `[UnmanagedFunctionPointer(CallingConvention.Cdecl)]` + Modern = `delegate* unmanaged[Cdecl]<...>`; `void*` userdata remains pointer-shaped; no lifetime/rooting helper in Layer 1; 7 callback-consuming functions for manual verification including Mix_RegisterEffect with 2 callback params + Mix_UnregisterEffect/Mix_EachSoundFont; **Mix_UnregisterAllEffects has no callback-typed parameter** and is not part of callback signature gate): **DURABLE — kernel covering (a) Compat vs Modern callback emit shape (delegate vs `delegate*`) + (b) 7-function manual-verification list with explicit Mix_UnregisterAllEffects-not-in-gate carve-out + (c) `void*`-userdata-stays-pointer-shaped rule + (d) Layer-1-does-not-add-rooting-helpers discipline**. Cross-checked against `sdl2-mixer-header-analysis.md` rows 102-113 (6 callback typedefs table) + row 17 (7 callback-typed functions with 8 callback params citing Mix_RegisterEffect 2-callback at :1367). **No reconnaissance gap on the kernel facts.** The (a) Compat/Modern callback emit shape + (c) `void*`-userdata-rule + (d) no-Layer-1-rooting discipline are durable callback-typedef generation mechanism → fold target `binding-generator-implementation-notes.md` callback-typedef-generation-mechanism subsection (per §2.3 audit additional-fold-needed item — content outline gap). **Additional fold needed (low-priority):** callback emit shape + `void*`-userdata rule + no-Layer-1-rooting discipline land in implementation-notes (new callback subsection per §2.3 fold) or distributed across §3 (opaque handles) + §10 (classification labels) + §13 (postprocess pipeline) per the existing content outline. The Mix_UnregisterAllEffects-carve-out is sunset-specific Item 5 verification detail → closure-record archive.
- §3.7 Callback Lifetime Deferral (Mixer callbacks can persist beyond registration call + may fire from SDL_mixer's audio callback or background thread; **real managed lifetime/rooting problem but not Layer 1 raw ABI generation problem**; deferred follow-up work tracked in roadmap §Deferred Follow-up Mixer Callback Lifetime Smoke + next-iteration-plan §Layer 2/Layer 3 Follow-ups; deferred policy must later define = Compat delegate rooting for full native registration lifetime + Modern `[UnmanagedCallersOnly]` / `delegate* unmanaged[Cdecl]` authoring rules + dummy-audio deterministic runtime smoke setup + cleanup/unregistration rules for persistent callbacks; Layer 1 closure must NOT add callback-rooting abstractions/helper classes/audio runtime smoke unless Deniz explicitly expands scope): **DURABLE — kernel covering (a) Layer-1-callback-generation-vs-Layer-2-callback-lifetime distinction + (b) 4-element deferred-policy catalog (Compat delegate rooting / Modern UnmanagedCallersOnly rules / dummy-audio runtime smoke / cleanup-unregistration rules) + (c) scope-discipline = Layer 1 does NOT preemptively add lifetime helpers**. The deferred follow-up is durable forward-work → already targeted for `next-iteration-plan.md` Layer 2/Layer 3 follow-ups per roadmap audit. The 4-element deferred-policy catalog is durable Layer-2-design-input → **Additional fold needed (low-priority):** confirm next-iteration-plan.md Mixer callback lifetime follow-up backlog includes all 4 deferred-policy elements (delegate rooting / UnmanagedCallersOnly rules / dummy-audio setup / cleanup-unregistration rules).
- §3.8 Constants, Enums, and Macros (MIX_InitFlags `[Flags]` via existing `flags-detect` suffix rule; Mix_Fading and Mix_MusicType must NOT receive `[Flags]`; object-like value macros to keep + version macros + legacy aliases matching TTF precedent + Image's SDL_MIXER_COMPILEDVERSION precedent; function-like macros SDL_MIXER_VERSION(X)/MIX_VERSION(X)/SDL_MIXER_VERSION_ATLEAST(X,Y,Z) NOT Layer 1 raw ABI declarations; if absent from generated output expected; generation report should classify skipped function-like macros as helper candidates rather than silently treating as generated API): durable enum-decoration discipline + value-macro-vs-function-macro distinction + report-classification-discipline → MIX_InitFlags `[Flags]` already covered by constitution §"Enums" + item-1-spec §5.4 audit fold target (implementation-notes §8); value-macro-vs-function-macro distinction is durable per-family-creation discipline → fold target maintenance playbook §7 (Adding a New SDL Satellite Family) per Q11. **Additional fold needed (low-priority):** value-macro-vs-function-macro-and-report-classification-discipline lands in maintenance playbook §7 as part of new-family addition checklist.
- §3.9 C# Project (same as TTF pattern + Support/NativeTypeNameAttribute.cs required because attribute is internal per assembly): durable per-family-creation pattern → already targeted for maintenance playbook §7 per item-4-spec §3.7 audit → no additional fold needed.
- §3.10 Orchestrator Activation (`selected_families("all")` adds `"mixer"`; update Python self-test expectation; do not activate SDL2_net or SDL3): closure-record orchestrator-activation diff → no fold needed.
- §3.11 Solution Wiring (add csproj to `.slnx` after TTF): closure-record solution wiring → no fold needed.
- §3.12 Oracle (oracle.cs already accepts sdl2-mixer in 5-family command path; minimal data/path/evidence correction only; expect possible evidence-tool gaps around callback typedef inspection before assuming generated ABI is wrong; do not refactor): durable evidence-tool-gap-discipline ("expect possible evidence-tool gaps around callback typedef inspection") is durable oracle-validation discipline → fold target `canonical/binding-output-oracle-validation.md` playbook (Phase 1 destination per Q11). **Additional fold needed (low-priority):** evidence-tool-gap-around-callback-typedef-inspection discipline lands in oracle-validation playbook as a section on callback-typedef-evidence-limitations and expected-evidence-coverage-gaps.
- §3.13 Peer Visual Checks (SDL2-CS divergence catalog + ppy/SDL3-CS callback-shape-evidence-only-not-SDL2-API-oracle carve-out + Silk.NET fallback per Item 4 finding): closure-record peer-visual-check workflow → durable peer-visual-check-discipline already covered → no fold needed.
- §3.14 Tests and Runtime Smoke (Layer 1 first-level gates = generation+ABI-shape-inspection+build+oracle+self-tests+determinism+slopwatch; runtime callback/audio smoke valuable but deferred until callback lifetime/rooting policy + dummy-audio setup + audio fixture policy designed; 4 deferred runtime smoke candidates = Mix_OpenAudio/Mix_CloseAudio lifecycle under dummy audio + Mix_LoadWAV/Mix_FreeChunk with tiny deterministic WAV fixture + Mix_SetPostMix or Mix_RegisterEffect callback roundtrip after rooting policy exists + Mix_EachSoundFont callback shape smoke if deterministic test data available without host MIDI assumptions): durable forward-work runtime-smoke catalog → already targeted for next-iteration-plan.md Layer 2/Layer 3 follow-ups per §3.7 audit → no additional fold needed.
- §4 Non-Goals + §5 Verification Gates + §6 Exit Criteria: closure-record iteration scoping → durable verification-gate pattern already covered → no fold needed.

**Verdict:** additional fold needed (multiple low-priority targets). (1) §2.1 callback-lifetime-risk header line citations (:1127-1130/:1168-1171/:1210-1221/:1247-1257) → `canonical/satellites/sdl2-mixer-header-analysis.md` §3 (ABI Risk Assessment Risk 1) as durable evidence anchor. (2) §2.3 + §3.6 ppy/SDL3-CS callback-shape evidence (`delegate* unmanaged[Cdecl]<...>` Modern + `[UnmanagedFunctionPointer(CallingConvention.Cdecl)]` Compat + void\*-userdata-pointer-shape rule) → `binding-generator-implementation-notes.md` callback-typedef-generation-mechanism subsection (new — content outline gap; not currently in §§1-13 explicitly). (3) §3.7 Mixer callback lifetime deferral 4-element policy catalog (delegate rooting / UnmanagedCallersOnly rules / dummy-audio setup / cleanup-unregistration rules) → confirm presence in `next-iteration-plan.md` Layer 2/Layer 3 follow-ups. (4) §3.8 value-macro-vs-function-macro distinction + report-classification-discipline → `canonical/binding-generator-maintenance.md` §7 (Adding a New SDL Satellite Family) checklist. (5) §3.12 evidence-tool-gap-around-callback-typedef-inspection discipline → `canonical/binding-output-oracle-validation.md` playbook (Phase 1 destination per Q11) as callback-typedef-evidence-limitations subsection. Otherwise: all durable kernel facts (97 functions all SDLCALL / Mix_Music opaque / Mix_Chunk transparent POD / 6 callbacks / MIX_InitFlags bitmask / no C long / no wchar_t / no variadics / no unions / no platform-conditioned) are confirmed already covered by `sdl2-mixer-header-analysis.md` rows 11-12/17-22/60-65/70-92/96-100/102-113/125-149. **One minor reconnaissance gap** (callback-lifetime-risk header line ranges not in satellite analysis — fold target #1 above). Per spec §Q7, this family-specific spec retires entirely with the items/ folder; the satellite analysis (which moves to `canonical/satellites/sdl2-mixer-header-analysis.md` per Q8) is the durable evidence destination, with the §2.1 line-range fold landing first.

### File: spikes/binding-generators/docs/testing/raw-abi-current-inventory.md

**Sections audited:**

- "Baseline Verification" block (2026-05-25 snapshot: `total: 712` / `failed: 0` / `succeeded: 712` / `skipped: 0` / executable TFMs `net10.0`+`net9.0`+`net8.0`+`net462` / `178` unique tests per executable TFM / explicit `net462` 178/178 passed): point-in-time test counts that volatilize every test run — drop entirely per spec §Q9 fold guidance ("point-in-time test counts ... DROP").
- "Current Files" 13-row table mapping each existing test file to discovered-tests-per-TFM count + current role + next action (`ThreadIdAbiTests.cs` 2 host/thread smoke / `RectAbiTests.cs` 18 deterministic integer-float rect ports / `GuidAbiTests.cs` 11 GUID parse-string with raw byte-order / `TimerAbiTests.cs` 2 performance counter smoke / `PlatformAbiTests.cs` 8 platform-version-revision-error ports with `SDL_VERSION` macro deferred + endian-swap + CPU feature + power-info + `SDL_GetErrorMsg` + `SDL_VERSIONNUM`/`SDL_VERSION_ATLEAST` helper macros all deferred / `PixelsAbiTests.cs` 89 pixel format names + palette allocation + gamma ramp ports / `RwopsAbiTests.cs` 11 memory-file-const-memory-endian-alloc RWops ports with `SDL_RWFromFP` deferred / `SurfaceAbiTests.cs` 9 BMP save-reload + missing-load failure + conversion-header smoke + overflow-pitch + blend-none header smoke with upstream fixture-comparison + disabled blend modes + 32-bit-only overflow path deferred / `WavAbiTests.cs` 6 WAV load-free + `SDL_BuildAudioCVT` + `SDL_ConvertAudio` narrow smoke + same-format `SDL_AudioStream` smoke with real-device loop + queued-device + disabled randomized convert sweep + signal-to-noise resample-loss deferred / `HintsAbiTests.cs` 3 custom hint round-trip + upstream env-default-override-reset + hint callback reset-delete with full `_HintsEnum` sweep deferred / `EventsAbiTests.cs` 3 event queue + NULL-userdata watch + userdata watch with delete verification / `AudioAbiTests.cs` 5 dummy audio init-quit + open-close + global status + device status + lock-unlock with callback pause-unpause timing + conversion-resample deferred / `VideoAbiTests.cs` 6 dummy video window lifecycle + flags + ID lookup + pixel format + size + window-surface-renderer with display-mode + brightness-gamma + position + min-max-size + window-data + centered-on-display + syswm + WM-event-sensitive deferred / `RenderAbiTests.cs` 5 dummy-software render driver enumeration + lifecycle-clear + primitive draw + texture query-copy + texture color modulation with upstream image-comparison blit + disabled blend-alpha suites deferred): point-in-time per-TFM test-count enumeration → drop counts (volatile per spec §Q9), but the **deferred-coverage notes** (per-file "Next action" deferred lists: SDL_RWFromFP / upstream fixture-comparison blit / disabled blend modes / 32-bit-only overflow / real-device loop / queued-device playback / signal-to-noise resample-loss / `SDL_VERSION` macro port / endian-swap / CPU feature probes / power-info / `SDL_GetErrorMsg` / `SDL_VERSIONNUM`/`SDL_VERSION_ATLEAST` helper macros / full `_HintsEnum` sweep / callback pause-unpause timing / display-mode / brightness-gamma / window-data / centered-on-display / syswm / upstream image-comparison blit / disabled blend-alpha) are durable forward-work catalogs → fold target `canonical/testing-strategy.md` "Generated Raw ABI Runtime Tests" deferred-coverage notes per spec §Q9.
- "Current Infrastructure" 17-row table cataloguing infrastructure files + responsibilities + next-action ("Added during normalization." / "Keep." / "Delegates to `SdlRuntimeProbe`."): closure-record infrastructure inventory for the spike-local AbiTests project → durable architectural facts (AbiCategories central constants / AbiParallelKeys keyed non-parallel groups / UpstreamSdlTestAttribute traceability / SdlAssert SDL return-bool assertion / SdlError `SDL_GetError`/`SDL_ClearError` / SdlUtf8 UTF-8 pin-decode / AbiTempDirectory per-test temp dir / UpstreamSdlAsset copied asset lookup / SdlSubsystemScope subsystem init-quit / SdlEnvironmentScope env-var restore / SdlHintScope hint restore / SdlRuntimeProbe runtime facts + driver discovery / RequiresVideoDummyDriverAttribute + RequiresAudioDummyDriverAttribute TUnit skips / SdlCallbackBridge.Modern + .Compat event-watch callback helpers / SdlMacro test-local function-like SDL macros) are already enumerated in `raw-abi-upstream-testing-spec.md` §"Infrastructure First" + §"Test Project Layout" tree → duplicate of testing-spec section folding into `canonical/testing-strategy.md` infrastructure layer-model section → no additional fold needed beyond the testing-spec fold.
- "Known Normalization Work" 6-bullet status report (AbiCategories replaces literals / AbiParallelKeys replaces broad NotInParallel / SdlRuntimeProbe centralizes / callback bridges isolate Modern-vs-Compat / SdlMacro only for actually-used macros / hints+events moved to `Upstream/GlobalState`): point-in-time normalization status — drop (sunset normalization completion record, not durable principle).
- "Known Generated Binding Blockers" 4-bullet list (1) `SDL_syswm` upstream automation deferred until `SDL_SysWMinfo`/`SDL_SysWMmsg` typed union generation + `SDL_GetWindowWMInfo` available; (2) thread creation runtime tests deferred until `SDL_CreateThread` macro/REAL entrypoint handling intentionally supported; (3) function-like SDL macros need generated-or-test-local helper policy before direct upstream ports are clean; (4) `SDL_GetErrorMsg` generated in raw ABI but local SDL2 upstream test sources do not exercise it — keep deferred rather than inventing behavior outside upstream evidence): **DURABLE — kernel covering 4 specific generated-binding-test-coverage blockers that survive any toolchain swap**. Per spec §Q9 these fold into testing-strategy "Generated Raw ABI Runtime Tests" deferred-coverage notes → fold target `canonical/testing-strategy.md` "Generated Raw ABI Runtime Tests" deferred-coverage subsection with the 4 specific blockers (SDL_syswm typed-union dependency / SDL_CreateThread macro+REAL entrypoint dependency / function-like SDL macro helper policy / SDL_GetErrorMsg-not-exercised-by-upstream discipline) as durable forward-work anchors.

**Verdict:** ready to git rm after fold (fold targets: (1) per-file deferred-coverage catalogs from "Current Files" table → `canonical/testing-strategy.md` "Generated Raw ABI Runtime Tests" deferred-coverage notes; (2) "Known Generated Binding Blockers" 4-bullet list → same destination as a durable forward-work anchor). Baseline-verification counts (712/178/TFM as of 2026-05-25) + per-TFM discovered-test counts + "Known Normalization Work" 6-bullet status all DROP per spec §Q9 volatility guidance. Per spec §Q9, this file retires entirely after the deferred-coverage notes + binding-blocker list fold into `canonical/testing-strategy.md` during Phase 2.3.

### File: spikes/binding-generators/docs/testing/raw-abi-upstream-testing-spec.md

**Sections audited:**

- "Purpose" paragraph (expand ClangSharp spike's AbiTests from small smoke into high-coverage SDL2 raw ABI conformance suite + validate Layer 1 SDLNative against real Janset.SDL2.Core.Native payloads + use upstream SDL2 automation tests as primary behavioral oracle but not blind SDL_test clone + port most deterministic+headless-safe+managed-binding-meaningful cases + add Janset-owned header-based tests where upstream coverage is thin + spike-local under `spikes/binding-generators/clangsharp/` until generator promotion): **DURABLE — kernel covering (a) upstream-SDL_test-as-behavioral-oracle-not-blind-clone + (b) port-criterion = deterministic + headless-safe + managed-binding-meaningful + (c) Janset-owned-header-tests-supplement-where-upstream-thin**. Fold target `canonical/testing-strategy.md` "Generated Raw ABI Runtime Tests" purpose/scope subsection per spec §Q9.
- "Hard Boundaries" 8-bullet list (one test project during spike `AbiTests.csproj` / no separate test projects per subsystem-stage-platform-manual / no move to root `tests/` during spike / no managed `Janset.SDL2.Core` package consumption — keep `ProjectReference` to Core csproj / native library resolved through `Janset.SDL2.Core.Native` local feed / `spikes/binding-generators/references/SDL` is gitignored reference clone not runtime dependency / copy only selected stable assets with provenance + license / manual diagnostics out of scope no `Manual/` + no `RequiresManualDiagnosticsAttribute` + no real-window visual diagnostics + no real speaker playback + no haptic + no GL/GLES/Vulkan + no hardware-input / broad subsystem porting starts only after reusable infrastructure stable): **DURABLE — kernel covering one-spike-project-boundary + Janset.SDL2.Core.Native-resolution + gitignored-reference-clone-policy + license-stamped-copy-of-stable-assets + manual-diagnostics-out-of-scope-discipline + infrastructure-first-before-broad-porting**. Fold target `canonical/testing-strategy.md` ground-rules section per spec §Q9.
- "Ground Rules → Headless-Safe Default": **default `dotnet test` runs only automated headless-safe tests + 6-bullet allowed-default-safe class catalog (pure scalar-struct-string / asset-backed file with committed tiny fixtures / temp-file workflows isolated under per-test temp dirs / dummy video-audio driver workflows with skip-attribute proof / software renderer on dummy video when validated stable / capability-gated with clear TUnit skips) + 7-bullet deferred-non-default classes catalog (real display-focus-gamma-brightness / hardware joystick-gamecontroller-haptic-sensor-hotplug / real speaker playback / GL/GLES/Vulkan/Metal diagnostics / `SDL_syswm` until typed-union generation / `SDL_CreateThread` family until thread-creation macro-REAL handling / OS clipboard + primary-selection unless capability-gated and OS-family-proven)**: DURABLE — kernel covering default-safe-vs-deferred classification taxonomy. Fold target `canonical/testing-strategy.md` ground-rules section per spec §Q9.
- "Ground Rules → Upstream Adoption" (use upstream SDL2 tests as behavior oracles + traceability anchors + port most `testautomation_*.c` cases satisfying headless-safe rule + every upstream-derived test carries `[UpstreamSdlTest("test/testautomation_rect.c", "rect_testIntersectRect")]` metadata + C# method name stays idiomatic describes behavior + metadata preserves upstream source without forcing C naming): **DURABLE — kernel covering UpstreamSdlTestAttribute traceability convention + idiomatic-C#-name + C-source-preserved-in-metadata pattern**. Fold target `canonical/testing-strategy.md` ground-rules section per spec §Q9.
- "Ground Rules → Janset Header-Based Coverage" 8-bullet list (exported symbol presence vs generated declarations / struct size + field offset + explicit-layout union shape + opaque-handle policy / enum + flag + macro constant values / function-like SDL macros that require C# helper equivalents / UTF-8 span macro constants / callback signatures + lifetime bridging across Modern+Compat TFMs / platform-width-sensitive types `size_t`/`Sint64`/`nint`/`CLong`/`CULong` + mechanical full-surface guardrails should eventually cover generated raw surface more broadly than hand-authored runtime tests + runtime tests prove representative behavior + high-risk ABI paths + mechanical checks prove breadth): **DURABLE — kernel covering Janset-owned-test-categories-supplementing-upstream + mechanical-breadth-vs-runtime-representative-depth distinction**. Fold target `canonical/testing-strategy.md` ground-rules section per spec §Q9.
- "Ground Rules → Infrastructure First" 9-bullet must-stabilize-before-broad-port catalog (AbiCategories constants / AbiParallelKeys constants / SdlAssert+SdlError+SdlUtf8 helpers / AbiTempDirectory + asset catalog / SdlSubsystemScope+SdlEnvironmentScope+SdlHintScope / SdlRuntimeProbe for native runtime facts + driver/capability discovery / reusable skip attributes for dummy video + dummy audio + OS/platform + capability + backend-sensitive / callback lifetime helpers bridging Modern `delegate* unmanaged` and Compat `IntPtr` delegate / small C# equivalents for stable SDL function-like macros needed by ports): **DURABLE — kernel covering reusable-infrastructure-as-prerequisite-for-broad-porting + 9 specific named infrastructure-collaborator requirements**. Fold target `canonical/testing-strategy.md` ground-rules section per spec §Q9.
- "Ground Rules → TUnit And Parallelism" 6-bullet list (pure ABI tests may run parallel / tests mutating SDL process-global state must use keyed non-parallel groups `SDL.Global`/`SDL.Video`/`SDL.Audio`/`SDL.Events`/`SDL.Hints`/`SDL.Error` / do not make whole assembly non-parallel — would hide ordering bugs + slow suite / prefer reusable skip attributes over inline early returns so skipped capabilities visible in reports / use data-driven TUnit tests for repetitive matrices = math + rect + pixel formats + strings + enum values + layout cases / keep bespoke tests for ownership-lifetime + callbacks + complex cleanup + multi-call workflows / use MTP/TUnit `--treenode-filter` for category runs — do not document old VSTest `--filter`): **DURABLE — kernel covering pure-parallel-vs-keyed-non-parallel discipline + 6 keyed-group names + data-driven-vs-bespoke-test-shape rules + MTP/TUnit-treenode-filter requirement**. Fold target `canonical/testing-strategy.md` ground-rules section per spec §Q9.
- "Ground Rules → Multi-Agent Execution" 6-bullet list (after infrastructure stabilization use multi-agent execution by SDL header or subsystem / agents may work parallel only when tasks do not require shared infrastructure edits / one agent per header/subsystem slice / each agent receives exact upstream source file(s) + generated binding file(s) + local target path + category/parallel policy + verification command / agents add or edit only assigned test files unless coordinator explicitly opens infrastructure change / each agent reports upstream cases ported + skipped + deferred + any generated binding blocker / coordinator runs full verification + resolves cross-slice consistency): **DURABLE — kernel covering multi-agent-per-header-subsystem-slice-dispatch + shared-infrastructure-edits-block-parallelism + per-agent-deliverable-catalog + coordinator-cross-slice-verification discipline**. Fold target `canonical/testing-strategy.md` ground-rules section per spec §Q9.
- "Test Project Layout" tree (one-project-folder-and-TUnit-categories split with `Infrastructure/` + `Mechanical/` + `Upstream/{Pure,Assets,DummyDrivers,GlobalState,CapabilityGated}/` + `TestAssets/sdl2-upstream/`; folder boundaries navigation only; assembly remains `Janset.SDL2.AbiTests`; runtime tests call `SDLNative` directly): **DURABLE — kernel covering one-project-folder-categorization + Upstream-class-folder-partition + folder-boundaries-are-navigation-not-architecture rule + SDLNative-direct-call-from-runtime-tests pattern**. Fold target `canonical/testing-strategy.md` ground-rules section per spec §Q9.
- "Categories" 10-row table (AbiSmoke = small always-run host smoke / AbiUpstreamPort = behavior ported from upstream SDL2 / AbiHeaderCoverage = Janset-owned header-based runtime or mechanical / AbiMechanical = symbol-layout-constant-macro surface guardrails / AbiPure = headless no real assets-dummy drivers-SDL global state / AbiAssets = headless using copied real assets or temp files / AbiGlobalState = mutates SDL process-global state / AbiDummyDriver = headless requires dummy video-audio or software renderer / AbiCapabilityGated = runs only when probed runtime capability exists / `SDL.<Subsystem>` = subsystem tag `SDL.Rect`/`SDL.RWops`/`SDL.Video`/`SDL.Audio`): **DURABLE — kernel covering 10-category test taxonomy used by `AbiCategories` constants**. Fold target `canonical/testing-strategy.md` Layer Model section per spec §Q9 — 10-category table is durable layer-model classification.
- "Coverage Strategy" — 10-bullet ABI risk family catalog (pointer ownership + create/free pairs / by-ref structs + out params / UTF-8 string input-output / callbacks + delegate-function-pointer signatures / event unions + explicit-layout structs / `SDL_bool` + enum + flag + scalar returns / `size_t`/`Sint64`/`nint`/`CLong`/`CULong` width-sensitive / Modern LibraryImport vs Compat DllImport behavior across TFMs / path-file marshalling through real assets + temp files / SDL global-state lifecycle through disciplined scopes) + 6-bullet targets-after-feasibility-review (P0 raw ABI spine `70-100` stable / P1 headless breadth additional `80-130` stable / P2 capability-gated/environment-sensitive additional `20-40` / default stable headless suite approximately `260-280` / aspirational total with gated lanes `300+` / mechanical guardrails close to full generated raw surface coverage for symbols-constants-layouts-callbacks-macro-policy) + caveat ("do not promise `300+` always-running headless until backend-sensitive proven stable; may reach `300+` total through capability-gated + scheduled lanes"): **DURABLE — kernel covering 10 ABI-risk families + numerically-targeted-coverage-tiers (P0 70-100 / P1 80-130 / P2 20-40 / default 260-280 / aspirational 300+) + mechanical-guardrails-aspiration-of-full-generated-surface + caveat-against-overpromising-always-running-headless-tests**. Fold target `canonical/testing-strategy.md` "Generated Raw ABI Runtime Tests" subsection per spec §Q9.
- "Upstream Feasibility Summary" (SDL2 upstream automation inventory: `318` declared `testautomation_*.c` cases / `310` enabled upstream cases / `260-280` strong headless+raw ABI candidates / `20-35` backend-sensitive but possible / `35-55` skip+defer+future-manual-hardware) + 19-entry P0/P1 high-value sources list (`testautomation_rect.c` + `testautomation_rwops.c` + `testautomation_surface.c` + `testautomation_pixels.c` + `testautomation_guid.c` + `testautomation_platform.c` + `testautomation_timer.c` + `testautomation_hints.c` + `testautomation_events.c` + `testautomation_audio.c` + `testautomation_video.c` dummy-safe subset + `testautomation_render.c` enabled software-render subset + `testautomation_keyboard.c` + `testautomation_mouse.c` dummy-safe subset + `testautomation_math.c` + `testautomation_stdlib.c` + `testautomation_main.c` + `testautomation_subsystems.c` + `testautomation_joystick.c` virtual joystick subset when available) + 4-bullet known-blockers-deferrals (`testautomation_syswm.c` waits for typed union generation + `SDL_GetWindowWMInfo` / thread creation waits for explicit `SDL_CreateThread` macro+REAL handling / OS clipboard+primary-selection backend-sensitive + capability-gated after dedicated review / real display-focus-gamma-brightness + interactive-input not default-safe): **DURABLE — kernel covering SDL2 upstream automation inventory (318/310/260-280 strong/20-35 backend-sensitive/35-55 skip-defer-future) + 19-source high-value-port-list + 4 known blockers**. Fold target `canonical/testing-strategy.md` "SDL2 Core Research Summary" subsection per spec §Q9.
- "Verification Commands" code block (`dotnet test --project spikes/binding-generators/clangsharp/tests/abi-tests/AbiTests.csproj -c Release` + `slopwatch analyze --fail-on warning --exclude "artifacts/**,external/**,vcpkg_installed/**,spikes/binding-generators/references/**,**/bin/**,**/obj/**"` + `git diff --check`) + caveat ("category-specific runs should use TUnit/MTP tree filters after the exact expression is verified for this project; do not document unverified filter commands"): **DURABLE — kernel covering 3-command verification gate + verified-filter-only discipline ("do not document unverified filter commands")**. The `dotnet test` and slopwatch commands are spike-local path strings (sunset-specific path), but the **verification-gate-discipline + verified-filter-only rule** are toolchain-neutral. Fold target `canonical/testing-strategy.md` ground-rules section per spec §Q9 (alongside the existing verification command catalog in destination doc).

**Verdict:** ready to git rm after fold (fold targets: (1) Purpose + Hard Boundaries + Headless-Safe Default + Upstream Adoption + Janset Header-Based Coverage + Infrastructure First + TUnit And Parallelism + Multi-Agent Execution + Test Project Layout + Verification Commands → `canonical/testing-strategy.md` ground-rules section; (2) Categories 10-row table → `canonical/testing-strategy.md` Layer Model section; (3) Coverage Strategy 10-bullet ABI risk families + numerically-targeted tiers (P0 70-100 / P1 80-130 / P2 20-40 / default 260-280 / aspirational 300+) → `canonical/testing-strategy.md` "Generated Raw ABI Runtime Tests" subsection; (4) Upstream Feasibility Summary 318/310/260-280-strong inventory + 19-source high-value list + 4 known blockers → `canonical/testing-strategy.md` "SDL2 Core Research Summary" subsection). Per spec §Q9, this file retires entirely after the durable kernel folds into `canonical/testing-strategy.md` during Phase 2.3.

### File: spikes/binding-generators/docs/testing/raw-abi-upstream-testing-plan.md

**Sections audited:**

- "Goal" + "Architecture" + "Tech Stack" headers (turn existing ClangSharp `AbiTests.csproj` into reusable high-coverage SDL2 raw ABI test lab supporting `260-280` stable headless + `300+` total with gated lanes / one spike-local TUnit project as execution boundary / build reusable infrastructure first + normalize current tests onto infrastructure + port upstream-header coverage by independent SDL header or subsystem slices using multi-agent waves / .NET 10 SDK + C# preview + TUnit + MTP + unsafe raw ABI + `Janset.SDL2.Core.Native` local-feed + upstream SDL2 `SDL2` branch under gitignored `spikes/binding-generators/references/SDL`): duplicate of `raw-abi-upstream-testing-spec.md` Purpose + Hard Boundaries + Infrastructure First (already folding into `canonical/testing-strategy.md` per testing-spec audit above) → no fold needed beyond that.
- "Execution Ground Rules" 7-bullet list (manual diagnostics stopped — no Manual/+no manual skip attributes+no real-window visual+no real speaker+no haptic+no GL/GLES/Vulkan+no hardware input / keep single spike project — rename/topology cleanup happens during production promotion not now / before broad subsystem ports harden infrastructure + retrofit current tests / default automated tests must be headless-safe + backend-sensitive requires explicit skip/capability attributes / upstream `testautomation_*.c` primary oracle but Janset-owned header tests equally valid when upstream does not cover generated binding risks / multi-agent waves start only after shared infrastructure stable + agents should not edit shared infrastructure during independent subsystem slices / category runs must use verified TUnit/MTP `--treenode-filter` + do not use stale VSTest `--filter`): duplicate of testing-spec Hard Boundaries + Headless-Safe Default + Janset Header-Based Coverage + Multi-Agent Execution + TUnit And Parallelism (already folding into `canonical/testing-strategy.md` per testing-spec audit above) → no fold needed beyond that.
- "Files And Responsibilities" 14-row table mapping each project path to responsibility (`AbiTests.csproj` single project + native package + asset copy / `Infrastructure/Classification/` category + parallel-key + traceability / `Infrastructure/Diagnostics/` SDL assertion-error / `Infrastructure/Text/` UTF-8 pin-decode / `Infrastructure/Assets/` asset lookup + per-test temp / `Infrastructure/Runtime/` runtime facts + capability probes + skip requirements + global-state scopes / `Infrastructure/Callbacks/` Modern+Compat callback lifetime + function-pointer / `Infrastructure/Macros/` C# equivalents for SDL function-like macros / `Mechanical/` symbol-layout-constant-header guardrails / `Upstream/Pure/` pure runtime tests / `Upstream/Assets/` asset+temp-file runtime tests / `Upstream/DummyDrivers/` dummy video-audio-software-renderer / `Upstream/GlobalState/` init-quit + hints + events + error state + subsystem lifecycle / `Upstream/CapabilityGated/` backend-sensitive with explicit capability skips): duplicate of testing-spec "Test Project Layout" tree (already folding into `canonical/testing-strategy.md` per testing-spec audit above) → no fold needed beyond that.
- "Phase 0: Stabilize Current Slice" — Task 0.1 Freeze Manual Scope (modify spec+plan to remove Stage D/manual diagnostics + state manual deferred until after raw ABI headless infrastructure-coverage stable + verify no active task asks agent to add `Manual/`) + Task 0.2 Capture Current Test Inventory (create `raw-abi-current-inventory.md` + list current test files + record current unique test count + multi-TFM execution count + mark each test keep/refactor/expand/replace + record known generated binding blocker) + Task 0.3 Verify Baseline Before Refactor: execution-scaffolding workflow tasks (sunset Phase 0 stabilization actions already completed and documented in `raw-abi-current-inventory.md`) → no fold needed.
- "Phase 1: Build Reusable Test Infrastructure" — Task 1.1 Add Category And Parallel-Key Constants + Task 1.2 Add Runtime Probe And Capability Skips + Task 1.3 Add Callback Bridge Helpers + Task 1.4 Add Macro Helper Surface + Task 1.5 Normalize Existing Tests: execution-scaffolding workflow tasks (sunset Phase 1 infrastructure-build actions already completed — files exist per inventory.md "Current Infrastructure" 17-row table) → no fold needed; the *infrastructure-first-before-broad-porting* principle is already captured in testing-spec §"Infrastructure First" folding into `canonical/testing-strategy.md` ground-rules.
- "Phase 2: P0 ABI Spine Multi-Agent Wave" 10-row agent-slice table (Rect = `test/testautomation_rect.c` → `Upstream/Pure/RectAbiTests.cs` port most/all 36 cases / RWops = `test/testautomation_rwops.c` → `Upstream/Assets/RwopsAbiTests.cs` port all 10 automation + temp-file hygiene / Surface = `test/testautomation_surface.c` → `Upstream/Assets/SurfaceAbiTests.cs` enabled software-surface+BMP cases + defer upstream-disabled blend / Pixels = `test/testautomation_pixels.c` → `Upstream/Pure/PixelsAbiTests.cs` all automation + header-owned format metadata / GUID = `test/testautomation_guid.c` → `Upstream/Pure/GuidAbiTests.cs` all cases+variants for byte-string ABI confidence / Platform-Error-Version = `test/testautomation_platform.c`+`testver.c`+`testerror.c` → `Upstream/Pure/PlatformAbiTests.cs` stable platform-version-error-endian / Timer = `test/testautomation_timer.c` → `Upstream/GlobalState/TimerAbiTests.cs` counter-frequency-delay-timer-callback with conservative timing / Hints+Events = `test/testautomation_hints.c`+`test/testautomation_events.c` → `Upstream/GlobalState/` all deterministic hint-event-callback / Audio Spine = `test/testautomation_audio.c`+`loopwave.c` → `Upstream/DummyDrivers/AudioAbiTests.cs`+`Upstream/Assets/WavAbiTests.cs` dummy-safe open-close+conversion+WAV load-free / Video-Render Spine = `test/testautomation_video.c`+`test/testautomation_render.c` → `Upstream/DummyDrivers/` dummy-safe window lifecycle + enabled software renderer): **DURABLE — kernel covering P0 wave 10-slice agent-dispatch matrix (slice name → upstream source → destination test file → port target)** — forward-work backlog. Fold target new "Raw ABI Upstream Port Backlog" subsection under `canonical/testing-strategy.md` Implementation Backlog per spec §Q9.
- "Phase 2" agent-per-wave deliverable catalog (upstream cases ported / upstream cases skipped with reason / generated binding blockers / new helper needs not part of locked infrastructure / verification command+result) + coordinator-duties-after-each-wave (review agent diffs for category-parallel-cleanup consistency / run full `dotnet test` / run slopwatch / update inventory with counts+deferrals): duplicate of testing-spec §"Multi-Agent Execution" 6-bullet list (already folding into `canonical/testing-strategy.md` ground-rules per testing-spec audit) → no fold needed beyond that.
- "Phase 3: P1 Headless Breadth Multi-Agent Wave" 8-row agent-slice table (Math = `test/testautomation_math.c` → `Upstream/Pure/MathAbiTests.cs` representative-or-full matrix with clear floating-point tolerance / Keyboard = `test/testautomation_keyboard.c` → `Upstream/DummyDrivers/KeyboardAbiTests.cs` scancode-key-name-mod-state + gate text-input-focus if needed / Mouse = `test/testautomation_mouse.c` → `Upstream/DummyDrivers/MouseAbiTests.cs` cursor-state + defer focus-relative-mode unless dummy-stable / Stdlib = `test/testautomation_stdlib.c` → `Upstream/GlobalState/StdlibAbiTests.cs` environment-string-memory with cleanup / Main-Subsystems = `test/testautomation_main.c`+`test/testautomation_subsystems.c` → `Upstream/GlobalState/SubsystemAbiTests.cs` serialized lifecycle / Log = `test/testautomation_log.c` → `Upstream/GlobalState/LogAbiTests.cs` log priority-output function with callback bridge / Virtual Joystick = `test/testautomation_joystick.c` → `Upstream/CapabilityGated/JoystickAbiTests.cs` virtual joystick path when SDL build supports it / Atomic-Mutex-Thread Adjacent = `testatomic.c`+`testlock.c`+`testsem.c` → `Upstream/Pure/` or `Upstream/GlobalState/` deterministic non-torture cases + defer thread creation until generator support): **DURABLE — kernel covering P1 wave 8-slice agent-dispatch matrix (slice → upstream source → destination → port target+conditional rules)** — forward-work backlog. Fold target same "Raw ABI Upstream Port Backlog" subsection per spec §Q9.
- "Phase 4: P2 Capability-Gated Wave" 4-row area table (Clipboard = `testautomation_clipboard.c` capability-gated per OS-session + no default failure when clipboard backend absent / Filesystem-Locale-Power-LoadSO = mini tests smoke-level + avoid exact environment values / YUV-Iconv-Geometry = mini tests + assets pure conversion or asset-backed paths only / Additional Video-Mouse Focus = automation subsets only after dummy or CI display behavior proven stable): **DURABLE — kernel covering P2 wave 4-area capability-gated matrix (area → upstream source → applied rule)** — forward-work backlog. Fold target same "Raw ABI Upstream Port Backlog" subsection per spec §Q9.
- "Phase 5: Mechanical Header Coverage" 5-row guardrail table (Symbol coverage = generated extern declarations vs native exports where feasible / Constant coverage = enum-flag-macro constants by header / Layout coverage = struct size-field offsets-fixed buffers-explicit-layout unions / Callback coverage = generated callback signatures + TFM-specific ABI bridge shape / Macro policy coverage = function-like macros classified as emitted helper/test helper/unsupported/intentionally skipped) + "this track may run in parallel with P0/P1 only if it does not require shared infrastructure changes" caveat: **DURABLE — kernel covering Phase 5 mechanical-guardrail 5-row scope catalog + parallel-with-P0-P1-if-no-shared-infra-edits discipline**. Fold target same "Raw ABI Upstream Port Backlog" subsection per spec §Q9.
- "Verification" + "Commit Gate" sections (3-command verification gate after every infrastructure task + each agent wave + expected outcomes pass-across-TFMs + skipped tests have explicit capability reasons + slopwatch zero issues + `git diff --check` no whitespace errors / commit gate = changed file summary + verification evidence + proposed commit message + do not commit until Deniz approves): duplicate of testing-spec §"Verification Commands" (already folding into `canonical/testing-strategy.md` ground-rules per testing-spec audit) + AGENTS.md commit-gate operating rules → no fold needed.

**Verdict:** ready to git rm after fold (fold targets: (1) Phase 2 P0 wave 10-slice agent-dispatch matrix + Phase 3 P1 wave 8-slice agent-dispatch matrix + Phase 4 P2 wave 4-area capability-gated matrix + Phase 5 Mechanical Header Coverage 5-row guardrail catalog → new "Raw ABI Upstream Port Backlog" subsection under `canonical/testing-strategy.md` Implementation Backlog). Everything else (Goal+Architecture+Tech Stack headers / Execution Ground Rules / Files And Responsibilities / Phase 0 sunset tasks already complete / Phase 1 sunset tasks already complete / Phase 2 per-wave deliverable catalog / Verification + Commit Gate sections) is execution-scaffolding workflow whose durable kernel already folds into `canonical/testing-strategy.md` via the testing-spec audit above — no separate fold needed. Per spec §Q9, this file retires entirely after the 4-wave forward-work backlog folds into `canonical/testing-strategy.md` during Phase 2.3.

### File: docs/playbook/binding-generator-maintenance.md

**Sections audited:**

- "Status" / "Last updated" sunset banner header (sunset-reference status; sunset Cake-hosted CppAst implementation under `build/_build/Targets/GenerateBindings/`; ADR-004 Reopened 2026-05-23 toolchain re-evaluation note): sunset-specific (drop) — replaced by Status banner of the NEW `canonical/binding-generator-maintenance.md` written from scratch per Q11.
- "Purpose paragraph" (covers maintenance work touching generated bindings, platform macro catalogs, CppAst/libclang versions, hybrid-static native inputs; intentionally conservative + generated bindings as public API + generator tied to vcpkg-built native payloads): sunset-specific framing (CppAst/libclang naming) — replaced by NEW playbook §1 (Purpose + When to Use), which retains the "generated bindings are public API + generator is build infrastructure" principle but reframes the toolchain as ClangSharp tool (`dotnet-tools.json`) + `libclang.runtime.*` (transitive) + `Microsoft.CodeAnalysis.CSharp` postprocess + `oracle.cs` `#:package` directive.
- "When To Use This" 6-trigger list (SDL header changes via vcpkg baseline/SDL version bump; PlatformCatalog/parse-view macro definitions review; CppAst/libclang/libClangSharp version changes; new SDL satellite enters scope; SDL3 planning starts; overlay triplets/ports change): **toolchain-neutral trigger taxonomy** → adapt to NEW playbook §1 (Purpose + When to Use). Replace "CppAst/libclang/libClangSharp version changes" with "ClangSharp tool / `libclang.runtime.*` / `Microsoft.CodeAnalysis` postprocess version changes"; replace "PlatformCatalog/parse-view macro definitions" with "`global.platform_views` / `all_platform_macros` in `family-config.json`"; rest of triggers (SDL version bump via vcpkg baseline / new satellite / SDL3 planning / overlay triplet changes) transfer literally.
- "Current Stage 1 Contract" (generator home `build/_build/Targets/GenerateBindings/`; header input services `HeaderSet/`; parse catalog `PlatformViews/PlatformCatalog.cs`; persisted stamp `GeneratedStamp.cs` + `GeneratedStampRepository.cs`; current parse-view model = Neutral/WindowsDesktop/WinRT/GDK/Linux/MacOS/IOS/Android; backends bundled into OS parse views; DirectFB/Vivante/MIR/OS-2 explicit exclusions): sunset-specific Cake-impl path enumeration + sunset Stage 1 parse-view model (drop) — the parse-view list itself is durable but lives in `global.platform_views` of `family-config.json` per spec §3 and constitution; the sunset Cake-impl `PlatformCatalog.cs` path is being replaced by `family-config.json` config-driven platform views which the NEW playbook §3 (Configuration Surface Map) documents.
- "Trio Pinning" (CppAst + libclang.runtime + libClangSharp.runtime moves as coordinated set per ADR-004; mismatches surface as runtime AST parsing failures; 2-row version table 0.24.0 + 20.1.2 + 20.1.2 pinned vs 0.25+ candidates; `LibclangVersionAsserter` defensive runtime assertion; 6-step bump procedure update `Directory.Packages.props` → `dotnet restore --force-evaluate` → `tools.cs generate-bindings` → update version-assertion pattern → update table → update ADR-004): **DURABLE — kernel covering coherent-set version pinning discipline + table + bump procedure + defensive runtime assertion**. Toolchain identity changes (sunset trio = CppAst + libclang.runtime + libClangSharp.runtime; new quartet = ClangSharp tool + `libclang.runtime.*` (via ClangSharp transitive) + Microsoft.CodeAnalysis.CSharp + `oracle.cs` `#:package` directive). Adapt to NEW playbook §2 (Toolchain Version Pinning) — the *coherent-set-pinning + table-of-pinned-vs-candidate + defensive-runtime-assertion + step-by-step-bump-procedure* pattern is toolchain-neutral; carry the principle forward with the new quartet names. The `LibclangVersionAsserter` mechanism in the new toolchain becomes verification that ClangSharp's transitive `libclang.runtime.*` resolves to the pinned major version at oracle/generation entry. The 6-step bump procedure adapts as: update `dotnet-tools.json` + `Directory.Packages.props` + `postprocess/Janset.SDL2.PostProcess.csproj` VersionOverride + `oracle.cs` `#:package` directive → `dotnet restore --force-evaluate` → regenerate via spike toolchain → run oracle → update version-assertion check → update table → update ADR-004.
- "Post-Stage-1 Trio Revalidation" (after Stage 1 production output stable attempt to bump trio to latest compatible NuGet; deliberate maintenance slice; update package versions + run generation + compare output + compile-check + update table): **DURABLE — toolchain-neutral periodic-revalidation discipline**. Adapt to NEW playbook §2 (Toolchain Version Pinning) as a "Post-Layer-1 Quartet Revalidation" subsection — after Layer 1 production output is stable (post-flip Phase 3.3+), attempt to bump the ClangSharp + libclang.runtime + Microsoft.CodeAnalysis + oracle.cs quartet to the latest compatible NuGet versions as a deliberate maintenance slice. Same discipline rules apply.
- "Local Generation Loop" (3-command catalog `dotnet run --file tools.cs -- generate-bindings` + `--rebuild-image` flag + `--no-cache` flag; output lands `artifacts/generated-bindings-preview/sdl2-core/` gitignored; vcpkg binary cache as named Docker volume `janset-vcpkg-cache`; production-location flag-flip to `src/SDL2.<Family>/Generated/` is Stage 1 Task 7 concern): sunset-specific (drop) — sunset Cake `tools.cs generate-bindings` invocations replaced by the spike's ClangSharp toolchain (`dotnet tool run ClangSharpPInvokeGenerator @rsp/<family>.rsp` or equivalent invocation per spike `clangsharp/README.md`). The *local-generation-loop concept* (named generation command + flag inventory + output artifact location + cache-volume management) survives toolchain-neutral; NEW playbook §6 (Version-Bump Procedures) replaces the literal commands with ClangSharp toolchain commands and points at spike's `clangsharp/scripts/` invocation entrypoints. The production-location flag-flip discipline becomes Phase 3.3 production flip in NEW playbook §6.
- "Profile Boundary Maintenance" (M3 binding-generation-profile maintenance; `binding_generation.profile_id` as routing key into code-owned profile policy not JSON behavior switch; 5-step satellite-addition checklist: verify manifest carries family identity facts (`profile_id`/namespace/primary class/platform catalog id/owned prefixes/accepted export macro token inventory); resolve satellite core dependency from `package_families[].depends_on`; add/update manifest integration fixtures; add focused `.h` fixtures when touching parse/model behavior — SDL2_gfx scope macros + SDL2 satellite declaration tokens + callbacks + C `long` + `SDL_bool` + by-value core structs + pointer-to-pointer; keep disabled satellites non-emitting; Linux/libclang/vcpkg-installed-header check via focused docker command override): **DURABLE — toolchain-neutral satellite enablement checklist + profile-routing-key discipline + manifest-integration-fixture discipline + focused-`.h`-fixture-shape catalog + focused-docker-command-override pattern**. Adapt to NEW playbook §7 (Adding a New SDL Satellite Family) — the 5-step checklist transfers literally with toolchain-neutral path names (manifest entry in `build/manifest.json package_families[]` + `family-config.json` family entry with required fields + `rsp/sdl2-<family>.rsp` family identity + csproj + opaque handle audit + flags enum audit + clong methods audit + oracle activation + AbiTests expansion per spec §3 outline). The focused-`.h`-fixture-shape catalog (gfx scope macros / DECLSPEC declarations / callbacks / C `long` / `SDL_bool` / by-value structs / pointer-to-pointer) is durable per-family-shape evidence reference. The focused-docker-command-override pattern (`docker run --rm --entrypoint bash ... -lc "<focused test filter>"` over running full generation) was already captured in the M3 audit fold target for §9 (Linux-Canonical Generation) — confirm that fold target lands in NEW playbook §9.
- "Synthetic Headers Maintenance" (`SyntheticHeaders/` ships 8 platform-specific stub headers — 5 mandatory + 3 defensive — used by parser's `SystemIncludeFolders` because `libclang.runtime.linux-x64` ships no clang resource directory + Linux container has no Windows/Apple/WinRT SDKs; pure type stand-ins no business logic; 4 maintenance triggers SDL2 minor/patch release lands in vcpkg pin + new parse view added to `PlatformCatalog` + SDL3 binding scope arrives + stub content shape rules; peer baseline ppy/SDL3-CS ships exactly one stub vs Silk.NET apt-installs clang for real `/usr/lib/clang/<ver>/include`; retirement criteria = escalate from single-host Linux-canonical parsing to multi-runner cross-OS pipeline): **DURABLE in concept** — the synthetic-headers maintenance discipline survives toolchain swap because ClangSharp also uses `libclang.runtime.*` with no shipped clang resource directory on Linux. **However**, the spike's ClangSharp + Roslyn toolchain has restructured the platform-header story: spec §"Linux-Canonical Generation" (NEW playbook §9) references `--use-platform-header-shims` flag for Windows-local iteration, suggesting the platform-header-shim mechanism is a separate concern from production-evidence Linux canonical generation. **Adapt to NEW playbook §9 (Linux-Canonical Generation)** — the *stub-header-as-parser-input-not-runtime-binary* discipline + *4 maintenance triggers* + *retirement criteria* (single-host vs multi-runner cross-OS) survive. The 8-stub Cake-impl inventory is sunset-specific — actual ClangSharp shim inventory will be re-enumerated in the new playbook from the spike's `platform-header-shims/` directory.
- "Header Set Resolver Exclusions Maintenance" (`HeaderSetResolver` filters `*.h` glob over SDL2 vcpkg include via `manifest.library_manifests[].binding_generation.header_set.excluded_headers` + `excluded_header_prefixes`; current 7 categories umbrella `SDL.h` + scaffolding `begin_code.h`/`close_code.h` + satellite umbrellas + satellite prefix `SDL2_*` + graphics-API convenience wrappers + GL/GLES sub-headers + test scaffolding `SDL_test*` with detailed rationale per category; 3 maintenance triggers vcpkg pin bump + new satellite enters scope + test parity `HeaderSetResolverTests`): **DURABLE in concept** — toolchain-neutral header-exclusion-discipline survives the swap. ClangSharp toolchain uses `family-config.json` `families.<family>.headers[]` allowlist + `platform_sensitive_headers[]` instead of exclusion-based filtering. The *header-exclusion-category discipline* (umbrella headers / scaffolding / satellite umbrellas / graphics-API convenience wrappers / GL/GLES sub-headers / test scaffolding) becomes a *header-inclusion-discipline* in the new approach but the same 7 categories drive the discipline. **Adapt to NEW playbook §3 (Configuration Surface Map)** — the new playbook documents the `families.<family>.headers[]` schema; the 7-category header-classification taxonomy (umbrella vs scaffolding vs satellite vs convenience wrapper vs sub-header vs test scaffolding) survives as the *why-this-header-is-or-isn't-in-headers[]* rationale. The 3 maintenance triggers (vcpkg pin bump + new satellite + test parity) transfer literally.
- "Macro Constants Maintenance" (macro constants source-first; generator collects object-like `SDL_*` macros from parsed core public headers + classifies non-API/header-control macros + emits safe string and numeric constants + writes macro evidence into `parse-views.json`; `SDL_config*.h` macros source-visible but not public API stay classified as non-API report evidence rather than emitted constants; macro parser fixture discipline `build/_build.Tests/Fixtures/Data/GenerateBindings/`; `binding_generation.required_constants` for SDL.h umbrella-excluded constants + `binding_generation.macro_constants.excluded` for source-visible-but-not-public + `binding_generation.macro_constants.overrides` for explicit managed shape; unused manual entries fail by default with `allow_stale: true` + reason escape hatch): **DURABLE in concept** — source-first macro-constants discipline + required/excluded/overrides classification + unused-entries-fail-by-default + `allow_stale` escape-hatch are toolchain-neutral. ClangSharp toolchain doesn't emit `parse-views.json` (the spike's `oracle-evidence-clangsharp.md` plays a different role per Q11 — oracle.cs is automated evidence reporter not generator self-report). The macro-classification mechanism is re-implemented in ClangSharp output + Roslyn postprocess but the *required/excluded/overrides + fail-by-default-on-unused-entries* config-surface discipline survives. **Adapt to NEW playbook §3 (Configuration Surface Map)** — `family-config.json` will have analogous fields (or the discipline lives in `rsp/*.rsp` per-family files via ClangSharp `--config exclude=` / `--remap` rules + dedicated postprocess audit). The *parse-views.json* path reference is sunset-specific (drop).
- "Parser Options Audit Cadence" (per-family `parse_defines` + `clang_args` entries in `library_manifests[].binding_generation` carry workarounds for SDL header / libclang interaction bugs; not free + each entry documented mechanism + drift discipline; audit run at every trio bump + every SDL2 minor release; 3-step procedure re-read each entry's rationale → try removing one entry at a time + smoke + observe → compare against ppy/SDL3-CS `SDL3-CS/SDL3/*.rsp` files; bare-flag additions not acceptable): **DURABLE — toolchain-neutral parse-options-audit-discipline + audit-cadence triggers + comparison-with-peer-RSP-files**. Adapt to NEW playbook §4 (RSP File Maintenance) — the *audit-cadence-discipline* (per-entry rationale + try-removing-one-at-a-time + compare-against-ppy-RSP-files + bare-flag-additions-forbidden) transfers literally. The migration from sunset Cake-impl `library_manifests[].binding_generation.parse_defines` + `clang_args` to the new ClangSharp `rsp/base.rsp` + `rsp/sdl2-<family>.rsp` + `rsp/per-header/<header>.rsp` three-tier organization is structural; the discipline survives. Per spec §3 (RSP File Maintenance) — three-tier organization + when-to-add-an-entry + type-remap-discipline + additive-only-semantics-for-keyed-entries are the new playbook's articulation of this discipline.
- "Parse-time configuration surface (per family) — sdl2-core" (verified working at SDL2 2.32.10 + CppAst 0.24.0 + libclang 20.1.2; `parse_defines` rationale list `SDL_DECLSPEC=` + 9 GCC-intrinsic-disable macros `SDL_DISABLE_IMMINTRIN_H=1` etc. with line citations SDL_cpuinfo.h:118-133 and peer evidence Alimer.Bindings.SDL uses SDL3 equivalents; `clang_args` rationale `-fdeclspec` for `EGLAPI` `__declspec(dllimport/export)` in SDL_egl.h under SDL_VIDEO_DRIVER_WINDOWS + `-U__has_builtin` for `SDL_stdinc.h:127-131` forcing `_SDL_HAS_BUILTIN(x)` always-zero with peer reference ppy/SDL3-CS `SDL_stdinc.rsp` + side-effect-audit `SDL_assert.h:54` `SDL_endian.h:134/136/138`; non-config baseline `TargetSystem = "linux"` overriding CppAst's `"windows"` ctor default; `required_functions` 5 hand-curated SDL.h-umbrella-excluded base functions + Neutral-view merge mechanism; `required_constants` 10 hand-curated `SDL_INIT_*` macros with computed compound `SDL_INIT_EVERYTHING`; drift posture 2026-05-17 SDL2 maintenance mode since 2.28.0 + `SDL_INIT_*` set unchanged since SDL 2.0.0 + drift risk essentially zero + per-bump diff-review checklist sufficient): **DURABLE rationale content — sunset Cake-impl knob names but durable SDL2 header-interaction kernel**. Adapt to NEW playbook §4 (RSP File Maintenance) **rationale-per-entry discipline** + concrete sdl2-core RSP file (`rsp/sdl2-core.rsp`) rationale tables. The 9-macro GCC-intrinsic-disable rationale + line citations SDL_cpuinfo.h:118-133 + Alimer peer evidence is durable per-entry rationale. The `-fdeclspec` rationale for SDL_egl.h under SDL_VIDEO_DRIVER_WINDOWS is durable. The `-U__has_builtin` rationale + ppy `SDL_stdinc.rsp` peer reference + 3-site side-effect-audit (`SDL_assert.h:54` + `SDL_endian.h:134/136/138`) is durable. The `TargetSystem = "linux"` non-config baseline becomes a non-config baseline in ClangSharp (set by toolchain invocation, not in RSP). The 5 hand-curated `required_functions` + 10 hand-curated `required_constants` translate to ClangSharp `--include` entries in `rsp/per-header/SDL.h.rsp` (or analogous) — the per-RSP-file articulation is per Q11 spec §4. The 2026-05-17 drift posture analysis (SDL2 maintenance mode since 2.28.0 + `SDL_INIT_*` unchanged since SDL 2.0.0 + drift risk essentially zero) is durable maintenance-policy framing — fold into NEW playbook §4 or §6 (Version-Bump Procedures) as SDL2 drift-risk-posture context.
- "Parse-time configuration surface (per family) — sdl2-image/mixer/ttf/gfx/net" (Stage 2 satellite-rationale-fills-in-as-each-satellite-enabled; SDL2-image 59 declarations + watch `char **xpm` helpers; SDL2-mixer 97 declarations + callback typedefs + `SDL_bool` returns + owned types Mix_Chunk/Mix_Music/Mix_Fading/Mix_MusicType; SDL2-ttf 85 declarations + `TTF_Font` opaque handles + `SDL_Color` by-value + `long` parameters/returns + `SDL_bool` + legacy `const Uint16*` Unicode APIs + deprecated functions; SDL2_gfx not a `DECLSPEC` satellite — split across 4 headers with `SDL2_GFXPRIMITIVES_SCOPE`/`SDL2_IMAGEFILTER_SCOPE`/`SDL2_ROTOZOOM_SCOPE`/`SDL2_FRAMERATE_SCOPE` + mixed owned-prefix list `pixelColor`/`rotozoomSurface`/`SDL_imageFilter*`/`SDL_initFramerate`/`FPSmanager`; use `rg --no-ignore` for `vcpkg_installed/` audits): **DURABLE — toolchain-neutral satellite reconnaissance baseline (declaration counts + foreign-type catalogs + gfx 4-scope-macro inventory + gfx mixed-owned-prefix list)** but this evidence is already captured in `canonical/satellites/sdl2-{image,mixer,ttf,gfx}-header-analysis.md` per audit entries above. Adapt to NEW playbook §4 (RSP File Maintenance) by *citing* the satellite-analysis docs as the per-family rationale source (declaration-count + foreign-type-catalog + scope-macro-inventory live in satellites/; per-family RSP rationale in maintenance playbook §4 cross-references those facts). The `rg --no-ignore` tip for `vcpkg_installed/` audits is durable maintenance hint — fold into NEW playbook §3 or §7 as a tool tip.
- "Dynapi Manifest Cross-Check" (SDL2 ships `src/dynapi/SDL2.exports` Watcom-format text manifest autogenerated by `gendynapi.pl` listing every public export; cross-platform single source of truth — Linux/macOS/Windows binaries all export same symbol set per SDL2's dynapi design; lives inside vcpkg `buildtrees/sdl2/src/<sha>.clean/src/dynapi/SDL2.exports` after `vcpkg install sdl2` completes real source build; coverage limit name-only not signature — Watcom DEF-file records only `++'_NAME'.'SDL2.dll'.'NAME'` entries; vcpkg behaviour binary-cache hits skip source extraction + buildtrees empty on cache hit; reach mechanisms local container baked-in via Docker layer + CI multi-path `actions/cache@v5` covers buildtrees + host dev `tools.cs setup` populates buildtrees; scope SDL2-only — satellites consume different validator strategy header-derived expected-export sets or harvested-binary symbol extraction via `BinaryClosureWalker`; validator runs post-emit against manifest with 3 set operations Generator-emit-∩-manifest=correct + Generator-emit-\-manifest=false-positive-fail-closed + Manifest-\-generator-emit=false-negative-fail-closed; validator reused across 3 stages defense-in-depth Stage 1 GenerateBindings task / Stage 2 PreFlightCheck task / Stage 2 Pack stage `BindingSymbolExistenceValidator`; 4 maintenance triggers vcpkg pin bump + validator drift + Watcom-disabled lines + SDL3 transition uses `gendynapi.py --dump`'s `sdl.json`; peer evidence ppy/SDL3-CS `generate_bindings.py::check_generated_functions`): **DURABLE — toolchain-neutral dynapi-cross-check workflow + 3-set-operation discipline + 3-stage defense-in-depth + 4-maintenance-trigger catalog**. Adapt to NEW playbook §8 (Drift Detection Between Versions) — per spec §3 outline drift-detection covers `git diff --ignore-cr-at-eol` + `oracle-evidence-clangsharp.md` before/after + AbiTests pass counts + **dynapi coherence percentage** + Slopwatch. The *dynapi coherence percentage* slot is the new playbook's articulation of this discipline (the 3-set-operation framework feeds into a single coverage percentage). The *3-stage defense-in-depth* (Stage 1 GenerateBindings + Stage 2 PreFlightCheck + Stage 2 Pack `BindingSymbolExistenceValidator`) transfers to the new toolchain as: oracle.cs lane post-generation + PreFlightCheck before native build + Pack-stage per-RID binary export validation. The *Watcom DEF-file name-only coverage limit* + *vcpkg-buildtrees-binary-cache-hit-empty-source* + *3 reach mechanisms* (local container baked-in / CI multi-path cache / host dev `tools.cs setup`) are durable maintenance-mechanism facts. The *satellite-uses-different-validator-strategy* note (header-derived expected-export-sets or harvested-binary-symbol-extraction via `BinaryClosureWalker`) is durable per-family-boundary discipline. The *4 maintenance triggers* (vcpkg pin bump + validator drift + Watcom-disabled lines + SDL3 `gendynapi.py --dump` `sdl.json` transition) transfer literally. The *peer evidence ppy `check_generated_functions`* citation is durable peer reference.
- "Platform Macro Catalog Maintenance" (platform catalog as explicit maintenance surface — normal for SDL binding generators per peer ppy/SDL3-CS practice; not set-and-forget; 6-step procedure inspect platform-related headers SDL2 `SDL_platform.h`+`SDL_config.h`+`SDL_stdinc.h`+`SDL_system.h`+`SDL_main.h`+`SDL_syswm.h` vs SDL3 `SDL_platform_defines.h`+`SDL_platform.h`+`SDL_init.h`+`SDL_system.h`+any-header-with-`SDL_PLATFORM_*`; search for platform/backend conditionals SDL2-style `_WIN32`/`__APPLE__`/`__MACOSX__`/`__IPHONEOS__`/`__ANDROID__`/`linux`/`__linux__`/`SDL_VIDEO_DRIVER_*` vs SDL3-style `SDL_PLATFORM_*`; compare found macros with `PlatformCatalog.AllPlatformMacros`; add/remove parse views only when public declarations or emitted attribution change; update `PlatformCatalogTests`; regenerate + review diff; do not copy SDL2 macro list into SDL3 — SDL3 different platform macro contract gets own catalog when Phase 5 starts): **DURABLE — toolchain-neutral platform-macro-catalog-maintenance discipline + 6-step procedure + SDL2-vs-SDL3-platform-macro-separation rule**. Adapt to NEW playbook §3 (Configuration Surface Map) — `global.platform_views` + `all_platform_macros` are the new playbook's articulation of `PlatformCatalog`. The 6-step procedure transfers: inspect platform-related headers (same SDL2 6 headers + SDL3 4+ headers), search for platform/backend conditionals, compare with `family-config.json` `global.all_platform_macros`, add/remove views only when public declarations or attribution change, update tests, regenerate + review. The SDL2-vs-SDL3-platform-macro-separation rule is durable per-toolchain-version discipline.
- "Header Set And Stamp Maintenance" (header discovery + fingerprinting are target-local generation services not Data-layer repositories — read vcpkg-installed input tree for one generation run; `.generated-stamp` is persisted build-host contract belongs under `Data/BindingGeneration/`; when header inputs change header fingerprint should change + `.generated-stamp` should update + PreFlight coherence validation should eventually fail if stale; stamp must not contain wall-clock timestamps — record reproducible state generator/toolchain versions + vcpkg state + manifest library version + header fingerprint + header count + parse views): **DURABLE in concept** — toolchain-neutral *target-local-vs-persisted-contract* separation + *no-wall-clock-in-stamp + reproducible-state-in-stamp* discipline survives. ClangSharp toolchain's equivalent stamp/fingerprint mechanism (if any) is spike-specific. **Adapt to NEW playbook §8 (Drift Detection Between Versions)** — the *header-fingerprint-changes-when-input-changes + stamp-updates-then + PreFlight-coherence-validation-eventually-fails-if-stale* discipline is one input to drift-detection. The *no-wall-clock + reproducible-state-in-stamp* rule is durable hygiene; if the new toolchain re-implements a `.generated-stamp`-equivalent (per `library_version` field per family in `family-config.json` mentioned in spec §3 outline + per-family generated artifact provenance), this rule transfers.
- "Hybrid-Static / Overlay Coupling" (binding maintenance coupled to native build maintenance; 5 binding-specific checks when updating overlays or vcpkg state — overlay port changes SDL feature flags → confirm affected public headers + exported symbols / hybrid triplet changes compiler flags → confirm symbol visibility / Linux-macOS visibility flags or version scripts change → check generated entry points match exported symbols / satellite starts/stops exposing function due to build options → generated binding surface + future symbol-existence validation reflect that / vcpkg baseline changes upstream port patches without changing SDL upstream version → still regenerate/validate `.generated-stamp` because header patches can change ABI-relevant declarations; cross-reference `docs/playbook/overlay-management.md` for overlay procedure): **DURABLE — toolchain-neutral overlay/triplet/hybrid-static binding-maintenance coupling discipline + 5 binding-specific check catalog**. Adapt to NEW playbook §6 (Version-Bump Procedures) under "vcpkg triplet bump" subsection — the 5 binding-specific checks (overlay-feature-flag-changes-affect-headers/exports + triplet-compiler-flag-changes-affect-visibility + Linux-macOS-visibility-or-version-script-changes-affect-entrypoints + satellite-build-option-changes-affect-emitted-surface + vcpkg-baseline-port-patch-changes-can-change-ABI-declarations) transfer literally. The *cross-reference `docs/playbook/overlay-management.md`* survives — overlay-management playbook is unchanged by Phase 4 doc consolidation.
- "SDL2 vs SDL3 Maintenance" (SDL2 relatively stable + SDL3 not; SDL2 = patch updates require regeneration plus diff review + SDL2_gfx effectively frozen but still needs export/symbol validation because third-party + `SDL_syswm.h` typed unions remain Stage 2 scope; SDL3 = treat platform macros as new catalog not extension + expect `SDL_PLATFORM_*` + re-evaluate legacy TFMs before copying SDL2's `netstandard2.0`/`net462` obligations + re-evaluate bool/IO/handle rules before emitting public API): **DURABLE — toolchain-neutral SDL2-vs-SDL3-maintenance-distinction + SDL2-stable-vs-SDL3-volatile posture + SDL2_gfx-frozen-but-third-party-validation-still-needed + SysWM-Stage-2 + SDL3-new-catalog-not-extension + SDL3-re-evaluate-TFMs-bool-IO-handle-rules**. Adapt to NEW playbook §6 (Version-Bump Procedures) — the SDL2 vs SDL3 posture transfers (SDL2 = patch updates trigger regeneration + diff review; SDL3 = new family additions trigger full re-evaluation). The SysWM-Stage-2 scoping is already in canonical roadmap M5 "SysWM Layout And Satellite Sweep Close" per spec §Q4D. The SDL3-platform-catalog-separation is already captured in the §"Platform Macro Catalog Maintenance" fold target (NEW playbook §3).
- "Maintenance Checklist" 13-item generic checklist (relevant SDL platform headers inspected / new-or-removed platform-backend macros compared with PlatformCatalog / SDL2 and SDL3 macro models kept separate / header-set fingerprint behavior still matches intended inputs / `.generated-stamp` updated without wall-clock fields / overlay triplet/port changes reviewed for header/export impact / generated source diff reviewed for public API changes / build host tests passed / package-first smoke path identified for affected family / docs updated if maintenance procedure changed / Trio version table reviewed for CppAst bump candidacy / manual `tools.cs generate-bindings` smoke after maintenance change / SyntheticHeaders/ reviewed against new platform-specific `#include` references / HeaderSetResolver.ExcludedHeaders reviewed for new convenience wrappers etc / Parser BaseDefines+BaseAdditionalArguments audited / Dynapi manifest (SDL2.exports) refreshed for new pin): **DURABLE in concept** — toolchain-neutral maintenance-checklist-as-final-gate discipline. Many items transfer literally (platform headers inspected / platform-backend macros compared with config / SDL2-SDL3 separation / stamp no wall-clock / overlay impact reviewed / generated source diff reviewed / build host tests passed / package-first smoke / docs updated / SyntheticHeaders or platform-header-shims reviewed / Dynapi manifest refreshed). Items mentioning sunset Cake-impl names (`tools.cs generate-bindings` / Trio version table for CppAst / HeaderSetResolver.ExcludedHeaders / Parser BaseDefines+BaseAdditionalArguments) get renamed for the new toolchain. **Adapt to NEW playbook §6 (Version-Bump Procedures) or a dedicated §"Maintenance Checklist" subsection** — depending on whether the new playbook keeps a single concluding checklist or distributes checklist items per §1-§11 subsection.
- "Related Docs" 6-item cross-reference list (constitution / roadmap / testing-strategy / overlay-management / vcpkg-update / 3 ADRs): sunset-specific cross-reference list — replaced by NEW playbook §11 (References) per spec §3 outline; ADR-002 / ADR-003 / ADR-004 references survive; constitution + roadmap + testing-strategy paths update to canonical/; overlay-management + vcpkg-update playbook cross-references survive unchanged.

**Verdict:** ready to git rm after CppAst-independent adapt-to-NEW-playbook fold. Fold targets identified (CppAst-independent kernel + new-playbook destination): (1) Trio Pinning + Post-Stage-1 Trio Revalidation → NEW §2 (Toolchain Version Pinning) with new quartet identity ClangSharp + libclang.runtime.* + Microsoft.CodeAnalysis + oracle.cs. (2) Parser Options Audit Cadence + sdl2-core rationale catalog (9-intrinsic-disable + `-fdeclspec` + `-U__has_builtin` + side-effect-audit + drift-posture-2026-05-17) → NEW §4 (RSP File Maintenance) per-RSP-rationale. (3) Dynapi Manifest Cross-Check + 3-set-operation + 3-stage-defense-in-depth + 4 maintenance triggers → NEW §8 (Drift Detection Between Versions) dynapi coherence percentage. (4) Satellite Enablement Checklist (Profile Boundary Maintenance) → NEW §7 (Adding a New SDL Satellite Family). (5) Profile Boundary Maintenance (`profile_id` as routing key + manifest family identity facts) → NEW §3 (Configuration Surface Map) `families.<family>` schema. (6) Local Generation Loop → NEW §6 (Version-Bump Procedures) — replace `tools.cs generate-bindings` with ClangSharp toolchain commands. (7) Synthetic Headers Maintenance → NEW §9 (Linux-Canonical Generation) — adapt to ClangSharp `--use-platform-header-shims` mechanism. (8) Header Set Resolver Exclusions Maintenance (7 categories + 3 triggers) → NEW §3 (Configuration Surface Map) `families.<family>.headers[]` schema rationale. (9) Macro Constants Maintenance (required/excluded/overrides + fail-by-default-on-unused + `allow_stale` escape hatch) → NEW §3 (Configuration Surface Map) macro-policy fields. (10) Platform Macro Catalog Maintenance (6-step procedure + SDL2-vs-SDL3-separation) → NEW §3 (Configuration Surface Map) `global.platform_views` + `all_platform_macros`. (11) Header Set And Stamp Maintenance (target-local-vs-persisted-contract + no-wall-clock + reproducible-state) → NEW §8 (Drift Detection Between Versions) stamp-fingerprint discipline. (12) Hybrid-Static / Overlay Coupling (5 binding-specific checks) → NEW §6 (Version-Bump Procedures) "vcpkg triplet bump" subsection. (13) SDL2 vs SDL3 Maintenance posture → NEW §6 (Version-Bump Procedures). (14) Maintenance Checklist 13-item list → NEW playbook concluding gate (likely §6 or dedicated §"Maintenance Checklist"). (15) Related Docs cross-references → NEW §11 (References). Sunset-specific content to drop literally: Status/Last-updated sunset banners; Current Stage 1 Contract Cake-impl path enumeration; `parse-views.json` path references; sunset Cake-impl knob names (`library_manifests[].binding_generation.parse_defines`/`clang_args`/`required_constants`/`required_functions`/`macro_constants.excluded`/`macro_constants.overrides`) — replaced by `family-config.json` + RSP organization in the new toolchain. The new playbook is **written from scratch in Phase 3.2** per Q11, not produced by transforming this file in place; the fold is content-conceptual not text-mechanical.

### File: docs/playbook/binding-output-oracle-validation.md

**Sections audited:**

- "Status" / "Last updated" toolchain-re-evaluation banner (workflow toolchain-neutral remains active; specific commands and paths assume sunset Cake-hosted implementation; spike's active oracle evidence under `spikes/binding-generators/output/reports/` with spike's own `oracle.cs` Roslyn-based evidence reporter producing `oracle-evidence-clangsharp.md`): refresh-only (path/command updates per Phase 2.5) — Status banner replaces toolchain-re-evaluation language with post-spike-closure neutrality; the existing references to spike's `oracle.cs` and `oracle-evidence-clangsharp.md` become canonical evidence-input rows rather than sunset-Cake-vs-spike distinction.
- "Purpose paragraph" (workflow defines how to validate generated binding output using multiple imperfect oracles: official SDL sources + native exports + generated evidence + peer bindings + .NET API extraction; intentionally review-led; tooling collects evidence + agents and humans decide interpretation): durable methodology kernel — no refresh needed; survives toolchain swap.
- "When To Use This" 6-trigger list (`tools.cs generate-bindings` run produces new SDL2.Core preview / macro+enum+struct+callback+function translation changes generated public API / `parse-views.json` shows unexpected category-count movement / dynapi validation emits missing/extra function warnings / peer comparison needed before promoting generated output toward production source / vcpkg SDL2 pin bump changes public headers or exports): refresh-only (path/command updates per Phase 2.5) — replace `tools.cs generate-bindings` with toolchain-neutral language ("a generation run produces a new family preview" or "the spike's ClangSharp + Roslyn toolchain produces new generated output"); replace `parse-views.json` with `oracle-evidence-clangsharp.md` (oracle.cs evidence report). Otherwise the 6-trigger taxonomy survives.
- "Do not use as substitute" paragraph + reference to `testing-strategy.md` for compile checks + fixture tests + native smoke + package consumer smoke: durable methodology kernel + cross-reference to canonical sibling doc — cross-reference path updates to `../binding-autogen/testing-strategy.md` → `testing-strategy.md` after move into canonical/ subfolder.
- "Principle" paragraph (validator must not pretend every useful oracle is machine-truth; reliable automation for narrow facts exported symbol names + public access modifiers + generated file presence + schema shape + compile success; broader API correctness remains review problem because SDL2-CS is stale + SDL3 peer libraries have different public shapes + SDL wiki pages are semantic documentation rather than ABI manifests + dynapi is name-only; use automation as evidence extractor + multi-agent review and maintainer judgment for interpretation): durable methodology kernel — no refresh needed.
- "Source Hierarchy" 7-row ranked table (1 pinned SDL2 public headers from exact vcpkg/upstream version / 2 actual built native binaries / 3 SDL2 `src/dynapi/SDL2.exports` / 4 SDL2 `src/dynapi/SDL_dynapi_procs.h` / 5 SDL wiki/API docs / 6 peer bindings .NET-first curated cross-language-second / 7 `parse-views.json` generator provenance): durable hierarchy kernel — refresh-only (Phase 2.5) — Rank 7 row `parse-views.json` → replace with `oracle-evidence-clangsharp.md` (oracle.cs evidence report as automated lane evidence; the spike's `oracle-evidence-clangsharp.md` is the generator-provenance source per Q11 spec). The "current SDL2 pin wins + do not compare generated output to SDL `main` + record exact source version/ref" guidance is durable.
- "Existing Evidence Inputs" 6-row table (`artifacts/generated-bindings-preview/sdl2-core/**/*.g.cs` Consumer-visible generated source before production-location flip / `artifacts/generated-bindings-preview/sdl2-core/parse-views.json` Machine-readable parse/emission report / `tests/binding-compile-check/SDL2.Core.CompileCheck.csproj` Multi-TFM compile proof / `vcpkg_installed/<triplet>/include/SDL2/*.h` Local pinned public header input / `external/vcpkg/buildtrees/sdl2/src/*/src/dynapi/SDL2.exports` SDL2.Core function-name export manifest / `external/sdl2-cs/src/SDL2.cs` Local legacy SDL2 compatibility oracle): refresh-only (Phase 2.5) — **major path refresh scope**. Row 1 `artifacts/generated-bindings-preview/sdl2-core/**/*.g.cs` → `src/Janset.SDL2.Core/Generated/**/*.g.cs` + analogous satellite paths `src/Janset.SDL2.Image/Generated/**/*.g.cs` etc. (sunset Cake `artifacts/generated-bindings-preview/` path retired; the spike commits generated `.g.cs` directly under `src/Janset.SDL2.*/Generated/`). Row 2 `parse-views.json` → `oracle-evidence-clangsharp.md` (spike's oracle.cs evidence report under `spikes/binding-generators/output/reports/`). Row 3 compile-check project — confirm exact spike path during Phase 2.5 purification (the spike likely has its own multi-TFM compile-check setup; if path moved, update). Row 4 vcpkg_installed path — durable (unchanged by toolchain swap). Row 5 dynapi manifest path — durable (unchanged by toolchain swap; same `external/vcpkg/buildtrees/sdl2/src/*/src/dynapi/SDL2.exports` location regardless of generator toolchain). Row 6 SDL2-CS local oracle — durable (unchanged). The remote peer sources URL-and-ref recording discipline is durable methodology.
- "What Can Be Automated Reliably" 9-bullet good-candidates-for-fail-gate list (generated preview compiles across net10.0/net9.0/net8.0/netstandard2.0/net462 / no effectively public DllImport-LibraryImport leak into generated public API / no public raw ABI class `SDLNative` exposed — lexically public methods inside internal raw container are not leaks / no function emitted absent from SDL2 dynapi manifest excluding configured exclusions / generated function names present in actual packaged native binary exports at Pack stage / `parse-views.json` exists has expected schema lists all generated files / hard internal macro denylist entries stay skipped with explicit reasons / source-visible manual macro includes-excludes-overrides consumed or explicitly stale-tolerant / generated API snapshots can be produced per TFM for review): refresh-only (Phase 2.5) — bullet 6 (`parse-views.json`) → replace with `oracle-evidence-clangsharp.md` (oracle.cs evidence report exists + has expected schema + lists all evidence categories). The remaining bullets are toolchain-neutral; the *evidence-first-not-fail-first* discipline + promote-to-fail-gate-only-after-repeated-runs-show-real-product-risk discipline is durable methodology kernel.
- "What Must Stay Review-Led" 9-bullet review-required list (SDL2-CS missing generated function may be stale rather than bug / SDL2-CS `IntPtr` where we expose typed handles / SDL3 peer libraries modeling concept differently / cross-language bindings modeling concept differently / SDL wiki docs implying ownership-lifetime semantics not visible in headers / platform-specific declarations whose visibility depends on parse-view macros / structs-unions public in headers but not useful or not safe in Stage 1 / callback and string overload ergonomics / acceptable modern API deltas from legacy SDL2-CS compatibility): durable methodology kernel — no refresh needed. The "if reviewer cannot tie finding to current SDL2 headers + shipped exports + explicit public API decision → classify as signal rather than hard bug" rule is durable.
- "Peer Binding Tiers" — "Primary .NET Peers" 4-row table (SDL2-CS local + ppy/SDL3-CS remote + Alimer.Bindings.SDL remote + Silk.NET remote) + "Secondary Cross-Language Peers" 5-row table (Rust-SDL2 sdl2-sys + Rust-SDL2 sdl2 high-level + go-sdl2 + BindBC SDL2 branch + PySDL2) + "Ad Hoc Secondary Peers" 3-row table (Fermium + haskell-game/sdl2 + Vladar4/sdl2_nim) + exclusion examples (old Go SDL2 forks + obsolete standalone Rust satellite crates + DerelictSDL2 + Zig demos + pygame/pygame-ce): durable peer-tier-taxonomy kernel — no refresh needed; the peer-tier discipline survives toolchain swap. Cross-reference paths (URLs) durable.
- "Finding Taxonomy" 6-row classification table (Hard Bug / Likely Bug / Compatibility Risk / Design Signal / Accepted Delta / Follow-up): durable finding-taxonomy kernel — no refresh needed.
- "Recommended Agent Lanes" — 6 lanes (Lane 1 Official SDL Header And Docs Review + Lane 2 Dynapi And Export Review + Lane 3 Peer Binding Review + Lane 4 Generated .NET Public API Review + Lane 5 Macro And Internal Leak Review + Lane 6 Aggregator Review): durable multi-lane-agent-orchestration kernel — refresh-only (Phase 2.5) inside lane prompts. Lane 1 prompt references `artifacts/generated-bindings-preview/sdl2-core` (generated output path) + `artifacts/generated-bindings-preview/sdl2-core/parse-views.json` (parse report) + `vcpkg_installed/<triplet>/include/SDL2` (pinned headers) — refresh generated output path to `src/Janset.SDL2.Core/Generated/` and parse report to `oracle-evidence-clangsharp.md`. Lane 2 prompt references same generated output + parse report + dynapi manifest — same refresh. Lane 3 prompt references generated output + local SDL2-CS + 5 optional remote peers + 5 curated secondary peers — refresh generated output path. Lane 4 prompt references generated output + compile-check project `tests/binding-compile-check/SDL2.Core.CompileCheck.csproj` — refresh generated output path; confirm compile-check project path during Phase 2.5 (spike may have moved this). Lane 5 prompt references `artifacts/generated-bindings-preview/sdl2-core/Constants.g.cs` (generated constants) + `parse-views.json` + `build/_build/Targets/GenerateBindings/Translation/MacroApiPolicy.cs` (macro policy source) — refresh constants path to `src/Janset.SDL2.Core/Generated/Constants.g.cs`; refresh parse report to `oracle-evidence-clangsharp.md`; refresh macro policy source path to the spike's ClangSharp + Roslyn postprocess equivalent (likely `postprocess/Janset.SDL2.PostProcess/MacroPolicy.cs` or analogous — confirm during Phase 2.5). Lane 6 aggregator prompt is path-free — no refresh needed.
- "Tooling Guidance" 8-row table (`rg`/source scan / `parse-views.json` / `dotnet build tests/binding-compile-check/...` / Reflection over compiled assemblies / PublicApiGenerator / Roslyn analysis / ApiCompat / Verify snapshots): refresh-only (Phase 2.5) — row 2 (`parse-views.json`) → replace with `oracle-evidence-clangsharp.md` (oracle.cs evidence report). Row 3 (compile-check command) — confirm exact spike path during Phase 2.5. Other rows are toolchain-neutral.
- "Current Command Evidence" 5-command pwsh block (`dotnet run --file tools.cs -- generate-bindings` + `dotnet build tests/binding-compile-check/SDL2.Core.CompileCheck.csproj -c Release` + `dotnet test --project build\_build.Tests\Build.Tests.csproj -c Release --framework net10.0` + `slopwatch analyze --fail-on warning --exclude "artifacts/**,external/**,vcpkg_installed/**,**/bin/**,**/obj/**"` + `git --no-pager diff --check`) + Docker-mounted Linux fixture command (`docker run --rm --entrypoint dotnet -v "${repo}:/workspace" -w /workspace "janset-binding-generator:focal-latest" test --project "/workspace/build/_build.Tests/Build.Tests.csproj" -c Release --framework net10.0 -- --treenode-filter "/*/*/SemanticHeaderFixtureTests/*"`): refresh-only (Phase 2.5) — **major command refresh scope**. Replace `dotnet run --file tools.cs -- generate-bindings` with the spike's ClangSharp toolchain invocation (e.g., `dotnet tool run ClangSharpPInvokeGenerator @rsp/<family>.rsp` per spec §3 outline + spike's `clangsharp/scripts/`). Replace `dotnet build tests/binding-compile-check/SDL2.Core.CompileCheck.csproj` with the spike's multi-TFM compile-check project path (confirm during Phase 2.5). Replace `dotnet test --project build\_build.Tests\Build.Tests.csproj` with the spike's AbiTests project path (`spikes/binding-generators/clangsharp/tests/abi-tests/AbiTests.csproj` per `raw-abi-upstream-testing-plan.md`). Update slopwatch exclude list paths to reflect spike layout (`spikes/binding-generators/references/**` exclusion already in spike usage). Update Docker-mounted Linux fixture command path to `docker/binding-generator.Dockerfile` + the spike's specific test filter (the `SemanticHeaderFixtureTests` filter is sunset Cake-impl test name — the spike has its own equivalent test project; confirm exact filter during Phase 2.5).
- "Report Template" markdown shape (Date YYYY-MM-DD / Generated output / SDL2 version / Generation command / Compile-check result / Summary go-no-go + Hard bugs N + Likely bugs N + Compatibility risks N + Follow-ups N / Hard Bugs table / Likely Bugs table / Compatibility Risks table / Accepted Deltas table / Follow-ups table / Lane Evidence): durable report-template kernel — refresh-only (Phase 2.5) — "Generated output" path placeholder updates to point at `src/Janset.SDL2.<Family>/Generated/`; "Generation command" placeholder updates to ClangSharp toolchain command; "Compile-check result" placeholder may rename to "Compile-check + AbiTests result" depending on spike test layout. The 6-section report structure (Summary + Hard Bugs + Likely Bugs + Compatibility Risks + Accepted Deltas + Follow-ups + Lane Evidence) is durable methodology.
- "When To Build A Small Internal Tool" 5-bullet (do not start with custom validator + start with playbook + parallel agents; build small internal report tool only when at least two review runs repeat same manual extraction; first candidates load `parse-views.json` + emit category-platform-macro summaries / extract public API from compiled assemblies per TFM into JSON / Roslyn source checks for public raw container + effective public extern leaks / join generated functions with dynapi and binary export facts / produce markdown report agents can review; keep first tool evidence-first + promote individual checks to fail gates only after failure mode is precise and low-noise): durable methodology kernel with two path refreshes — bullet 1 (`parse-views.json`) → adapt to "oracle.cs evidence report content" since the spike has already built oracle.cs as the automated lane per Q11. Several bullets describe candidate-tooling-for-future-work that oracle.cs now embodies — refresh framing acknowledges that oracle.cs is the realization of these candidates rather than future hypothetical work. The "do not start with custom validator + start with playbook + parallel agents + first-tool-evidence-first + promote-to-fail-gate-after-precise-failure-mode" discipline is durable methodology.
- "Common Mistakes" 7-bullet list (treating SDL2-CS as truth + treating SDL3 peer output as SDL2 ABI evidence + treating dynapi as signature validation + treating `parse-views.json` as upstream oracle + flattening all deltas into bugs + skipping embedded `.h` fixture coverage when changing parser or macro evaluator behavior + building large validator before knowing which findings stable enough to automate): refresh-only (Phase 2.5) — bullet 4 (`parse-views.json` as upstream oracle) → replace with `oracle-evidence-clangsharp.md` as upstream oracle (same anti-pattern: the oracle.cs evidence report is local-generator-provenance, not independent upstream truth). Other 6 bullets are toolchain-neutral durable mistake catalog.
- "Related Docs" 4-item cross-reference list (`../binding-autogen/binding-generator-constitution.md` / `../binding-autogen/binding-generator-roadmap.md` / `../binding-autogen/testing-strategy.md` / `binding-generator-maintenance.md`): refresh-only (Phase 2.5) — paths update to sibling canonical/ paths after move into canonical/ subfolder: `binding-generator-constitution.md` / `binding-generator-roadmap.md` / `testing-strategy.md` / `binding-generator-maintenance.md` (all relative to `canonical/` subfolder where this file now lives). The sunset `docs/playbook/binding-generator-maintenance.md` cross-reference target also moves to NEW `canonical/binding-generator-maintenance.md`.

**Verdict:** ready to git mv (purification scope for Phase 2.5 recorded). Path/command refresh items consolidated: (1) Status / Last-updated banner → post-spike-closure neutral wording. (2) `tools.cs generate-bindings` (3 occurrences across When-To-Use trigger 1 + Current Command Evidence command 1) → spike's ClangSharp toolchain invocation per spec §3 (e.g., `dotnet tool run ClangSharpPInvokeGenerator @rsp/<family>.rsp`). (3) `artifacts/generated-bindings-preview/sdl2-core/**/*.g.cs` (Existing Evidence Inputs row 1 + multiple Lane prompt references) → `src/Janset.SDL2.Core/Generated/**/*.g.cs` + analogous satellite paths (sunset Cake artifacts/ path retired; spike commits `.g.cs` directly under `src/Janset.SDL2.*/Generated/`). (4) `parse-views.json` (Source Hierarchy row 7 + Existing Evidence Inputs row 2 + Lane 1+2+5 prompt references + What-Can-Be-Automated bullet 6 + Tooling Guidance row 2 + Common Mistakes bullet 4 + When-To-Build-A-Small-Internal-Tool bullet 1 — **8 distinct occurrences**) → `oracle-evidence-clangsharp.md` (oracle.cs evidence report under `spikes/binding-generators/output/reports/` — automated lane per Q11; treat as local-generator-provenance source not independent upstream truth, same anti-pattern as the sunset framing). (5) `tests/binding-compile-check/SDL2.Core.CompileCheck.csproj` (Existing Evidence Inputs row 3 + Lane 4 prompt + Current Command Evidence command 2 + Tooling Guidance row 3) → confirm spike compile-check project path during Phase 2.5 purification. (6) `build/_build/Targets/GenerateBindings/Translation/MacroApiPolicy.cs` (Lane 5 prompt macro policy source) → spike postprocess equivalent (likely `postprocess/Janset.SDL2.PostProcess/MacroPolicy.cs` or analogous — confirm during Phase 2.5). (7) `build\_build.Tests\Build.Tests.csproj` (Current Command Evidence command 3 + Docker-mounted Linux fixture command) → spike's AbiTests project path `spikes/binding-generators/clangsharp/tests/abi-tests/AbiTests.csproj` per `raw-abi-upstream-testing-plan.md`. (8) `janset-binding-generator:focal-latest` Docker image tag + `SemanticHeaderFixtureTests` test filter (Docker-mounted Linux fixture command) → confirm spike's Docker image tag + analogous test filter during Phase 2.5; `docker/binding-generator.Dockerfile` reference per spec §3 outline survives. (9) Related Docs cross-reference paths (4 paths from `../binding-autogen/` and `binding-generator-maintenance.md`) → update to sibling canonical/ paths (file moves into `canonical/` subfolder so cross-references become local). (10) Trigger 3 `parse-views.json shows unexpected category/count movement` reframes for oracle.cs report categories. (11) Tooling Guidance row 2 (`parse-views.json`) replacement also references the *evidence-tool-gap-around-callback-typedef-inspection* discipline folded in from item-5-spec §3.12 audit (callback-typedef-evidence-limitations) — add as a new subsection or expanded row note. (12) Report Template "Generation command" + "Compile-check result" placeholder labels — update per spike toolchain. Everything else (Purpose + Principle + Source Hierarchy ranks 1-6 + What Must Stay Review-Led + Peer Binding Tiers + Finding Taxonomy + Recommended Agent Lanes 6-lane structure + Report Template 6-section shape + When-To-Build-A-Small-Internal-Tool methodology + Common Mistakes bullets 1-3+5-7) is **durable toolchain-neutral methodology** that survives the move with no content change. The move target is `spikes/binding-generators/docs/canonical/binding-output-oracle-validation.md` per spec §3 line 239; the purification happens in-place after the `git mv`.

### Files: .github/prompts/{binding-generator-spike-priority-c-handoff, satellite-expansion-research-cross-check, item-4-closure-item-5-mixer-handoff}.prompt.md

**Sections audited (all 3 prompts):**

- Spike-specific priming content (handoff state, slice scope, verification commands): work shipped; no durable kernel.
  - `binding-generator-spike-priority-c-handoff.prompt.md`: Priority C closure state snapshot (2026-05-24 — slice C-A/B/C summaries with commit IDs `adb64d0` / `45fdab6` / `247aac4` / `09957a7`; `37 commits ahead of origin` branch state; "Recommended Next Step" options Layer 2 / Alimer CppAst comparison / CI per-RID matrix). Architecture map, Constitution policy index, locked policy recap, opaque-handle invariants, mandatory grounding reading list — all reference live canonical docs (`priority-c-closure-summary.md` / `binding-generator-constitution.md` / `opaque-handle-roster.json` / `oracle-evidence-clangsharp.md` / ADR-004). Critical learnings (same-named tag+typedef shape, ClangSharp forward-declaration emit, SDL_iconv_t drift, SDL_BlitMap drift) already captured in `priority-c-closure-summary.md` per Q15 amendment scope.
  - `satellite-expansion-research-cross-check.prompt.md`: 2026-05-25 cross-check review prompt scaffolding (Agent 1 TTF / Agent 2 Mixer / Agent 3 GFX / Agent 4 Image / Agent 5 infrastructure sub-agent dispatch instructions). All durable findings (Mix_Chunk no-union claim, IMG_InitFlags missing [Flags] bug, satellite-owned opaque handle gap for TTF_Font + Mix_Music, include-directory assumption) live in the satellite analysis docs (`spikes/binding-generators/docs/satellites/sdl2-{ttf,mixer,gfx}-header-analysis.md` + `sdl2-image-cross-check.md`) — not in this prompt.
  - `item-4-closure-item-5-mixer-handoff.prompt.md`: 2026-05-27 Item 4 → Item 5 transition handoff (TTF implementation outcomes table — generation / config / RSP / C# project / C `long` dual-dispatch / no-SDLCALL functions / peer evidence; Item 4 commit message draft; Item 5 facts mirror `sdl2-mixer-header-analysis.md`; "Lessons To Carry From Item 4 Into Item 5" tactics — trust generated evidence not assumptions / don't preemptively create per-header RSPs / owner-mode handles work for non-`SDL_` names / oracle false positives can happen around generated helper mechanics / peer evidence is compatibility evidence not authority / runtime smoke can be scoped out when assets/lifecycle policy not ready). All durable kernels already in `spikes/binding-generators/docs/items/item-4-sdl2-ttf-layer-1-spec.md` + `item-4-sdl2-ttf-layer-1-plan.md` + `next-iteration-plan.md` Review Follow-up Backlog + `llm-handoff.md`.
- Generic prompt patterns (frontmatter `name`/`description`/`argument-hint`/`agent`/`model`; First Principle quote-block; Mandatory Grounding reading list; Working Rules / Guardrails / Locked Policy Recap; Required Onboarding numbered list): not spike-specific; covered by `.github/prompts/session-pickup-template.prompt.md` (kept per Q15 amendment).

**Verdict (all 3):** ready to git rm — empty unique-info kernel per spec §Q15 amendment. Spike-specific priming work is done (Priority C closure 2026-05-24 shipped; satellite expansion research artifacts persisted to `spikes/binding-generators/docs/satellites/` and consumed by Items 3-5; Item 4 closed via commit `1153b74` and Item 5 closed via commit `08b7e74`). No durable knowledge survives the slice that isn't already captured in canonical docs (Constitution / Closure Summary / Item specs+plans / satellite analyses / Review Follow-up Backlog / llm-handoff). The generic priming pattern survives via `session-pickup-template.prompt.md`.

### Files: spikes/binding-generators/output/reports/{oracle-comparison-clangsharp, oracle-comparison-alimer}.md

**Sections audited (both reports):**

- Legacy ClangSharp vs Cake preview comparison / Legacy Alimer vs Cake preview comparison: superseded by `oracle-evidence-clangsharp.md` (active oracle.cs output) + `iteration-2-comparison.md` (toolchain decision evidence). Flagged "legacy, not regenerated" in spike README L115-116. Both reports compare spike output against the now-retired Cake-generated preview at `artifacts/generated-bindings-preview/sdl2-core/` (sunset oracle path) — the comparison frame itself is obsolete now that the Cake toolchain is retired per ADR-004. Category-count tables (Functions/Enums/POD structs/Opaque handles/Constants/Callbacks) and Dynapi coherence data are reproduced (current-state, family-aware) by `oracle-evidence-clangsharp.md`; the spike-vs-Alimer toolchain decision is captured by `iteration-2-comparison.md`.
- Per-category set-diff details (`SDL_*` symbol enumerations of spike-only / oracle-only / common across Functions / Enums / POD structs / Opaque handles / Constants): point-in-time delta evidence against the retired Cake oracle — not durable; the *current* spike output is the canonical raw ABI surface per Constitution + Priority C closure, and any future delta-analysis runs against the live oracle (`oracle-evidence-clangsharp.md` produced by `oracle.cs` Roslyn analyzer).
- "Why this is not a 1:1 comparison" preamble paragraphs (ClangSharp-per-header vs Cake-per-category emission; opaque-handle inference; inline `delegate* unmanaged[Cdecl]<...>` vs named callbacks; macro pipeline computed-constants vs inline use-site): toolchain-neutral framing notes already absorbed into Constitution §"Generator Engine And SDL Policy" + Priority C closure summary (the per-header vs per-category emit shape is a known characteristic of the ClangSharp toolchain; opaque-handle classification is covered by the Pattern B handle policy + opaque-handle roster).

**Verdict (both):** ready to git rm — empty unique-info kernel per spec §Q15e.

### Files: .github/prompts/{general-deep-dive-code-reviewer, session-pickup-template}.prompt.md

**Sections audited (both templates):**

- `general-deep-dive-code-reviewer.prompt.md`: 500-line generic reviewer template covering reviewer contract (scope gate / review mission / core rules / evidence-and-confidence model / grounding protocol / review lenses / severity rubric / output contract / failure modes / success criteria) + repo-specific grounding pointers (`AGENTS.md`, `docs/onboarding.md`, `docs/plan.md`, `docs/decisions/2026-05-05-d3seg-and-package-first.md`, `docs/decisions/2026-05-05-target-centric-build-host.md`, `docs/decisions/2026-05-12-build-host-data-layer.md`, `docs/knowledge-base/release-guardrails.md`, `docs/knowledge-base/extraction-guidelines.md`, `docs/knowledge-base/testing-guidelines.md`, `docs/phases/phase-2-adaptation-plan.md`, `docs/playbook/local-validation.md`, `docs/playbook/overlay-management.md`) + working hypotheses about target-centric Cake build host — all pointers are root-level project docs and ADRs, none reference `docs/binding-autogen/`, the spike's `spikes/binding-generators/docs/`, retired closure docs (`llm-handoff` / `priority-c-closure-summary` / `generator-spike-goals` / `oracle-evidence-design` / `oracle-evidence-implementation-plan` / `satellite-expansion-roadmap`), or `.github/prompts/binding-generator-spike-handoff.prompt.md`. Mentions of `external/sdl2-cs` (transitional repo-local SDL2-CS clone) and `build/manifest.json` (canonical package/runtime source-of-truth) are generic repo-reviewer hypotheses, not binding-autogen specifics. No binding-autogen-specific content found.
- `session-pickup-template.prompt.md`: 136-line generic session-pickup template covering frontmatter placeholder shape (`name`/`description`/`argument-hint`/`agent`/`model`), body skeleton (Opening paragraph / First Principle / What Just Happened / Onboarding Snapshot / Current State / Recommended Next Step / Mandatory Grounding / Locked Policy Recap / Final Steering Note), drift-sensitivity table, authoring discipline rules (verify-before-claiming, anchor-to-commit-SHAs, drop-non-applicable-sections, don't-re-derive-AGENTS.md, sign-off-with-concrete-next-action). Mandatory Grounding numbered list cites `docs/onboarding.md`, `AGENTS.md`, `CLAUDE.md`, `docs/plan.md`, `docs/phases/{{active-phase-doc}}`, `docs/decisions/`, `docs/knowledge-base/release-guardrails.md` — all canonical project docs, none binding-autogen-specific. The Locked Policy Recap examples (master-direct commits / Cake owns orchestration / `BuildContext` is invocation state / `Tools/` is `Tool<TSettings>` wrappers / test naming / living-docs) are repo-wide invariants. No references to `docs/binding-autogen/`, retired closure docs, or `.github/prompts/binding-generator-spike-handoff.prompt.md`. No binding-autogen-specific content found.

**Verdict (both):** keep unchanged per spec §Q15 amendment. Both templates are generic instruction scaffolding aligned with the target-centric Cake build-host architecture; their durability is independent of the binding-autogen workstream. No Phase 7.2 edits required for either file.

### File: spikes/binding-generators/clangsharp/README.md

**Sections audited:**

- Title and one-line description ("ppy-Style ClangSharp Spike" + "tests whether ClangSharpPInvokeGenerator plus thin orchestration can produce the desired SDL2.Core plus SDL2_image binding shape with less owned code"): the SDL2.Core plus SDL2_image framing is **stale scope** — post-spike-closure the spike covers 5 families (Core + Image + GFX + TTF + Mixer) per `08b7e74` / `1153b74` / `3d085ab` commits. Refresh-only content drift, not a path/cross-ref issue.
- Commands block (3 `python spikes/binding-generators/clangsharp/generate_bindings.py` invocation examples covering `--family all` dry-run + `--family all --execute --vcpkg-triplet x64-windows-hybrid --use-platform-header-shims` full run + `--family core` single-family run): toolchain-path strings (`spikes/binding-generators/clangsharp/generate_bindings.py`) and flag names (`--family`, `--execute`, `--vcpkg-triplet`, `--use-platform-header-shims`) — all durable spike-local toolchain references. No `docs/binding-autogen/` paths, no Constitution/roadmap/testing-strategy line-number refs, no retired-doc refs.
- "dry-run by default" + `--execute` + Compat/Modern internal-backends framing paragraph: durable spike-orchestrator behavior description; no path/cross-ref items.
- `--use-platform-header-shims` paragraph (Windows-local spike aid for synthetic Linux/macOS/iOS platform passes; `shims/platform-headers/` for missing system SDK headers `endian.h`/`AvailabilityMacros.h`/`TargetConditionals.h`; do-not-treat-shim-output-as-final-production-evidence caveat; real platform generation needs native Linux/macOS runners): durable platform-shim mechanism description; the `shims/platform-headers/` path is a spike-relative subdirectory (not a `docs/binding-autogen/` doc path), and the production-evidence caveat is durable methodology already covered by `binding-output-oracle-validation.md` Linux-canonical-generation discipline.

**Verdict:** modify-in-place (path-refresh scope for Phase 7.2 recorded). **Phase 7.2 path-refresh scope is empty for this file.** No occurrences of:

- `docs/binding-autogen/...` paths (need rewrite to `docs/canonical/...` per Q11 + Phase 7.2 Step 2);
- `Constitution L\d+` / `Roadmap L\d+` / `Testing-Strategy L\d+` line-number cross-refs (need section-name refs per Rule 3 + Phase 7.2 Step 3);
- Retired-doc refs (`llm-handoff`, `priority-c-closure-summary`, `generator-spike-goals`, `oracle-evidence-design`, `oracle-evidence-implementation-plan`, `satellite-expansion-roadmap`) (need removal/replacement per Phase 7.2 Step 4);
- `.github/prompts/binding-generator-spike-handoff.prompt.md` references (stale prompt; file does not exist — confirmed via Glob).

Only stale-content refresh item identified (out of Phase 7.2 strict scope but worth flagging for Task 7.2 Step 2 reviewer): the line-3 framing "SDL2.Core plus SDL2_image binding shape" is family-count-stale post-Item 3/4/5 closure (`08b7e74` Mixer / `1153b74` TTF / `3d085ab` GFX) — the README's one-line description should reflect 5-family SDL2 Layer 1 closure (SDL2.Core + SDL2_image + SDL2_gfx + SDL2_ttf + SDL2_mixer) rather than the original 2-family Item 1 scope. Phase 7.2 is strictly path/cross-ref refresh per plan; the family-count-staleness is a content-refresh observation passed to Task 7.2 Step 2 for the reviewer to fold into a single edit pass or defer until a future content-refresh task. No additional fold needed into canonical docs.
