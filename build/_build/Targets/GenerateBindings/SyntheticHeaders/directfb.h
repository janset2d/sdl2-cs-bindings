#pragma once
// Stub <directfb.h>: SDL_syswm.h pulls this under SDL_VIDEO_DRIVER_DIRECTFB.
// PlatformCatalog never sets that macro, so this stub is defensive — it ensures
// future view additions can't break the parse just by enabling that driver.
